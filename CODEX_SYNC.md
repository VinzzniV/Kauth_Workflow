# CODEX_SYNC.md

Dieses Dokument ist das Handoff-Protokoll zwischen Codex und Claude.

## Zweck

- Codex traegt hier nach jeder abgeschlossenen Aufgabe einen Eintrag ein.
- Claude liest diese Datei am Anfang jeder Session, um sich ueber Codex-Aenderungen zu informieren.
- So entsteht kein blinder Fleck, wenn beide KIs am selben Repo arbeiten.

## Regeln

- Eintrag pro abgeschlossener Aufgabe (nicht pro Commit).
- Format: Datum | Aufgaben-ID | betroffene Dateien | kurze Zusammenfassung | offene Risiken.
- Wenn eine Aufgabe nur teilweise umgesetzt wurde, Eintrag mit Status `PARTIAL` und Erklaerung.
- Claude prueft bei jedem Sync-Eintrag, ob die Aenderung die eigene Arbeit (CLA-*) beeinflusst.
- Veraltete Eintraege (aelter als 4 Wochen, Task `done` in TODO.md) koennen entfernt werden.

---

## Sync-Log

| Datum | Aufgabe | Status | Betroffene Dateien | Zusammenfassung | Offene Risiken |
| 2026-05-03 | LA3 | done | `IWorkflowRepository.cs`, `PostgresWorkflowRepository.WorkflowQueryOperations.cs`, `PostgresWorkflowRepository.WorkflowCreateOperations.cs`, `PostgresWorkflowRepository.LinkOperations.cs`, `IWorkflowRuntimeService.cs`, `WorkflowRuntimeService.cs`, `IWorkflowCatalogService.cs`, `WorkflowCatalogService.cs`, `WorkflowEndpoints.cs`, `AdminRuntimeConfigEndpoints.cs`, `WorkflowMasterDataEndpoints.cs`, `WorkflowLinkEndpoints.cs`, `WorkflowDtos.cs`; Frontend: `workflowApi.ts`, `queryKeys.ts`, `workflowQueries.ts`; Tests: `AdminWorkflowDefinitionConfigEndpointsTests.cs`, `WorkflowEndpointsTests.cs`, `PostgresWorkflowRepositoryAuditLogIntegrationTests.cs` | **`legacyProcessTypeKey`-Filter-Params vollstaendig auf `workflowDefinitionKey` umgestellt.** Backend: `WorkflowListQuery.LegacyProcessTypeKey` entfernt (war paralleles Feld neben `WorkflowDefinitionKey`), SQL-Filter-Block entfernt, `GetWorkflowsAsync`-Signatur bereinigt. `CreateWorkflowRequest.LegacyProcessTypeKey` entfernt — `WorkflowCreateOperations` liest jetzt nur noch `WorkflowDefinitionKey`. Alle Catalog-Service-Parameter umbenannt: `GetRequirementsAsync`, `GetWorkflowConfigAsync` (beide Service + Interface), `GetDerivedAnswers` (Interface + Repository-Implementierung). Alle vier Endpoints: `legacyProcessTypeKey` → `workflowDefinitionKey` in Query-Params + Error-Messages. `WorkflowLinkEndpoints`: `targetLegacyProcessTypeKey` → `targetWorkflowDefinitionKey`. Frontend: `WorkflowQueryOptions.legacyProcessTypeKey` + zugehoerigen URL-Param-Block aus `buildWorkflowQuery` entfernt. `getWorkflowConfig` und `getDerivedAnswers` Params + gesendete URL-Params umbenannt. `queryKeys.workflows.config` + `useWorkflowConfig` umbenannt. Tests: Stub-Methoden (`GetRequirements`, `GetWorkflowConfig`, `GetDerivedAnswers`) in zwei Fake-Repos nachgezogen. `CreateWorkflowRequest` in 2 Testdateien von `LegacyProcessTypeKey` auf `WorkflowDefinitionKey` umgestellt. Query-String-Params in `WorkflowEndpointsTests` auf `targetWorkflowDefinitionKey` umgestellt. 378 Backend-Tests (377 Passed, 1 Skipped), TypeScript sauber. | `primaryLegacyProcessTypeKey` im Admin-Builder-Model (Frontend) wurde NICHT umbenannt — das ist der interne Builder-Feldname fuer den Version-Draft, nicht ein API-Parameter. Bleibt als LA5-Folgearbeit. `WorkflowNotificationDispatchTarget.LegacyProcessTypeKey` ist ein DB-gemapptes Feld (`notification_dispatch`-Tabelle) — unberuehrt, kein API-Parameter. `legacyProcessTypeKey` als JSON-Config-Key in Node-Configs (z.B. `{"legacyProcessTypeKey":"onboarding"}`) ist LA5-Scope. |
| 2026-05-03 | LA4 | done | `AuthorizationPolicyService.cs` | **`HasLegacyRolePermission()` entfernt.** Methode (20 Zeilen Role-Switch) + Fallback-Aufruf in `HasPermission` geloescht. `HasPermission` prueft jetzt ausschliesslich das DB-basierte Permission-System (`user.HasPermission`). Vier Methoden um explizite `HasAnyRole`-Zweige ergaenzt: `CanCreateOrStartWorkflow` (+Hr/Admin), `CanObserveWorkflow` (+Hr), `CanAccessSupervisorStep` (+Admin), `CanAccessTechnicalTasks` (+Admin). 378 Tests (377 Passed, 1 Skipped). DB-Inventur-Voraussetzung (offene Tasks ohne node_instance_id) wurde nicht gegen Prod geprueft — System nicht produktiv. | DB-basierte Permissions muessen fuer alle Nutzer korrekt gesetzt sein. Wer bisher nur ueber Rollen Zugriff hatte und keine expliziten Permissions hat, verliert den Fallback-Zugriff. Das ist die beabsichtigte Migrationsrichtung. |
| 2026-05-03 | LA2 | done | `WorkflowDefinitionValidationService.cs`, `PostgresWorkflowRuntimeRepository.cs`, `WorkflowDefinitionValidationServiceTests.cs`; Frontend: `adminWorkflowBuilderModel.ts`; Schema: `01_schema.sql` | **`setup`-Node-Type vollstaendig entfernt** (Legacy-Abbau Schritt 2). Backend: `"setup"` aus `AllowedNodeTypes` + `MeasureGenerationNodeTypes` in `ValidationService` und Runtime-Repository geloescht. `case "setup":` aus zwei Switch-Statements (errors + issues) entfernt. Validierungsbedingung `string.Equals(measureNode.NodeType, "setup", ...)` in `ValidateMeasurePhaseConsistency` geloescht — `setup` wird nicht mehr als gueltiger Alias fuer measure-Nodes akzeptiert. Test `ValidateSnapshot_AllowsLegacySetupAliasForMigratedProcess` entfernt. Frontend: `normalizeLifecycleMeasureNodeType`/`normalizeLifecycleMeasureNodeTitle` mit setup-Zweig + `primaryLegacyProcessTypeKey`-Parameter komplett inlined und geloescht — `toNodeDraft` ist jetzt ein einfaches Mapping ohne Legacy-Normalisierung. Schema: `'setup'::character varying` aus CHECK-Constraint `workflow_nodes_node_type_check` entfernt. 378 Backend-Tests (377 Passed, 1 Skipped), TypeScript sauber. | DB-Inventur Produktion wurde NICHT durchgefuehrt — es koennte noch `type = 'setup'`-Rows in `workflow_nodes` fuer published Versions existieren. Normalisierungs-Code im Frontend (der `setup` → `measure_*` konvertiert haette) ist jetzt entfernt. Falls solche Rows existieren, wuerde der Builder beim Laden einer Definition mit `setup`-Node `normalizeNodeType` mit einem unbekannten Typ aufrufen, was zu einem Fallback fuehren wuerde. Risiko ist gering (System nicht produktiv laut bisherigen Eintraegen), aber vor Produktion-Go-Live sollte ein `SELECT COUNT(*) FROM workflow_nodes WHERE node_type = 'setup'` laufen. |
| 2026-05-02 | LA1 | done | `WorkflowStatusRules.cs`, `WorkflowRuntimeDtos.cs`, `WorkflowDtos.cs`, `PostgresWorkflowRepository.WorkflowQueryOperations.cs`, `PostgresWorkflowRepository.ReadOperations.cs`, `PostgresWorkflowRuntimeRepository.cs`, `WorkflowDefinitionRuntimeService.cs`; Tests: `WorkflowStatusRulesTests.cs`, `EndpointSupportTests.cs`, `AdminWorkflowRuntimeEndpointsTests.cs`, `WorkflowEndpointsTests.cs`, `PostgresWorkflowRepositoryWorkflowDefinitionIntegrationTests.cs`; Frontend: `workflow.ts`, `backendDtos.ts`, `mappers.ts` | **`LegacyWorkflowStatus` vollstaendig entfernt** (Legacy-Abbau Schritt 1). Backend: `OpenLegacy` + `ToLegacyStatus()` aus `WorkflowStatusRules` geloescht. `LegacyWorkflowStatus`-Feld aus `WorkflowDefinitionRuntimeDetailDto` entfernt, SQL-Reader-Indices nachgezogen (8-18 → 7-17). `Status`-Feld aus `WorkflowListItemDto`, `WorkflowDetailDto`, `PersonWorkflowSummaryDto` entfernt; alle 3 `Status = ToLegacyStatus(...)`-Aufrufe in Repository-Partials geloescht. `WorkflowDefinitionRuntimeService`: `LegacyWorkflowStatus == "completed"` → `CurrentRuntimeStatus == "completed"` (Nebeneffekt: Lifecycle-Projection wird nicht mehr bei `failed`-Workflows ausgeloest). Frontend: `WorkflowStatus`-Typ + `status`-Felder aus Typen + `status: string` aus Backend-DTOs + `status:`-Zeilen aus `mappers.ts` entfernt. 333 Backend-Unit-Tests + 172 Frontend-Tests gruen, TypeScript sauber. | `workflows.status`-Spalte wird weiterhin beschrieben (SetWorkflowRuntimeState), aber nicht mehr per DTO exponiert. Spalte kann in einem spaeteren DB-Cleanup-Schritt gedroppt werden, wenn alle Legacy-Lesepfade (alter Workflow-Code) ebenfalls migriert sind. |
| 2026-05-02 | LQ2-Z4 | done | `AppErrorBoundary.tsx`, `AdminConfigPage.tsx`, `WorkflowDetailPage.tsx` | `AppErrorBoundary` um `inline`-Prop erweitert: bei `inline=true` wird nur ein Panel gerendert statt der ganzen Seite. `AdminConfigPage`: 2 Wraps (Sidebar + Workspace-Content). `WorkflowDetailPage`: 2 Wraps (Tasks-Section + Secondary-Stack mit Links/Management/Notifications/AuditLog). TypeScript sauber. | Kein Test-Aenderung — Error-Boundaries sind Laufzeit-Schutz. Bei einem Render-Fehler in einer Sub-Section sieht der User jetzt ein Inline-Fehlerpanel statt eines Page-Crashes. |
| 2026-05-02 | LQ1-Z4 | done | `PostgresWorkflowRuntimeRepository.cs` | `ParseDecisionCondition`-Aufruf in `ResolveDecisionTarget` in try-catch eingebettet. Bei `InvalidOperationException` wird die Exception mit Source- und Target-NodeKey der Edge angereichert (via `graph.NodeById` Lookup). Format: "Decision condition on edge 'NodeA' → 'NodeB' is invalid: ...". Kein Verhalten geaendert, nur Fehlermeldung. | Keine. |
| 2026-05-02 | HQ2-Z4 | done | `IWorkflowRepository.cs`, `IRotationRepository.cs`, `TaskApplicationService.cs`, `PostgresWorkflowRepository.TaskMetadataOperations.cs`, `PostgresWorkflowRepository.ReadOperations.cs`, `PostgresRotationRepository.TaskGenerationOperations.cs`; Tests: `AdminWorkflowDefinitionConfigEndpointsTests.cs`, `WorkflowEndpointsTests.cs`, `RotationNotificationServiceTests.cs`, `RotationPlanningServiceTests.cs`, `RotationTemplateAdminServiceTests.cs` | **`responsibilityIds` → `effectiveResponsibilityIds`** in Methoden-Signaturen und Struct-Field. `WorkflowTaskListNarrowingFilter.ResponsibilityIds` → `EffectiveResponsibilityIds`. Lokale Var in `TaskApplicationService` umbenannt. `IRotationRepository.GetRotationTaskEnvelopesForUser` + alle 3 Test-Stubs angepasst. 384 Backend-Tests gruen. | Rein mechanischer Rename — kein Verhalten geaendert. Frontend hat keinen direkten Zugriff auf diese Signaturen. |
| 2026-05-02 | HQ1-Z4 | done | `WorkflowDtos.cs`, `WorkflowEndpoints.cs`, `AdminRuntimeConfigEndpoints.cs`, `WorkflowMasterDataEndpoints.cs`, `WorkflowLinkEndpoints.cs`, `IWorkflowRuntimeService.cs`, `IWorkflowCatalogService.cs`, `WorkflowCatalogService.cs`, `WorkflowRuntimeService.cs`, `INotificationTemplateService.cs`, `NotificationTemplateService.cs`, `GraphWorkflowEmailNotificationSender.cs`, `IWorkflowRepository.cs`, `PostgresWorkflowRepository.*.cs` (6 Partials), `PostgresWorkflowNotificationReadRepository.cs`, `PostgresWorkflowNotificationDispatchOperations.cs`, `web/src/services/workflowApi.ts`, `web/src/services/queries/workflowQueries.ts`, `web/src/services/queryKeys.ts`, `web/src/types/workflow.ts`; Tests: 3 Test-Dateien; Doku: `KauthWorkflow/Architektur/Entscheidungen.md` | **`ProcessTypeKey` → `LegacyProcessTypeKey`** Rename (HQ1-Z4). Alle oeffentlichen API-Parameter, DTOs und interne Service-Signaturen umbenannt. Query-Params: `processTypeKey` → `legacyProcessTypeKey` in 4 Endpoints; `targetProcessTypeKey` → `targetLegacyProcessTypeKey` in derive-answers. `CreateWorkflowRequest.LegacyProcessTypeKey` + `WorkflowNotificationDispatchTarget.LegacyProcessTypeKey`. `WorkflowListQuery.LegacyProcessTypeKey`. Frontend: `WorkflowQueryOptions.legacyProcessTypeKey` + URL-Params aktualisiert. 384 Backend-Tests gruen, TypeScript sauber. Compat-Doku mit Endpoint-Tabelle + Deprecation-Ziel in `Entscheidungen.md` hinzugefuegt. | Frontend-Aufrufstellen (Pages/Hooks) die `processTypeKey` als Named-Prop uebergaben hatten keine Aenderungen — Typ-Pruefung bestaetig, da TS-Check sauber. Callers von `useWorkflowConfig` und `getDerivedAnswers` verwenden Positionsargs, kein Breaking Change. |
|-------|---------|--------|--------------------|-----------------|----------------|
| 2026-05-02 | Z4-Review | done | **Neuer Code-Review-Zyklus 4** in `CODE_REVIEW.md` aufgesetzt (vom Nutzer angefragt mit explizitem Naming-/Lesbarkeits-Audit). Methodik: Explore-Agent fuer breite Suche + manuelle Verifikation jeder Behauptung mit `wc -l`/grep/Read. **Drei Agent-Befunde als Falsch-positiv verworfen + dokumentiert**: (1) "GetAdminWorkflowDefinitionVersionDetailById mutiert" — falsch, ist read-only via verifiziertem Read; nur der Wrapper `GetOrCreateAdminWorkflowDefinitionWorkingDraft` mutiert (Name ist explizit). (2) "tt/pt/wta sind C#-Variablen" — falsch verifiziert via `grep -nE "var (tt|pt|wta) ="`; sind SQL-Tabellen-Aliase in `@"..."`-Strings; standard SQL-Konvention. (3) "NormalizeNullableText braucht Rename" — "normalize" ist im Codebase-Vokabular etabliert. **Befunde:** 0 CRITICAL, 2 HIGH (HQ1-Z4 `ProcessTypeKey`→`LegacyProcessTypeKey`, HQ2-Z4 `responsibilityIds`→`effectiveResponsibilityIds`), 4 LOW (LQ1-Z4 ParseDecisionCondition-Error-Kontext, LQ2-Z4 Sub-Section-Error-Boundaries, LQ3-Z4 + LQ4-Z4 Naming-Refinements als defer). **Lesbarkeits-Audit:** Konventionen durchgaengig, neue Note "Lesbarkeit B+" eingefuehrt. **TODO.md** komplett umstrukturiert: Zyklus 3 (jetzt abgeschlossen) und Zyklus 2 in kompakte Done-Tabellen verschoben; Zyklus 4 als „Aktiver Zyklus" mit Reihenfolge-Empfehlung (HQ1-Z4 → HQ2-Z4 → LQ1-Z4 → LQ2-Z4). Restposten-Sektion auf 3 Items reduziert (R8, R10, L2). **vault `Code-Review-Status.md`**: Zyklus-3-Tabelle auf „abgeschlossen" + Zyklus-4-Tabelle „aktiv" + Gesamtbewertung mit neuer Lesbarkeits-Spalte. **Code-Aenderungen:** keine — diese Session ist reine Review/Doku-Bereinigung. | Zyklus 4 ist aufgesetzt + TODO.md bereinigt. Naechste Aufgabe per Reihenfolge: HQ1-Z4 (ProcessTypeKey→LegacyProcessTypeKey Rename + Compat-Doku, 2-3 h, medium reasoning, sonnet). | (1) Naming-Audit hat keine kritischen Probleme aufgedeckt — der Codebase ist strukturell und stilistisch in gutem Zustand. Die verbleibenden HIGH-Items sind Komfort-Aenderungen, keine Risk-Mitigations. (2) Trust-but-verify-Prinzip hat sich erneut bewaehrt — 3 von ~15 Agent-Befunden waren falsch oder ueberbewertet. Manuelle Verifikation pro Behauptung bleibt Pflicht. (3) Watch-Items aus Z3 (LQ2-Z3 EntraSync, LQ4-Z3 AdminConfigPage) unveraendert — keine Trigger seit letzter Review. |
| 2026-05-02 | Z3-HQ2 | done (partial) | **Neu:** `web/src/hooks/useAdminWorkflowVersionReferenceData.ts` (140 Zeilen). Pure-React-Hook mit Args `(versionDraft, definitions)` und Return-Type `AdminWorkflowVersionReferenceData = { taskTemplates, answerDefinitions, taskTemplateConditions, taskTemplateDependencies }`. Internes useState mit `EMPTY`-Konstante als Initialwert. Single useEffect: ermittelt referenzierte legacyProcessTypeKeys aus `versionDraft.primaryLegacyProcessTypeKey` und parsed Node-Configs (try-catch um JSON.parse), mappt auf `definitions[].id`, lädt parallel `getAdminTaskTemplates` + `getAdminAnswerDefinitions`, dedupliziert per template-key/composite-key, lädt dann pro Template `getAdminTaskTemplateConditions` + `getAdminTaskTemplateDependencies`. cancellation-flag verhindert race-Conditions; catch-all setzt EMPTY. Dependencies-Array `[definitions, versionDraft.nodes, versionDraft.primaryLegacyProcessTypeKey]` — re-laedt nur bei wirklichen Aenderungen. **Geaendert:** `web/src/hooks/useAdminWorkflowBuilder.ts` 865 → **761** Zeilen (-12%, -104 Zeilen). Importe: 4 unused getAdminXxx-Funktionen + 4 unused Type-Imports entfernt. State-Slots: `taskTemplates`, `answerDefinitions`, `taskTemplateConditions`, `taskTemplateDependencies` (alle 4 useState-Slots) entfernt — kommen jetzt aus `useAdminWorkflowVersionReferenceData(versionDraft, definitions)`. `resetLoadedVersion` und `applyLoadedVersionDetail` haben 4 manuelle `setXxx([])`-Calls verloren — die Reference-Data-Hook resettet sich automatisch wenn versionDraft.nodes leer ist (kein referenzierter ProcessType). 99-Zeilen-useEffect (alter Loader-Block) komplett entfernt via `sed -i '263,361d'`. Hook-Call platziert direkt nach `activeActionDefinitionsByKey`-useMemo, vor `loadDefinitions`-useCallback. **Hook-API zur FormSection unveraendert** — Return-Object hat weiterhin `taskTemplates`, `answerDefinitions`, `taskTemplateConditions`, `taskTemplateDependencies`-Properties, jetzt aus dem Sub-Hook destructured. **Build:** `npx tsc --noEmit` clean. **Tests:** Frontend `npm test` **172 passed** (unveraendert gegenueber HQ1-Z3 — keine Verhaltens-Aenderung, rein refactoring; nutzt die jetzt isolierte Loading-Logik aber dieselben Aufrufe wie vorher). | HQ2-Z3 partial done — Reference-Data-Loading ist jetzt in einem dedizierten Hook gekapselt (testbar isoliert wenn jemand will). Main-Hook ist 12% kleiner. Note Frontend-Architektur B → B+. | (1) **3 weitere Sub-Hook-Splits deferred** — `useAdminWorkflowDefinitionSelection`, `useAdminWorkflowVersionDetail`, `useAdminWorkflowBuilderValidation`. Sind eng mit den verbleibenden 12 useState-Slots verzahnt: `loadDefinitions` setzt sowohl Reference-Data als auch Selection-State, `saveVersion` greift auf 4 verschiedene State-Slots, `applyLoadedVersionDetail` fanned out in mehrere. Ein sauberer Split braucht erst eine Trennung der Tx-aehnlichen Update-Pfade — separater Folge-Task wenn Hook weiter waechst. (2) Keine Tests fuer den neuen Reference-Data-Hook geschrieben — er ist verhaltens-aequivalent zum alten useEffect, wird durch die existierenden Builder-Tests indirekt abgedeckt. Direkter Test waere wertvoll wenn die Loading-Logik in Zukunft verfeinert wird (z. B. Partial-Reload pro Definition). (3) Hook-Args sind 2 grosse Objekte (`versionDraft` + `definitions`) — useState-Identitaeten triggern useEffect bei jedem Render der Parent-Hook. Aktuell kein Performance-Problem (versionDraft wird nur bei echten Aenderungen ersetzt), aber bei Memoization-Issues koennte ein finer-grained Args-Set helfen. |
| 2026-05-02 | Z3-HQ3 | done (partial) | **TaskTemplate-Familie 3-fach gesplittet:** Original-Datei `PostgresWorkflowRepository.TaskTemplateAdminOperations.cs` 1017 → **391** Zeilen (-61%) — behaelt Templates-CRUD (`Get/Create/Update/Delete AdminTaskTemplate`) + cross-cutting Helpers (`MapAdminTaskTemplate`, `Validate/BindAdminTaskTemplateParameters`, `NormalizeAdminTaskTemplateCategory`, `NormalizeNullableText` (auch von Conditions genutzt), `EnsureProcessTypeExists` (auch von Dependencies genutzt), `EnsureTemplateKeyAvailable`, `TemplateHasWorkflowTaskReferences`, `GetAdminTaskTemplateById` (auch von Conditions+Dependencies genutzt)). **Neu:** `PostgresWorkflowRepository.TaskTemplateConditionOperations.cs` (251 Zeilen) — Conditions CRUD (`Get/Create/Delete AdminTaskTemplateCondition`) + `MapAdminTaskTemplateCondition`/`Validate`/`Bind`/`NormalizeOperator`/`EnsureAnswerKeyBelongsToProcessType`/`GetAdminTaskTemplateConditionById` + `AllowedAdminTaskTemplateConditionOperators`-Konstante. **Neu:** `PostgresWorkflowRepository.TaskTemplateDependencyOperations.cs` (391 Zeilen) — Dependencies CRUD (`Get/Create/Delete AdminTaskTemplateDependency`) + `GetAdminDependencyGraph` + `MapAdminTaskTemplateDependency`/`Validate`/`Bind`/`NormalizeStatus`/`LoadAdminTaskTemplateDependencyGraph`/`AdminTaskTemplateDependencyExists`/`WouldCreateDependencyCycle`/`GetAdminTaskTemplateDependencyById` + `AllowedAdminTaskTemplateDependencyStatuses`-Konstante. Cross-File-Helpers werden via partial-class-Mitgliedschaft erreicht. **WorkflowDefinition: GraphMapping ausgelagert, Versions-Split deferred:** Original `PostgresWorkflowRepository.WorkflowDefinitionAdminOperations.cs` 1430 → **1103** Zeilen (-23%). **Neu:** `PostgresWorkflowRepository.WorkflowDefinitionGraphMappingOperations.cs` (350 Zeilen) — `BuildWorkflowDefinitionValidationSnapshot` (74-Zeilen-Methode), `MapValidationIssues`, `CreateDefinitionReferenceIssue`, `TryGetNodeConfigValue`, `ToDraftNode`, `ToDraftEdge`, `PersistWorkflowDefinitionVersionGraph` (160-Zeilen-Methode), `HasWorkflowNodePositionColumns`. Cross-File-Helpers `LoadDefinitionRequiresSupervisorStep`, `LegacyProcessTypeExists`, `LegacyTaskTemplateExists`, `NormalizeWorkflowDefinitionOptionalText` bleiben in WorkflowDefinitionAdminOperations.cs und werden via partial-class erreicht. **Versions-Split bewusst deferred** — die Version-Methoden (CreateAdminWorkflowDefinitionVersion, GetOrCreateAdminWorkflowDefinitionWorkingDraft, ReplaceAdminWorkflowDefinitionVersion, GetAdminWorkflowDefinitionVersionDetailById etc.) sind eng mit `CreateAdminWorkflowDefinition` verzahnt (gemeinsame `BeginTransactionAsync` + `InsertWorkflowDefinitionVersion` atomisch im selben Tx-Scope). Ein sauberer Split braucht erst eine Refactor-Pass-Through der Tx-Boundary; aktuell zu hohes Regressions-Risiko. **Test-Stub-Updates:** keine — die neuen Partials sind alle `internal sealed partial class PostgresWorkflowRepository`, oeffentliche Interface-Methoden unveraendert. **Build:** `dotnet build` clean, 0 Warnings, 0 Errors. **Tests:** Backend `dotnet test` ohne Filter: **384 passed, 1 skipped, 0 failed** (unveraendert gegenueber HQ1-Z3 — keine Verhaltens-Aenderung, rein syntaktischer Move). | HQ3-Z3 partial done — TaskTemplate-Familie ist jetzt sauber 3-fach gegliedert mit klaren Verantwortlichkeits-Grenzen (Templates / Conditions / Dependencies). WorkflowDefinition hat GraphMapping als eigenes Konzern. Note Backend-Architektur B+ → A-. | (1) **Versions-Split offen** — wenn die Versions-Methoden weiter wachsen oder ein Refactor des Tx-Scopes ansteht, sollte das nachgezogen werden (separater Folge-Task). Aktuell deferred. (2) **Cross-File-Helper-Konvention** — die geteilten privat-statischen Helpers sind im Original-File mit Kommentar markiert ("Shared helper — also referenced by ..."). Wer einen Helper umzieht, muss die Kommentare nachziehen. (3) Keine neuen Tests fuer die ausgelagerten Methoden — der Move ist verhaltens-aequivalent und die existierenden Integration-Tests deckten die Pfade indirekt ab. (4) `EnsureProcessTypeExists` ist namensmaessig ein Legacy-Begriff aus der pre-WorkflowDefinition-Aera — operiert auf workflow_definitions.id. Umbennenen waere Folge-Cleanup. |
| 2026-05-02 | Z3-LQ1 | done | **Geaendert:** `api/API/Services/RotationNotificationHostedService.cs` (35 → 47 Zeilen). Neue private-static Konstante `SweepTimeout = TimeSpan.FromHours(2)`. In `ExecuteAsync` umschliesst `using var sweepCts = CancellationTokenSource.CreateLinkedTokenSource(stoppingToken); sweepCts.CancelAfter(SweepTimeout);` jeden `rotationNotificationService.ExecuteDailySweepAsync(sweepCts.Token)`-Call. Neuer `catch (OperationCanceledException ex)`-Block (separat vom shutdown-Filter) loggt mit `LogWarning("...exceeded {TimeoutMinutes}-minute timeout; retrying after backoff.", SweepTimeout.TotalMinutes)` und faellt in den existierenden 5-min-Retry-Pfad. Shutdown-Pfad (`when (stoppingToken.IsCancellationRequested)`) bleibt unveraendert und bricht die Schleife. **Tests:** Backend Unit-Tests **326 passed**, 0 failed (gleicher Stand wie vor dem Patch — keine neue Test-Coverage geschrieben, weil der Code 2 SLoC ist und ein Mock-basierter Timeout-Test disproportional waere). | LQ1-Z3 erledigt — stille 24-h-Verzoegerung bei haengendem Sweep ist nicht mehr moeglich. Worst-Case: 2 h + 5 min Backoff = ~125 min bis zum Retry, statt 24 h Stille. | (1) Kein Timeout-Test geschrieben — der Pfad ist trivial und das tatsaechliche Hangen einer DB-Query ist im Test schwer zu reproduzieren ohne echtes Postgres + WAL-Lock. (2) `SweepTimeout` ist hardcoded 2 h — bei Bedarf koennte das per `IOptions<NotificationOptions>` env-konfigurierbar werden, aber aktuell kein Anlass. (3) Healthcheck-Endpoint hat keinen Hinweis auf den letzten Sweep-Status — nicht im Scope dieser Aufgabe, koennte als LQ-Z4-Folgearbeit kommen wenn Operability weiter wichtig wird. |
| 2026-05-02 | Z3-HQ1 | done | **Neu:** `web/tests/useRotationOperationsView.test.tsx` (~250 Zeilen, 18 Tests). Pure-Helper-Tests fuer `isOperationallyOpen` und `isUpcomingAnchorDate` (Tagesgrenzen-Edge-Cases mit Window=0/14/15 + past-dates + null + NaN-Strings). Hook-Integration-Tests via `renderHook` aus `@testing-library/react` mit lokalem `QueryClientProvider`+`CurrentUserContext`-Wrapper; mockt `getMyTasks`. Verifiziert: Non-Rotation-Filter (taskFamily-Check), Search-Predicate ueber alle 5 Felder, departmentFilter, onlyOpen-Default-true, `inferredOwnDepartmentId`-Resolve aus `permissionScopes`, `upcomingChanges`-Aggregation per `${planId}:${anchor}:${trigger}`-Key, changeWindow-Cutoff (3-day-task in window=14 ja, 60-day-task nein), departmentSummaries (count+openCount), personSummaries-earliest-anchor-Selection. **Helper-Quirk umgangen**: Initial verwendete der Test `target.toISOString().slice(0,10)`, was in non-UTC-Timezones um 1 Tag drifted — neu rebuilt mit `getFullYear()/getMonth()/getDate()`-padStart, damit der lokale Datum-String dem `midnightToday`-Vergleich des Helpers entspricht. **Neu:** `web/tests/useRotationStationForm.test.tsx` (~210 Zeilen, 14 Tests). Pure-Helper-Tests fuer `createEmptyStationForm` (Default + custom orderIndex), `buildStationForm` (mit null-location/notes-Normalisierung), `normalizeStationPayload` (trim auf optionals + `Number("")===0`-Sentinel + NaN fuer "abc"). Hook-Integration mit lokalem `ToastProvider`+`QueryClientProvider`-Wrapper; mockt `createRotationStation`/`updateRotationStation`. Verifiziert: `openCreateStationForm` seedet orderIndex aus `orderedStationsCount`, `openEditStationForm` populiert form-state + tracking-id, `resetStationForm` clear-all, `handleSaveStation` rejected leere Form ohne API-Call, `handleSaveStation` ruft `createRotationStation` ohne editing-id und `updateRotationStation` mit editing-id, Form-Reset nach erfolgreichem Save. **Quirk dokumentiert**: `Number("")` returns `0` in JS — der Save-Handler-Guard `payload.departmentId <= 0` traegt die Sentinel-Logik (Test asserts `payload.departmentId === 0`). **Build:** `npx tsc --noEmit` clean. **Tests:** Frontend `npm test` **172 passed** (vorher 140, **+32 neue Tests**, 39 Test-Files). | HQ1-Z3 erledigt — beide HQ5-Hooks haben jetzt direkte Test-Coverage. Pure-Helper-Anteil ist hoch (~12 Tests), Hook-Integration ueber `renderHook` deckt die State-Transitions ab. Refactor-Aequivalenz nicht mehr nur durch Page-Tests gesichert. | (1) `reloadPlanData`-Cache-Invalidation-Targets sind nicht im Detail asserted — nur dass `createRotationStation` aufgerufen wird und Form danach zurueckgesetzt ist. Vollstaendiger queryKey-Match-Test waere zusaetzliche 30-50 Zeilen, koennte als Folgearbeit kommen wenn Cache-Drift in Praxis auftritt. (2) Toast-Calls (showError/showSuccess) wurden nicht direkt geprueft — der Test prueft das beobachtbare Verhalten (API-Call vs. kein-API-Call), nicht den Side-Effect-Toast. (3) `handleDeleteStation` und `handleRegenerateTasks` haben noch keine Tests — Folgearbeit wenn diese Pfade einen Bug zeigen, aktuell beide trivial-defensiv geschrieben. |
| 2026-05-02 | Z3-Review | done | **Neuer Code-Review-Zyklus 3** in `CODE_REVIEW.md` aufgesetzt. Methodik: breiter Explore-Agent-Sweep + gezielte Verifikation jeder Behauptung per `wc -l` und Read. Eine Agent-Behauptung („Transaktions-Luecke in WorkflowRuntimeService.CreateWorkflowAsync") wurde als **falsch positiv** verworfen — Repository-Pfad oeffnet `BeginTransactionAsync`. **Befunde:** 0 CRITICAL, 3 HIGH (HQ1-Z3 HQ5-Hooks ohne Tests; HQ2-Z3 useAdminWorkflowBuilder 865 Zeilen; HQ3-Z3 Repository-Partials 1430+1017 Zeilen splitten), 4 LOW (LQ1-Z3 RotationNotification-Sweep ohne Timeout, LQ2-Z3 EntraSync 2222 Zeilen defer, LQ3-Z3 Doku-Drift, LQ4-Z3 AdminConfigPage 423 Zeilen Watch). LQ3-Z3 sofort erledigt: `KauthWorkflow/Stand/Code-Review-Status.md` neu geschrieben mit Zyklus-1/2/3-Tabellen + aktuelle Gesamtbewertung. **TODO.md** umstrukturiert: Zyklus 2 zu „Abgeschlossen" verschoben, Zyklus 3 als „Aktiv" mit Reihenfolge-Empfehlung LQ3-Z3 → HQ1-Z3 → LQ1-Z3 → HQ3-Z3 → HQ2-Z3. **Code-Aenderungen:** keine — diese Session ist reine Review/Doku. | Zyklus 3 ist aufgesetzt. Naechste Aufgabe per Reihenfolge: HQ1-Z3 (Tests fuer HQ5-Hooks). | (1) Tests fuer LQ3-Z3-Doku-Drift gibt es nicht — Vault-Synchronitaet bleibt manueller Pflicht-Schritt am Ende jedes Zyklus (in TODO.md festgehalten). (2) Agent-Befund-Validierung war wertvoll — Falsch-positiv gefunden. Prinzip „trust but verify" sollte bei jedem Agent-Report durchgezogen werden. |
| 2026-05-02 | Z2-LQ-Bundle | done | **LQ7 (Hook-Tot-Code):** `web/src/hooks/useAdminWorkflowBuilder.ts` — `refreshReferenceData`-Definition + Export aus dem Return-Objekt entfernt; ungenutzten `referenceReloadTick`-State (war einziger Setter-Caller) inkl. dependency-Array-Eintrag entfernt. `loadVersionDetail` bleibt — interner Caller in `createDefinition` (Z. 637, 648). **LQ3 (Sim-Login-Untertitel):** `api/API/Contracts/AuthModels.cs` — `SimulationLoginUserOptionDto.RoleKeys: IReadOnlyList<string>` ergänzt. `api/API/Repositories/PostgresUserAuthorizationRepository.cs` — `GetSimulationLoginUsers`-SQL um Subquery erweitert: `(SELECT COALESCE(array_agg(DISTINCT r.role_key ORDER BY r.role_key), '{}') FROM (UNION von app_user_roles + directory_group_role_mappings via directory_group_members) effective JOIN app_roles r ...)`; Reader wandelt `string[]` per `GetFieldValue<string[]>(5)`. `web/src/types/auth.ts` + `web/src/pages/SimulationLoginPage.tsx` — neue `roleKeys`-Property, im Dropdown-Eintrag (`displayName (username) – roleA, roleB` oder `– keine Rolle`) und im Detail-Subtitle (`| Rollen: ...`). **LQ4 (Notification-Linter):** war bereits seit `NotificationTemplateService.ValidatePlaceholders` (Z. 465) implementiert; Test `NotificationTemplateServiceTests.UpdateAdminTemplate_RejectsUnknownPlaceholder` verifiziert das. CODE_REVIEW-Note war stale; nur Doku-Update notwendig. **LQ1 (Property-Catalog-Endpoint):** Neu `api/API/Services/AutomationPropertyCatalog.cs` — statischer Catalog mit Source-Konstanten + 3 camelCase-Property-Listen (workflow/target_person/directory_identity), passend zum Backend-Resolver `PostgresWorkflowAutomationOperations.ResolveAutomationReference`. Liefert `AutomationPropertyCatalogDto { Sources[] { Source, Label, Properties[] } }`. Neu `GET /admin/config/automation-property-catalog` in `AdminWorkflowDefinitionConfigEndpoints.cs` (Auth: `CanManageWorkflowBuilderAdvanced`, gleiche Scope wie `/action-definitions`). Frontend: neue Type + Loader `getAdminAutomationPropertyCatalog` in `web/src/services/adminConfigApi.ts`. `useAdminWorkflowBuilder` lädt Catalog in `loadDefinitions` (catch-all swallow weil optional) und exposed `automationPropertyCatalog` im Return. `WorkflowBuilderStepCard` und `WorkflowBuilderActionEditor` reichen es durch. `WorkflowBuilderActionMappingEditor` hat jetzt `propertyCatalog`-Prop, Helper `resolveProperties(catalog, source, fallback)` mit `FALLBACK_*_PROPERTIES`-Konstanten als Safety-Net falls Endpoint fehlschlaegt. ParameterRow-Sub-Komponente nimmt `workflowProperties`/`personProperties`/`directoryProperties` als Props statt der ehemals modulebenen Konstanten. **Bug-Fix-Nebeneffekt:** Die alten Frontend-Konstanten hatten zwei Mismatches gegen das Backend (`currentPositionRoleId` statt `roleId`, kein `personId`) — der Catalog bringt das in Sync. **LQ2 (Mapping-Editor flat-only):** `KauthWorkflow/Architektur/Entscheidungen.md` um Eintrag „Action-Mapping-Editor bleibt flach (LQ2, 2026-05-02)" erweitert mit Begruendung (kein produktiver Bedarf; Power-Modus deckt nested-Schemas ab; Erweiterung erst bei mehrfachem Auftauchen). **Build:** `dotnet build` clean, `tsc --noEmit` clean. **Tests:** Backend `dotnet test` ohne Filter: **384 passed, 1 skipped, 0 failed** (unveraendert gegenueber HQ2 — keine neuen Tests, keine Regressionen). Frontend `npm test`: **140 passed** (unveraendert). | Komplettes LQ-Bundle erledigt. Workflow-Builder-Property-Listen sind jetzt single-source-of-truth via Backend-Catalog (mit Fallback fuer Resilienz). Sim-Login zeigt Rollen sichtbar, neue Devs muessen nicht mehr raten. Hook-API ist sauberer (kein versteckter Tot-Code mehr). Mapping-Editor flat-only-Limitation ist eine bewusste Architektur-Entscheidung mit dokumentiertem Aufweich-Trigger. | (1) Catalog ist statisch im C# kodiert — wenn ein neues Property zum Resolver kommt, muss die Catalog-Liste manuell nachgezogen werden (Code-Kommentar warnt davor). Eine reflection-basierte Auto-Extraktion waere moeglich, aber overkill. (2) `RoleKeys`-Ausgabe im Sim-Login zeigt nur die Role-Keys (nicht den DisplayName) — Format ist kompakt und ausreichend fuer Dev-Kontext. (3) Property-Catalog-Endpoint ist auf `CanManageWorkflowBuilderAdvanced`-Scope gegated, gleiches Recht wie der Action-Catalog — wer den Mapping-Editor sieht, sieht auch den Catalog. |
| 2026-05-02 | Z2-HQ2 | done | **Geaendert:** `api/API/Repositories/PostgresWorkflowRepository.ReadOperations.cs` — neue interne Struct `WorkflowTaskListNarrowingFilter(long UserId, int[] ResponsibilityIds)`. `LoadTasks` bekommt optionales `narrowing`-Parameter; wenn gesetzt, wird die WHERE-Clause um `AND w.status <> 'completed' AND EXISTS (SELECT 1 FROM task_assignments wta WHERE wta.workflow_task_id = t.id AND wta.is_primary = TRUE AND ((wta.assignment_type = 'user' AND wta.assignee_user_id = @narrowUserId) OR (wta.assignment_type = 'responsibility' AND wta.assignee_responsibility_id = ANY(@narrowResponsibilityIds))))` erweitert. Parameter-Binding fuer narrowUserId (bigint) + narrowResponsibilityIds (int[]) nur wenn narrowing aktiv. **Geaendert:** `api/API/Repositories/IWorkflowRepository.cs` — neue Methode `GetTasksForUserNarrowed(long userId, int[] responsibilityIds)` mit Doc-Kommentar zur Voraussetzung (no full-task-access). **Geaendert:** `api/API/Repositories/PostgresWorkflowRepository.TaskMetadataOperations.cs` — Implementierung von `GetTasksForUserNarrowed`: instanziiert `WorkflowTaskListNarrowingFilter` und reicht es an `LoadTasks` durch; Rotation-Tasks unveraendert via `_rotationRepository.GetRotationTaskEnvelopesForUser` (die hatten den SQL-Filter schon). **Geaendert:** `api/API/Services/TaskApplicationService.cs` — `GetTasksAsync` Routing erweitert: Admin → `GetTasks()` (unchanged), Non-Admin **mit** `TasksAssignOverride`-Permission → `GetTasksForUser` (unchanged, Override-Nutzer brauchen volle Sicht), Non-Admin **ohne** Override → `GetTasksForUserNarrowed` (NEW). C#-`.Where(canUpdate || canDecide)` bleibt drin als Safety-Net, weil phase-aware Logik (`CanRegularlyEditWorkflow`) nicht in SQL pushbar ist — narrowing ist eine Obermenge. **Test-Stub-Updates:** `AdminWorkflowDefinitionConfigEndpointsTests.StubWorkflowDefinitionRepository` und `WorkflowEndpointsTests.StubWorkflowRepository` bekommen `GetTasksForUserNarrowed`-Member (throw NotSupportedException, gleiches Pattern wie GetTasksForUser). **Neu:** `api/API.Tests/PostgresWorkflowRepositoryTaskNarrowingIntegrationTests.cs` (~290 Zeilen, im Collection `PostgresWorkflowRepositoryIntegration`). 2 Tests: (1) `GetTasksForUserNarrowed_ReturnsOnlyMatchingPrimaryAssignmentsOnNonTerminalWorkflows` — Fixture mit isoliertem Department, 2 Workflows (active waiting_for_department + completed), 4 Tasks (matching-responsibility-active, foreign-responsibility-active, user-assigned-active, matching-responsibility-terminal), 2 Responsibilities, 1 User. Sanity-Check: `GetTasksForUser` enthaelt alle 4 Task-IDs. Assertion: `GetTasksForUserNarrowed` enthaelt matching-resp + user-assigned, **nicht** terminal-workflow-task, **nicht** foreign-resp-task. (2) `GetTasksForUserNarrowed_EmptyResponsibilityIds_StillMatchesUserAssignments` — gleiche Fixture, ruft mit Empty Array → nur User-Assignment-Pfad triggert. **Build:** `dotnet build` clean. **Tests:** `dotnet test` ohne Filter: **384 passed, 1 skipped, 0 failed** (vorher 382 + 1 skipped — neue 2 Narrowing-Tests dazu, keine Regressionen). | HQ2 erledigt — DB-Roundtrip fuer regulaere Non-Override-Nutzer schrumpft von „alle Workflow-Tasks" auf „eigene primaer-zugewiesene auf nicht-terminalen Workflows". Bei 10.000+ Tasks mit z.B. 50 zugewiesenen pro Nutzer: ~200x weniger Zeilen aus der DB. Korrektheit ist garantiert weil Narrowing eine **strikte Obermenge** dessen ist, was der C#-Filter ohnehin durchlaesst (beide Predicates `CanUpdateTaskStatus` + `CanDecideTaskApproval` verlangen `!IsTerminal` UND `MatchesTaskAssignment`; die SQL-EXISTS spiegelt `MatchesPrimaryAssignment` 1:1, und `IsTerminal` returnt true nur fuer "completed"). | (1) Phase-aware Filter (`CanRegularlyEditWorkflow`: nur Worker fuer `WaitingForDepartment` etc.) bleibt in-memory — die Logik braucht Permission-Tabelle und Role-Hierarchie und waere im SQL ein massiver JOIN. Akzeptabel, weil das ein Sub-Filter ueber die schon stark reduzierte Liste ist. (2) Override-Pfad (TasksAssignOverride-Permission ohne Admin-Rolle) lädt weiterhin ALLE Workflow-Tasks — das ist by design, weil der Nutzer jede Task bearbeiten kann. Falls in der Praxis viele Override-Nutzer auftauchen, koennte die Permission-Bedeutung nochmal hinterfragt werden. (3) Keine reale Performance-Messung gemacht — nur logische Garantien. Wer einen Vorher-Nachher-Benchmark mit 10k+ Tasks haben will, kann das mit der Testcontainers-Fixture als separater Folge-Task ausfuehren. (4) `task_assignments`-Tabelle hat aktuell keinen Index auf `(workflow_task_id, is_primary)` — bei sehr grossen Datensaetzen koennte das EXISTS-Subquery profitieren, aber Postgres greift normalerweise auf den Primary-Key oder einen FK-Index zurueck. Falls Performance-Probleme: `CREATE INDEX ... ON task_assignments (workflow_task_id) WHERE is_primary = TRUE` als Folgearbeit. |
| 2026-05-02 | Z2-HQ3 | done | **Neu:** `db/_archive/70_person_match_audit_log.sql` (Migration, idempotent via `CREATE TABLE IF NOT EXISTS`) — Tabelle `person_match_audit_log` (id bigint identity PK, matched_person_id, app_user_id, directory_identity_id, employee_number, match_strategy varchar(40), match_score numeric(5,2), fallback_used bool, source varchar(40), detail jsonb, created_at). 3 Indizes: `matched_person_idx` (partial WHERE NOT NULL), `created_idx` (DESC), `strategy_idx` (strategy + created_at DESC). **Geaendert:** `db/01_schema.sql` — Tabellendefinition + Sequence + Indizes + PK-Constraint vor `automation_job_attempts` eingefuegt (gleicher pg_dump-Stil wie umliegende Tabellen). **Geaendert:** `api/API/Services/EntraDirectorySyncService.cs` — beide Personen-Match-SQLs mit Audit-CTE umgebaut: (a) `linkPeopleByEmployeeNumberSql` (~Zeile 1447, Bulk-Pfad waehrend Sync): UPDATE in CTE `matched` mit `RETURNING p.id, linkable_identities.app_user_id/directory_identity_id/employee_number`, dann finaler `INSERT INTO person_match_audit_log SELECT ... FROM matched` mit strategy `employee_number`, score 1.00, fallback false, source `directory_sync_bulk`. (b) `EnsureDirectoryManagedPersonRecordAsync.matchExistingSql` (~Zeile 1921, Per-User-Match): WITH `latest_identity` + `matched` (UPDATE bestehender Person via employee_number) + `audit` (INSERT mit strategy `employee_number`, score 1.00, fallback false, source `directory_sync_per_user`); Final `SELECT id FROM matched LIMIT 1` (Postgres garantiert dass data-modifying CTEs auch ohne Referenz ausgefuehrt werden). (c) `EnsureDirectoryManagedPersonRecordAsync.sql` (~Zeile 1969, Insert-Fallback): WITH `inserted` (INSERT/UPSERT in people mit RETURNING incl. `(xmax = 0) AS was_inserted`-Trick) + `latest_identity_for_audit` + `audit` (INSERT mit strategy `created_new` oder `app_user_upsert` je nach was_inserted, score NULL, fallback true). **Build:** `dotnet build` clean, 0 Warnings, 0 Errors. **Tests:** Backend `dotnet test` aus `api/API.Tests/` ohne Filter: **382 passed, 1 skipped, 0 failed** (Total 383) — Postgres-Integration-Suite via Testcontainers (HQ4) durchgelaufen, Sync-Pfad wird indirekt mitgetestet, Audit-CTE bricht das Schema nicht. | HQ3 erledigt — Directory-Sync-Personen-Matching ist jetzt vollstaendig auditierbar. Bei jedem Match-Treffer (Bulk + Per-User) und jedem Fallback-Insert entsteht ein Audit-Eintrag. Tabelle ist abfragbar fuer Forensik („wer wurde wann gegen welche directory_identity gematcht und mit welcher Strategie?"). Atomar in derselben Transaktion wie der Match selbst. | (1) Kein dedizierter Audit-Test geschrieben — die SQL-CTEs sind bewusst inline mit dem Match-SQL, ein direkter Test wuerde die Sync-Methode mit echtem Postgres aufrufen muessen (waere wertvoll, aber HQ3-Scope war 0.5d). (2) `LoadTargetPerson`-Fuzzy-Name-Match in `PostgresRepositorySharedHelpers` (Zeile 596-606) wurde NICHT mit-instrumentiert — bewusst out-of-scope dokumentiert in CODE_REVIEW.md (READ-Pfad fuer aktuellsten Workflow, kein Person-zu-Person-Matching, self-correcting wenn workflow.target_person_id sauber gesetzt). (3) Audit-Log hat keine Retention-Policy — bei produktivem Sync taeglich kann die Tabelle wachsen. Falls relevant, koennte man eine TTL-Cleanup-Routine ergaenzen (z.B. „Eintraege aelter als 90 Tage loeschen"). |
| 2026-05-02 | Z2-HQ5 | done | **Neu:** `web/src/hooks/useRotationOperationsView.ts` (~307 Zeilen) — kapselt 8 Filter-States (`search`/`departmentFilter`/`statusFilter`/`changeWindowDays`/`onlyOpen`/`onlyItTasks`/`onlyOwnDepartment`) + 6 useMemo-Berechnungen (`rotationRows`/`departmentOptions`/`inferredOwnDepartmentId`/`filteredRows`/`upcomingChanges`/`departmentSummaries`/`personSummaries`) + Helper `isOperationallyOpen`/`isUpcomingAnchorDate`. Page erhält fertige Datenstrukturen plus `filters`/`setters`-Buendel. Typen `RotationTaskRow`/`UpcomingChangeSummary`/`DepartmentSummary`/`PersonSummary`/`RotationOperationsFilters` exportiert. **Neu:** `web/src/hooks/useRotationStationForm.ts` (~190 Zeilen) — kapselt 5 useState-Slots (`editingStationId`/`stationForm`/`isSavingStation`/`isRegenerating`/`deletingStationId`) + alle Handler (`openCreate/EditStationForm`, `resetStationForm`, `handleSaveStation/DeleteStation/RegenerateTasks`) + `reloadPlanData`-Invalidierung. Pure Helpers `createEmptyStationForm`/`buildStationForm`/`normalizeStationPayload` + Type `StationFormState` exportiert. Args-Interface `UseRotationStationFormArgs` (`numericPlanId`, `personId`, `orderedStationsCount`). **Neu:** `web/src/components/rotation/RotationStationFormCard.tsx` (~138 Zeilen) — Form-JSX als reine Praesentations-Komponente; nimmt `stationForm`/`setStationForm`/`departments`/`isDepartmentsLoading`/`isSavingStation`/`editingStationId`/`onSave`/`onReset` als Props. **Neu:** `web/src/components/rotation/rotationLabels.ts` (~58 Zeilen) — 4 Label-Helper (`getPlanStatusLabel`, `getPlanStatusPillClass`, `getStationStatusLabel`, `getGeneratedTaskStatusLabel`) extrahiert. **Geaendert:** `web/src/pages/RotationOperationsPage.tsx` — 564 → **367** Zeilen (-35%). Komplette State/Memo-Logik entfernt; Page ist jetzt thin Orchestrator (Hook + JSX-Sektionen). **Geaendert:** `web/src/pages/RotationPlanDetailPage.tsx` — 617 → **317** Zeilen (-49%). Helpers/State/Handler raus, Form-JSX in Sub-Komponente. PageHeader-Action nutzt `stationForm.handleRegenerateTasks()`, Stations-Liste nutzt `stationForm.openEditStationForm/handleDeleteStation`. Verhalten unveraendert (alle Side-Effects, alle UI-Texte, alle Routen). **Tests:** `tsc --noEmit` clean, Frontend `npm test` **140/140 passed** (37 Test-Files, gleiche Anzahl wie vor HQ5 — keine neuen Tests, aber auch keine Regressionen). | HQ5 erledigt — beide Rotation-Pages sind drastisch kleiner und haben klare Trennung zwischen State (Hooks), Logik (Helpers in Hooks) und Praesentation (Page + Sub-Komponenten). `useRotationStationForm` und `useRotationOperationsView` sind jetzt direkt unit-test-bar. `rotationLabels` ist gemeinsam wiederverwendbar. | Keine neuen Tests fuer die Hooks/Komponenten geschrieben — die Refactor-Aequivalenz steht und faellt mit den bestehenden 140 Tests, die nicht direkt diese Hooks ansprechen. Folgearbeit (LOW): Tests fuer `useRotationStationForm.normalizeStationPayload`-Validierung und `useRotationOperationsView`-Filter-Predicate koennten ergaenzt werden, sind aber kein HQ5-Scope. `AdminConfigPage` (423 Zeilen) blieb unangetastet — bewusst out-of-scope, weil HQ5 explizit nur die zwei 500+-Pages adressiert. |
| 2026-05-02 | Z2-LQ6 | done | **Helpers exportiert (intern → public):** `parseCondition`/`serializeCondition`/`ParsedCondition` in `WorkflowBuilderConditionEditor.tsx`; `parseMapping`/`stringifyMapping`/`readEntry`/`entryToJson` in `WorkflowBuilderActionMappingEditor.tsx`; `parseConfig`/`writeString` in `WorkflowBuilderStepConfigEditor.tsx`. Komponenten-Verhalten unveraendert — die Helpers waren bereits intern in derselben Form definiert. **Neu:** `web/tests/workflowBuilderEditorHelpers.test.ts` (~220 Zeilen, 29 Tests). Drei Suites: (1) Condition-Editor — leerer Default, is_true-Parse, eq-Text/eq-Boolean-Parse, unknown-Operator-invalid, non-object-invalid, malformed-JSON-invalid, Serialize-Empty/is_true/eq-Text/eq-Boolean-bevorzugt-Bool, JSON-Roundtrip. (2) Mapping-Editor — Empty-Default, invalid-JSON-null, Empty-Object-leerer-String, readEntry fuer alle 5 Quellen (workflow/answer/person→target_person/static-Scalar/missing-key), entryToJson-Roundtrip, full Roundtrip mit zwei Parametern. (3) Step-Config-Editor — parseConfig-Default/invalid, writeString-set-on-empty/preserve-existing/delete-when-empty/empty-when-last-removed. **Geaendert:** `web/tests/adminWorkflowBuilderModel.test.ts` — 4 neue Edge-Case-Tests fuer `topologicallyOrderNodes`: Cycle-Handling (zwei Knoten in Zyklus → beide an Array-Reihenfolge angehaengt), Self-Loop-Ignoring, Unknown-Source/Target-Refs werden ignoriert, Blank-nodeKey-Handling. **Resultate:** Frontend `npm test`: **140 passed, 0 failed** (37 Test-Files). Vorher 108 Tests — **+32 neue Tests** (29 Helper + 4 Topological-Edge-Cases, 1 vorhandener Roundtrip-Test wurde nicht doppelt gezaehlt). | LQ6 erledigt — die L7-Form-Editor-Investition ist jetzt durch direkt testbare Helpers abgesichert. JSON-Roundtrip fuer alle drei Editoren bewiesen. Topological-Sort robust gegen 4 typische Edge-Cases. | Component-Tests (mit @testing-library/react) wurden bewusst nicht ergaenzt — die fragile Render-Tests waeren disproportionaler Aufwand zu der Helper-Coverage. Wenn der UI-Kontrakt wichtig wird (z.B. fuer Storybook-Integration), kann das nachgeschoben werden. |
| 2026-05-02 | Z2-LQ5 | done | **Neu:** `web/src/components/feedback/AppErrorBoundary.tsx` — class-Component (React 19 hat noch keinen Hook fuer ErrorBoundary). `getDerivedStateFromError` setzt error-State, `componentDidCatch` ruft `reportUserVisibleError` mit category=ui/eventKey=ui_render_error/source=AppErrorBoundary + scope+componentStack ins detailJson. `componentDidUpdate` resettet bei Aenderung von `resetKey`. Fallback-UI ist ein `panel panel-error` mit Fehlermessage + zwei Aktionen: „Erneut versuchen" (lokaler Reset) und „Seite neu laden" (window.location.reload). **Geaendert:** `App.tsx` — Wrapper-Funktion `RoutesWithErrorBoundary({ defaultRoute })` extrahiert; nutzt `useLocation()` fuer pathname als `scope` und `resetKey`. Bei Routenwechsel wird die Boundary automatisch resettet — ein Render-Fehler killt nur die aktuelle Route, nicht das Layout. **TypeScript-Check + Frontend-Tests:** sauber (108/108 passed). | LQ5 erledigt — Render-Fehler werden jetzt sichtbar abgefangen statt die ganze App zum White-Screen zu fuehren. Auto-Reporting an System-Log (gleicher Pfad wie `reportUserVisibleError`). | Pro-Sektion-Boundaries (z.B. eine zusaetzliche Boundary innerhalb einer Page um nur eine Sub-Komponente zu isolieren) wurden nicht eingefuehrt — bei Bedarf kann `<AppErrorBoundary scope="...">` aber inline im Komponenten-Tree wiederverwendet werden. Fallback-UI hat keine Production-Mode-Variante (zeigt also auch in Prod den `error.message` — fuer interne Plattform akzeptabel). |
| 2026-05-02 | Z2-HQ4 | done | **`api/API.Tests/API.Tests.csproj`:** PackageReference `Testcontainers.PostgreSql 4.0.0` ergaenzt. **`PostgresWorkflowRepositoryDatabaseFixture.cs`:** Fixture-Logik erweitert. Wenn `ONBOARDING_TEST_CONNECTION_STRING` nicht gesetzt ist, wird per `PostgreSqlBuilder` ein `postgres:16-alpine`-Container hochgefahren (DB `appdb`, User `app`, Pass `app_pw`), `GetConnectionString()` als Env-Var gesetzt. Der existierende Schema-Init-Code (`db/01_schema.sql` + `db/02_dev_seed.sql` + Supplemental-Seed) bleibt unveraendert und nutzt die neue Connection. `DisposeAsync` stoppt den Container und cleared die Env-Var. **Fallback-Pfad:** Externe Connection-String-Konfiguration (z.B. fuer CI mit shared Postgres) bleibt unveraendert nutzbar — Container wird nur gestartet wenn keine Env-Var. **Test-Resultate:** Vorher 324 Unit-Tests + alle Integration-Tests mit `Failed to connect to 127.0.0.1:26432` gefailt. Jetzt **382 passed, 1 skipped, 0 failed** (Total 383) inkl. PostgresWorkflowRepository* + Concurrency + Audit + Automation + Link + WorkflowDefinition Integration-Suites. Container-Start ~3-4s, Tests selbst ~11s. **Voraussetzung:** Docker muss laufen (`docker info` muss erreichbar sein). Wenn nicht: Tests scheitern mit aussagekraeftigem Testcontainers-Fehler statt mit Npgsql-Connection-Failure. | HQ4 erledigt — Integration-Tests laufen jetzt out-of-the-box auf jedem Dev-Rechner mit Docker, ohne manuelles Postgres-Setup. **Voraussetzung HQ2 (In-Memory-Task-Filter nach SQL ziehen) erfuellt** — wir koennen Repository-Aenderungen jetzt mit echten Tests absichern. CI-Integration (GitHub Actions Docker-in-Docker etc.) als spaetere Folgearbeit. | Tests benoetigen Docker — wenn auf der CI-Runner-Maschine kein Docker verfuegbar ist, muss `ONBOARDING_TEST_CONNECTION_STRING` mit einem extern gehostete Postgres gesetzt werden (Pfad ist erhalten geblieben). Container-Image `postgres:16-alpine` ist hardcoded; bei Major-Version-Wechsel des Production-Postgres muss das nachgezogen werden. |
| 2026-05-02 | Z2-HQ1 | done | **Befund:** `WorkflowDefinitionValidationService.ValidateDecisionConditions` existiert seit Initial-Rework-Commit (`30b61ad`) und blockt Save mit invalidem JSON / fehlendem answerKey/operator / unsupported operator. **Lücke:** `eq`/`neq` ohne `expectedValue*` wurde bisher NICHT geblockt — `TaskConditionEvaluator.EvaluateEquality` returnt dann `false`, also verhaelt sich der Pfad silent unerwartet (`eq` nimmt nie, `neq` immer den Pfad). **Fix:** Beide Overloads von `ValidateDecisionConditions` um `HasAnyExpectedValue(root)`-Check erweitert; neuer Helper prueft Anwesenheit von `expectedValueText` (non-empty string), `expectedValueBoolean` (bool) oder `expectedValueNumber` (number). Error/Issue-Code: `missing_decision_expected_value`. **Migration:** `db/_archive/69_normalize_decision_conditions.sql` als idempotente, audit-loggende Best-Effort-Konvertierung von 3 historischen Freitext-Patterns (`key == value`, `key != value`, `key`) zu Backend-JSON-Format. RAISE NOTICE pro Pattern + RAISE WARNING fuer nicht konvertierbare Reste mit edge.id-Liste. Idempotent: bereits gueltige JSON-Eintraege werden nie angefasst. **Tests:** 2 neue InlineData-Cases `missing_decision_expected_value_eq` und `missing_decision_expected_value_neq` in `WorkflowDefinitionValidationServiceTests.ValidateAndNormalize_RejectsInvalidDrafts`. ValidationService-Suite: 40/40 passed. Backend-Unit-Tests gesamt: 326/326 passed (+2 gegenueber R5). | HQ1 erledigt — kein latenter Runtime-Bug mehr fuer `eq`/`neq`-Edges ohne Wert (Save blockt). Migration steht bereit fuer DBA, falls Audit-Log alte Freitext-Strings findet (in einer frischen DB unwahrscheinlich, aber defensiv abgesichert). | Migrations-Skript wurde NICHT auf einer realen DB ausgefuehrt — die Patterns sind anhand der Skizzen-Beispielen abgeleitet; falls in der Praxis andere Freitext-Formen aufgetaucht sind, fallen sie in den `RAISE WARNING`-Block und muessen manuell konvertiert werden. Backend-Integration-Tests konnten weiterhin nicht laufen (Test-DB-Setup fehlt — siehe HQ4). |
| 2026-05-02 | L7-Restposten-R9 | done | **Bug-Fix + Feature.** Backend (`PostgresWorkflowRuntimeRepository.ParseDecisionCondition`) erwartet `conditionExpression` als JSON-Objekt mit `answerKey`/`operator`/`expectedValue*` — der bisherige Freitext-Modus aus Block D haette beim ersten Decision-Pfad mit `Decision condition is not valid JSON` gefailt. Skizzen-Annahme „Freitext wie heute" war falsch. **Neu:** `WorkflowBuilderConditionEditor.tsx` (~210 Zeilen). Form-Builder mit 3 Dropdowns: Antwort (aus `answerDefinitions`, sortiert), Operator (`is_true`/`is_false`/`is_not_null`/`is_null`/`eq`/`neq` mit DE-Labels), Wert (kontextabhaengig: Boolean-Dropdown bei `inputType=boolean`, Text-Input sonst, ausgeblendet bei is_*-Operatoren). Ungueltige Operatoren fuer den Antwort-Typ werden disabled angezeigt. Validierungs-Hints: Antwort-Key existiert nicht; Operator passt nicht zum Typ. **Roundtrip JSON↔Form:** `parseCondition`/`serializeCondition` Helpers. Empty Form → empty string (kein leeres JSON-Objekt). **JSON-Power-Modus:** Toggle „JSON-Editor (Power)" zeigt raw textarea fuer Migration alter Daten oder edge cases; bei ungueltigem JSON wird er forciert. **Geaendert:** `AdminWorkflowBuilderFormSection.tsx` — Edge-Tabelle nutzt fuer Decision-Source-Edges jetzt den neuen Editor statt eines Text-Inputs; Nicht-Decision-Quellen zeigen einfach „—". **CSS:** `.wf-condition-editor*`, `.wf-condition-fields` (Grid 3 Spalten, mobile 1 Spalte), `.wf-condition-mode-toggle`. **Esbuild-Bug:** Anfangs hatte ich `„Ja"` (deutsches öffnendes Quote + ASCII closing) in einem JS-String — Esbuild parste das als unterminierten String. Behoben durch Entfernung der deutschen Quotes in den OPERATOR_LABELS-Strings (in JSX-Text klappen sie weiter). **Tests:** Frontend 108/108 passed. Build clean, Bundle 72 kB (gzip 18.27 kB, +5 kB gegenueber R4). | R9 erledigt — Decision-Edges sind jetzt produktiv nutzbar (vorher latenter Bug). User klickt Antwort + Operator + Wert; das Backend bekommt das erwartete JSON. Power-Modus existiert fuer alte Daten oder seltene Edge-Cases. | Wenn ein Workflow ALTE conditionExpression-Strings im Format `hardwareTakeover == true` enthaelt, zeigt der Form-Editor automatisch den JSON-Power-Modus mit Fehlermeldung — der Nutzer muss dann den Eintrag manuell konvertieren. Das ist konsistent mit dem Backend-Verhalten (`Decision condition is not valid JSON`). |
| 2026-05-02 | L7-Restposten-R4 | done | **`DependencyGraphEditor.tsx` komplett neu** (~210 Zeilen, vorher 330) als Tabellen-Editor: Toolbar mit Stats + „+ Abhaengigkeit hinzufuegen"-Toggle, aufklappbare Inline-Form (Quelle/Ziel/RequiredStatus-Dropdowns + Anlegen-Button), Tabelle Quelle/→/Ziel/Status/✕. Quelle/Ziel-Zellen sind Buttons (`onSelectTemplate`-Callback erhalten). Status-Badges farbcodiert pro `requiredStatus`. Confirm-Dialog beim Loeschen via existierendem `useConfirmationDialog` (unveraendert). **Geloescht:** `DependencyGraphNode.tsx`, `DependencyGraphEdge.tsx`. **package.json:** `@xyflow/react` und `@dagrejs/dagre` entfernt (`npm uninstall`); package-lock.json aktualisiert (lokal `grep -c xyflow\|dagre` = 0 in beiden). **CSS:** Neuer Block `.dep-editor-*` (~110 Zeilen) im workflow.css fuer Toolbar/Create-Form/Tabelle/Status-Badges (5 Status-Farben). **Test-Cleanup:** `tests/AdminWorkflowBuilderSection.test.tsx` geloescht (testete entfernten Canvas-Editor). `tests/adminWorkflowBuilderModel.test.ts`: 3 Position-Tests (`autoLayoutVersionDraft`, `forceAutoLayoutNodePositions`) entfernt + 2 neue Tests fuer `topologicallyOrderNodes` (linearer + paralleler Pfad mit Tie-Breaker). `tests/WorkflowBuilderPage.test.tsx` umgeschrieben fuer Form-Editor (Sektions-Headings + Admin-only Buttons). **Test-Resultate:** Frontend `npm test`: **108/108 passed**, vorher 5 failed wegen entfernter Canvas-Texte. Backend Unit-Tests unveraendert 324/324. **Bundle-Effekt:** AdminConfigPage 449 → **228 kB** (gzip 124 → 50 kB) — fast 50% weniger; Hauptbundle minimal kleiner. | R4 erledigt — Phase-3-Surface („React Flow Dependency aus package.json entfernen") jetzt vollstaendig eingeloest. Massive Bundle-Einsparung. DependencyGraph als Tabelle bleibt funktional aequivalent: gleiche Aktionen (Anlegen, Loeschen, Template auswaehlen), aber ohne Drag-and-Drop und ohne dagre-Layout. | UX-Aenderung: Anwender, die das visuelle Diagramm gewohnt waren, sehen jetzt eine sortierte Tabelle. Funktionale Aequivalenz ist gewahrt (alle Aktionen verfuegbar), aber die graphische Uebersicht „wer haengt von wem ab" ist weniger sofort erfassbar bei groesseren Graphen. Falls negative Resonanz: man koennte einen kollabierbaren Reverse-Index (pro Template eine Liste eingehender/ausgehender Abhaengigkeiten) nachreichen. |
| 2026-05-02 | L7-Restposten-R2 | done | **`adminWorkflowBuilderModel.ts`:** Neue pure Function `topologicallyOrderNodes(nodes, edges)` (Kahn's Algorithm). Tie-Breaker: ursprueglicher Array-Index (= manuelle Reihenfolge ueber `moveNode`). Knoten ohne nodeKey oder Knoten in Zyklen werden in Array-Reihenfolge ans Ende angehaengt. Edges mit ungueltigen Source/Target-Refs werden ignoriert. Self-Loops werden uebersprungen. **`AdminWorkflowBuilderFormSection.tsx`:** `Section2Steps` ruft `useMemo(() => topologicallyOrderNodes(arrayNodes, edges), [arrayNodes, edges])` und rendert die topologisch sortierte Liste. ↑/↓-Buttons bewegen weiterhin im Array (via `moveNode`), wirken nur als Tie-Breaker bei mehrdeutigen Pfaden — Hinweis im Sektions-Subtitle aufgenommen. Build clean, Bundle 67 kB (gzip 17.29 kB, +1 kB gegenueber R3). | R2 erledigt — Karten erscheinen jetzt in der Reihenfolge in der sie tatsaechlich abgearbeitet werden, abgeleitet aus den Uebergaengen. Manuelle Reorder-Buttons bleiben als Notbremse bei Parallel-Pfaden. Pure Function ist unit-test-bar (kein Test in dieser Aenderung). | Bei eindeutiger Topologie hat ↑/↓ keinen sichtbaren Effekt — koennte einen Nutzer verwirren („warum tut der Knopf nichts?"). Mitigation: Sektions-Hinweis erklaert die Tie-Breaker-Rolle. Falls relevant, koennte man die Buttons in eindeutigen Topologien disablen — bewusst nicht gemacht, weil das den Tie-Break-Workflow erschwert wenn der Nutzer kuenftig parallele Pfade plant. |
| 2026-05-02 | L7-Restposten-R3 | done | **`useAdminWorkflowBuilder.ts`:** `localValidationIssues`-State von `string[]` zu `WorkflowBuilderLocalIssue[]` umgestellt. `collectValidationMessages` → `collectValidationIssues`, gibt strukturierte Issues mit `scope`/`message`/`referenceKey` zurueck. Neuer `dedupeIssues`-Helper (deduped per scope+referenceKey+message). Aufrufer in `saveVersion`/`validateDraft` angepasst. Re-export `WorkflowBuilderLocalIssue` aus dem Modell. **`AdminWorkflowBuilderFormSection.tsx`:** Neue Helpers `stepAnchorId(node)` (`wf-step-{node.id}`), `edgeAnchorId(edge)` (`wf-edge-{edge.id}`), `jumpToAnchor(id)` (ScrollIntoView smooth + temporaere `wf-jump-highlight`-Class fuer 1.6s), `resolveLocalIssueAnchor` und `resolveBackendIssueAnchor` (mappen scope+referenceKey auf Karten/Edges). Karten-Liste wickelt jede Karte in `<div id={stepAnchorId}>`, Edge-Tabelle setzt `<tr id={edgeAnchorId}>`. `Section4Validation` rendert Issue-Message als `<button class="wf-validation-link">` wenn ein Anker aufgeloest werden konnte, sonst als `<span>`. Auch lokale Issues haben jetzt scope/referenceKey-Meta-Zeile (vorher nur Backend). **CSS:** `.wf-validation-item-body`, `.wf-validation-link`, `.wf-validation-item--clickable:hover`, `@keyframes wfJumpHighlight` mit Box-Shadow-Pulse + Background-Fade ueber 1.5s. Build clean, Bundle 66 kB (gzip 16.91 kB, +4 kB gegenueber R6). | Validation-Liste ist jetzt navigierbar — Klick auf einen Issue scrollt direkt zur Karte oder Edge-Zeile mit kurzer Hervorhebungs-Animation. Lokale + Backend-Issues bekommen die gleiche Behandlung; Issues ohne aufloesbaren Anker (z.B. scope=version/definition) bleiben als reiner Text. | Anker-Aufloesung scheitert wenn `referenceKey` einen Schritt-Key meint, der zwischenzeitlich umbenannt wurde — Issue zeigt dann nur Text ohne Sprungziel. Akzeptierbar, weil dann ist die Aussage des Issues sowieso veraltet bis zur naechsten Validierung. |
| 2026-05-02 | L7-Restposten-R6 | done | **Geaendert:** `web/src/components/admin-config/AdminWorkflowBuilderFormSection.tsx` — `WorkflowSelectorBar` bekommt `canManageAdvanced`-Prop. Im Admin-Modus zeigt sie rechts zwei neue Buttons: „+ Neuer Workflow" (Toggle) und „Workflow loeschen" (Confirm via `window.confirm`, disabled wenn `hasUnsavedChanges` oder keine Definition gewaehlt). Beim Aufklappen erscheint Inline-Form `CreateDefinitionForm` mit Name (autoFocus, Pflicht) + Beschreibung + Anlegen/Abbrechen-Buttons. Nutzt die bereits im Hook vorhandenen `createDefinition`/`deleteDefinition`/`newDefinitionDraft`/`updateNewDefinitionDraft`/`isCreatingDefinition`/`isDeletingDefinition` (waren als toter Code verdaechtig — sind jetzt aktiv). **CSS:** `.wf-form-selector-actions`, `.wf-form-create-definition` (gestrichelter Brand-Border), `.wf-form-create-definition-actions`. Build clean. | R6 erledigt — Form-Editor ist jetzt selbsttragend produktiv nutzbar: Workflow erschaffen, editieren, loeschen alles aus einer UI. Kein Wechsel mehr in andere Tabs noetig. Hook-API jetzt komplett genutzt (kein versteckter Tot-Code mehr). | Loeschen ist nur via `window.confirm` abgesichert (keine Soft-Delete-Markierung). Ist konsistent mit der frueheren Canvas-Loesch-UX und der Backend-API. |
| 2026-05-02 | L7-Restposten-R1 | done | **Neu:** `web/src/components/admin-config/WorkflowBuilderStepConfigEditor.tsx` (~135 Zeilen). Typ-spezifische Inline-Felder fuer form/approval/task: Zustaendig-Dropdown (sortiert nach `responsibilityName`), Vorlage-Dropdown (Approval-Filter `!isDepartmentPhaseTask`, sonst alle aktiven), Benachrichtigte-Input, Kurzbeschreibung-Textarea. Form-Schritt zeigt zusaetzlich Hinweis auf gefundene Antwort-Definitionen. Warnung wenn `legacyTemplateKey` auf nicht-existierende Vorlage zeigt. Helpers `parseConfig`/`readString`/`writeString` schreiben/lesen `responsibilityKey`/`legacyTemplateKey`/`notificationLabel`/`summaryText` aus `node.configText` — gleiches Pattern wie der frueher geloeschte `BuilderInspectorPanel`. Edit-Roundtrip JSON↔Form. **Geaendert:** `WorkflowBuilderStepCard.tsx` — `StepCardBody` bekommt `onUpdate` aus den Props, ersetzt fuer `case "form"|"approval"|"task"` die alten Hint-Texte durch `<WorkflowBuilderStepConfigEditor ...>`. **CSS:** `.wf-step-config` (3 Zeilen, einfaches `flex column gap`). Build clean, Bundle 62 kB (gzip 16 kB, +0.5 kB gegenueber R7-Stand). | R1 erledigt — Hauptlast der „kein-JSON-fummeln"-Promise der Skizze ist jetzt eingeloest. Form/Approval/Task haben echte Form-Felder, nicht mehr nur „Technische Details ► JSON"-Verweis. Pattern (parseConfig/readString/writeString) ist wiederverwendbar wenn weitere config-keys per UI gepflegt werden sollen. | Antwort-Definitionen-Verwaltung selbst (was im Form sammeln soll) bleibt in der Antwort-Definitions-Workspace — der Form-Step verlinkt nicht direkt dorthin (frueher gab's „Gezielt bearbeiten ►"-Buttons im InspectorFocusPanel; das Cross-Workspace-Springen wurde bewusst nicht uebernommen). |
| 2026-05-02 | L7-Restposten-R5+R7 | done | **R5 (Tests):** `dotnet test` aus `api/API.Tests/` mit Filter `--filter "FullyQualifiedName!~Integration&FullyQualifiedName!~Postgres"` → **Passed: 324, Failed: 0, Skipped: 0**. Integrationstests scheitern alle mit `Npgsql.NpgsqlException: Failed to connect to 127.0.0.1:26432` — die Test-DB ist in dieser Umgebung nicht hochgefahren (bekanntes Infrastruktur-Problem laut `KauthWorkflow/Stand/Code-Review-Status.md` „Bekanntes Dauerproblem: Test-Fixture"). Frontend hat einen `tests/`-Ordner aber kein `npm test`-Script in `package.json` — kein automatisierter Frontend-Test-Lauf moeglich. **R7 (CSS-Cleanup):** `web/src/styles/workflow.css` von 2471 → 1963 Zeilen (508 Zeilen weg, ~21%). Geloescht: alle `.builder-*`-Styles ausser `.builder-product-page` (von `WorkflowBuilderPage.tsx` als Page-Layout-Wrapper genutzt) und sein Media-Query-Override. Konkret raus: `.builder-product-section`, `.builder-studio-*`, `.builder-topbar-*`, `.builder-action-button*`, `.builder-validation-pill`, `.builder-rail-panel*`, `.builder-sidebar-*`, `.builder-canvas-*`, `.builder-empty-stage`, `.builder-inspector-hero*`, `.builder-validation-grid/card`, `.builder-readonly-grid`, `.builder-palette-*`, `.builder-mode-badge*`, `.builder-lock-card`, `.builder-admin-disclosure`, `.builder-selection-list`, `.builder-validation-strip`, plus zugehoerige Media-Query-Bloecke. `vite build` clean, Bundle-Groessen unveraendert (nur CSS-Dead-Code-Removal). | R5 + R7 aus Restposten-Liste erledigt. Backend Unit-Tests waren immer schon gruen (mein Hook-Cleanup hat nichts kaputtgemacht). CSS-Datei deutlich kuerzer und ehrlicher — keine Phantom-Klassen mehr. | **R5 unvollstaendig:** Integration-Tests (PostgresWorkflow*-Suite, RotationRepo, etc.) konnten nicht laufen — DB-Setup auf Port 26432 fehlt in der Umgebung. Wenn die Test-DB anderswo verfuegbar ist (CI?), muss dort separat verifiziert werden. Frontend-Test-Lauf nicht moeglich (kein Test-Setup). |
| 2026-05-02 | L7-Phase3-HookCleanup | done | **`useAdminWorkflowBuilder.ts`:** Tot-Code aus Phase 3 entfernt. Gelöscht: `selectedNodeId`/`selectedEdgeId`-State + alle Setter, `selectedNode`/`selectedEdge`-Memos, `selectNode`/`selectEdge`/`updateNodePosition`/`autoLayoutNodes`/`removeSelectedNode`/`removeSelectedEdge`/`connectNodes` (Callbacks), `inspectorFocus`-State + `openInspectorFocus`/`closeInspectorFocus`, `BuilderInspectorFocusMode`/`Section`/`Target`-Types, `createEmptyInspectorFocus`-Helper. `removeNode` von Selection-State-Cleanup befreit (war einzige nicht-triviale Verflechtung). `removeEdge` ebenso. `addNode` setzt nicht mehr `setSelectedNodeId`. `applyLoadedVersionDetail`/`resetLoadedVersion`/`saveVersion` putzen keine Selection mehr. **`adminWorkflowBuilderModel.ts`:** `autoLayoutVersionDraft` (No-Op) und `withFallbackNodePositions` (No-Op) entfernt; `toVersionDraft` ruft sie nicht mehr auf. Datei schrumpft auf 320 Zeilen. **Hook-File:** 1059 → 660 Zeilen (~38% kleiner). Build: `tsc --noEmit` clean, `vite build` clean. Form-Editor-Bundle 60 kB (gzip 15.64 kB) — ~2 kB kleiner als vor dem Cleanup. | Hook-API jetzt streng auf Form-Editor-Bedarf reduziert: 39 returnte Properties (vorher 47). Zustandsmodell deutlich einfacher — keine Selection mehr (alles inline in den Karten). | Risiko-Restwert minimal — Cleanup von echtem Tot-Code, alle Verbraucher (FormSection + Karten) compilieren weiterhin. |
| 2026-05-02 | L7-Phase3 | done | **Gelöscht:** 7 Canvas-Dateien (`AdminWorkflowBuilderSection.tsx`, `WorkflowBuilderCanvasNode.tsx`, `BuilderPalettePanel.tsx`, `BuilderInspectorPanel.tsx`, `BuilderInspectorFocusPanel.tsx`, `WorkflowBuilderSidebar.tsx`, `workflowBuilderStructuredLayout.ts`). **Geändert:** `web/src/pages/WorkflowBuilderPage.tsx` — URL-Query-Switch entfernt, Form-Editor ist jetzt Default und einziger Modus. `useSearchParams`-Import entfernt. `web/src/components/admin-config/AdminConfigWorkspaceSections.tsx` — `AdminWorkflowBuilderSection`-Import + Verwendung umgestellt auf `AdminWorkflowBuilderFormSection` (Builder-Tab in Admin-Konfig nutzt jetzt ebenfalls den Form-Editor). `web/src/hooks/adminWorkflowBuilderModel.ts` — `buildWorkflowBuilderStructuredLayout`-Import entfernt; `autoLayoutVersionDraft` zu No-Op (gibt draft unverändert zurück); `withFallbackNodePositions` zu No-Op; `forceAutoLayoutNodePositions` und `buildStructuredPositions` gelöscht. Folgewirkung: Save-Pfad setzt keine Positionen mehr — Backend akzeptiert via `toNullableInteger` ohnehin nullable Positionen. **Hook (`useAdminWorkflowBuilder.ts`) bewusst weitgehend unverändert** — die ungenutzten Funktionen (`selectNode`/`selectEdge`/`updateNodePosition`/`autoLayoutNodes`/`connectNodes`/`removeSelectedNode`/`removeSelectedEdge`/`inspectorFocus`/`openInspectorFocus`/`closeInspectorFocus` + Inspector-Focus-Types) bleiben als toter Code drin. Begründung: gezielter Cleanup hätte die Selection-State-Verflechtung in `removeNode` brechen können — für Phase 3 zu riskant, kann später als separater Aufräum-PR kommen. **React-Flow-Dep (`@xyflow/react`) bleibt im package.json** — wird vom `DependencyGraphEditor` (Task-Template-Abhängigkeitsgraph) noch produktiv genutzt. Die Skizze hatte das nicht antizipiert. **Build:** `tsc --noEmit` clean, `vite build` clean. Form-Editor-Bundle 62 kB (gzip 16 kB). | **Phase 2 + 3 von L7 (WorkflowBuilder Form-Editor) komplett.** Aktiver Zyklus geschlossen. Canvas-UX ist Geschichte; Form-Editor ist die einzige UI für Workflow-Builder. **Restposten:** Hook-Cleanup als Folgearbeit; React-Flow-Dep-Removal benötigt erst Migration des DependencyGraphEditors (separater Scope). | **Browser-Verifikation steht noch aus.** CLAUDE.md fordert für UI-Änderungen Dev-Server-Test — der Form-Editor ist syntaktisch und typmäßig sauber, aber nicht durchgeklickt. **Kein Rollback-Pfad mehr** — Canvas ist physisch gelöscht. Bei kritischen Bugs muss git revert helfen. **Hook-Tot-Code:** ~150 Zeilen ungenutzte Funktionen + 25 Zeilen Inspector-Focus-Types bleiben drin. |
| 2026-05-02 | L7-Phase2-BlockD | done | **Geändert:** `web/src/components/admin-config/AdminWorkflowBuilderFormSection.tsx` — `Section3EdgesPlaceholder` ersetzt durch echte `Section3Edges`-Tabelle: Spalten Von/→/Zu/Pfad-Reihenfolge/Bedingung/Aktion, beide End-Dropdowns aus aktueller Schritte-Liste (Title + nodeKey), Sortierung primär nach Source-Key dann nach Priority. Bedingungs-Spalte read-only mit Hinweis-Tooltip "Nur bei Entscheidung-Schritten relevant" wenn Quelle keine Decision; Decision-Set wird live aus den Nodes berechnet. „+ Übergang hinzufügen"-Button am Ende, ✕-Remove pro Zeile. Disabled-State wenn weniger als 2 Schritt-Keys. `Section4ValidationPlaceholder` ersetzt durch `Section4Validation`: kombiniert `localValidationIssues` (Frontend) + `versionDetail.validationIssues` (Backend), gruppiert mit Counts, Severity-Farb-Codes (`error`/`warning`/`info`), Backend-Issues zeigen scope/code/referenceKey als Meta. Erfolgs-Panel "Bereit zum Veröffentlichen" wenn 0 Issues. „Lokal prüfen"-Button ruft `builder.validateDraft()`. **CSS:** ~120 Zeilen `.wf-edge-table*`, `.wf-validation-*`. Build clean (tsc --noEmit 0 Fehler). | Sektion 3 + 4 funktional komplett. Form-Editor (`?editor=form`) deckt jetzt das volle Volume des Canvas-Editors ab — Stammdaten, Schritte (alle 12 Typen mit Inline-Editoren), Übergänge mit bedingter Bedingungs-Spalte, Validierung kombiniert. **Phase 2 (Blöcke A–D) ist komplett.** Nur Phase 3 (Canvas-Code löschen, React-Flow-Dependency raus) bleibt offen. | „Click-zu-Springen" auf betroffene Sektion/Karte aus der Issue-Liste ist nicht implementiert (nice-to-have, brauchte Routing-Anker oder ScrollIntoView pro Karte). Edge-Tabelle hat keine Drag-and-Drop-Reorder — Reihenfolge wird nur über `priority`-Number gesteuert. Form-Editor ist live unter `?editor=form` testbar; Browser-Verifikation fehlt (CLAUDE.md fordert Dev-Server für UI-Änderungen). |
| 2026-05-02 | L7-Phase2-BlockC | done | **(a) Maßnahmen-Vorschau:** Neu `web/src/components/admin-config/workflowBuilderMeasureAnalysis.ts` (~190 Zeilen, Helper extrahiert aus BuilderInspectorPanel: `buildMeasureNodeDetails`, `buildConditionGroupSummaries`, `buildDependencySummaries`, `formatConditionLabel`, `formatDependencyLabel`). Neu `WorkflowBuilderMeasurePreview.tsx` (~125 Zeilen): aufklappbarer „Geplante Maßnahmen (N, davon X Pflicht)"-Block für `measure_*`-Karten. Read-only Liste mit Pflicht-Badge, Bereich, Verantwortlich, kompakte Bedingungen (UND/ODER) + Abhängigkeiten. **(b) Action-Liste:** Neu `WorkflowBuilderActionEditor.tsx` (~150 Zeilen): nummerierte Liste sortiert nach `executionOrder`, Action-Key-Dropdown aus `actionDefinitions`, ↑/↓-Buttons für Reorder (tauscht executionOrder mit Nachbarn), ✕-Remove, „+ Aktion hinzufügen ▼"-Dropdown am Ende mit Filter inaktiv. **(c) Form-Builder Eingabe-Mapping:** Neu `WorkflowBuilderActionMappingEditor.tsx` (~290 Zeilen). Pro Parameter aus `inputSchema.properties` ein Block mit Quellen-Dropdown (5 Quellen: `static`/`workflow`/`target_person`/`directory_identity`/`answer`) + dynamische Felder je Quelle (Property-Dropdown mit hardcodierten Listen aus PostgresWorkflowAutomationOperations.cs, oder Antwort-Key-Dropdown). Roundtrip JSON↔Form: `parseMapping`/`stringifyMapping`/`readEntry`/`entryToJson`. Pflicht-Marker (*) aus `required`-Array. „custom"-Badge für Parameter, die nicht im Schema stehen. Fallback auf raw-JSON-Textarea wenn das Mapping ungültiges JSON ist. „+ Parameter hinzufügen" für freie Parameter. **Card-Integration:** `WorkflowBuilderStepCard.tsx` komplett überarbeitet — measure-Karten zeigen jetzt MeasurePreview, automation-Karten zeigen ActionEditor, Standard-Typen unverändert. Card-Props um Domain-Daten + 3 Action-Callbacks erweitert. **FormSection:** `STANDARD_STEP_TYPES` → `STEP_TYPE_OPTIONS` umbenannt + um `measure_provision/deprovision/change/rename` und `automation` ergänzt (jetzt 12 Typen). Karten bekommen alle Hook-Daten durchgereicht. **CSS:** ~210 Zeilen für `.wf-measure-preview*`, `.wf-action-editor*`, `.wf-action-item*`, `.wf-mapping-*`. Build clean (tsc --noEmit 0 Fehler). | Block C funktional komplett: Maßnahmen-Karten zeigen Read-only-Vorschau mit Pflicht/Bedingungen/Abhängigkeiten; Automation-Karten zeigen vollständige Action-Liste mit Form-Builder für Eingabe-Mapping (Option B aus Skizze). Mapping-JSON wird vom Form-Builder generiert, Roundtrip funktioniert für die 5 Backend-Quellen. Keine `BuilderInspectorPanel`/`BuilderInspectorFocusPanel`-Abhängigkeit mehr — die Maßnahmen-Helper sind in eigene Datei extrahiert, die in Phase 3 zusammen mit dem Canvas-Code überlebt. | **Property-Listen sind hardcodiert** (Workflow/Person/Directory) — bei Backend-Erweiterung müssen sie nachgezogen werden. Kein direkter Test gegen Backend; die Source-Strings (`workflow`, `target_person`, `directory_identity`, `answer`, `static`) entsprechen exakt dem Switch in `PostgresWorkflowAutomationOperations.ResolveAutomationReference`. **Form-Builder pausiert in Fallback-Modus**, wenn der bestehende `inputMappingText` ungültiges JSON enthält (statt zu crashen). Form/Approval/Task-Spezialfelder (Vorlage-Auswahl, Verantwortlich) bleiben in „Technische Details ► Konfiguration (JSON)" — könnten nachträglich auch ins Form-Layout gezogen werden, aber das war nicht Block C-Scope. |
| 2026-05-02 | L7-Phase2-BlockB | done | **Neu:** `web/src/components/admin-config/WorkflowBuilderStepCard.tsx` (~210 Zeilen). Karten-Komponente pro Schritt: Header (#N, Typ-Badge, Titel-Input, ↑/↓/✕-Buttons), Body mit typ-spezifischen Hinweisen (start/end ohne Body, decision/parallel-Hinweise, form/approval/task-Hinweise auf Block C), „Technische Details ►"-Aufklappblock mit `nodeKey` + raw `configText` (JSON). Karten für `automation` und `measure_*` markiert als read-only mit Block-C-Hinweis. **Geändert:** `web/src/hooks/useAdminWorkflowBuilder.ts`: neue `moveNode(nodeId, direction: "up"|"down")`-Action. Verschiebt Knoten im `versionDraft.nodes`-Array und re-numeriert `sortOrder` deterministisch (1..N). Im Return-Objekt exportiert. **Geändert:** `web/src/components/admin-config/AdminWorkflowBuilderFormSection.tsx`: `Section2StepsPlaceholder` ersetzt durch echte `Section2Steps`-Komponente. Rendert Karten in Array-Reihenfolge, „+ Schritt hinzufügen ▼"-Dropdown am Ende mit 7 Standard-Typen (form/approval/task/decision/parallel_split/parallel_join/end). Zusätzlicher „+ Start-Schritt anlegen"-Button wenn noch kein Start existiert. Confirm-Dialog beim Entfernen. Konstante `STANDARD_STEP_TYPES` enthält bewusst KEINE measure_*/automation-Typen (kommen in Block C). **Geändert:** `web/src/styles/workflow.css`: ~180 Zeilen CSS für `.wf-step-card`, `.wf-step-card-iconbtn`, `.wf-step-card-hint`, `.wf-step-add`, `.wf-step-add-menu`. Build clean (tsc --noEmit 0 Fehler). | Sektion 2 funktional: Karten anlegen/verschieben/löschen für 7 Standard-Typen, Titel inline editierbar, Schritt-Key + raw-JSON-Config in „Technische Details" aufklappbar. Form-Builder für typ-spezifische Felder (Vorlagen, Verantwortliche) bewusst deferred zu Block C — wird gemeinsam mit Form-Builder für Eingabe-Mapping (Option B aus Skizze) gebaut, weil Pattern wiederverwendbar. | StepCards für `measure_*` und `automation` zeigen nur "Wird in Block C voll editierbar"-Hinweis. Wenn ein Workflow mit solchen Knoten geladen wird, kann der Nutzer aktuell nur Titel/nodeKey/configText editieren. Wird in Block C aufgehoben. |
| 2026-05-02 | L7-Phase2-BlockA | done | **Neu:** `web/src/components/admin-config/AdminWorkflowBuilderFormSection.tsx` (~250 Zeilen, Form-Editor-Skeleton mit 4 Sektions-Slots + Sticky-Footer). Sektion 1 (Stammdaten) vollständig: Name/Beschreibung via `definitionDraft` + `updateDefinitionDraft`, Prozessbezug-Dropdown via `versionDraft.primaryLegacyProcessTypeKey` + `updateVersionDraftField`, „Technische Details ►"-Aufklappblock mit `definition_key` (read-only) + `primaryLegacyProcessTypeKey` (editierbar). Sektionen 2–4 als beschriftete Platzhalter. Sticky Footer: `Verwerfen` (bei `hasUnsavedChanges`), `Speichern (Entwurf)`, `Veröffentlichen`. **Geändert:** `web/src/pages/WorkflowBuilderPage.tsx`: `useSearchParams`-Switch `?editor=form` zwischen `AdminWorkflowBuilderFormSection` (neu) und `AdminWorkflowBuilderSection` (alt), Toggle-Button oben rechts. **Geändert:** `web/src/styles/workflow.css`: CSS-Block `.wf-form-*` hinzugefügt (~110 Zeilen). Build clean (tsc --noEmit 0 Fehler). | Form-Editor Bootstrap: Selector-Bar, Sektion 1 fertig, 3 Platzhalter, Footer. Routing-Switch läuft über `?editor=form` URL-Query — sicherer Migrations-Ansatz ohne Canvas zu entfernen. | Sektionen 2/3/4 sind noch Platzhalter. Form-Editor ist noch nicht funktional für Schritte/Übergänge. |
|-------|---------|--------|--------------------|-----------------|----------------|
| 2026-05-02 | CODE-REVIEW-L1-L3-L5-L6 | done | **L1 (Retry-Delays konfigurierbar):** Neu `api/API/Services/WorkflowAutomationRetrySettings.cs` (Singleton mit `MaxAttempts=3`, `FirstRetryDelay=1min`, `SubsequentRetryDelay=5min` Defaults). `WorkflowAutomationService` Constructor um `WorkflowAutomationRetrySettings` erweitert; Switch-Expression `attemptNumber switch { 1 => 1min, 2 => 5min, _ => 5min }` ersetzt durch `retrySettings.ResolveRetryDelay(attemptNumber)`. `MaxAttempts` ersetzt das hardcodierte `< 3`. `LifecycleServiceCollectionExtensions.BuildAutomationRetrySettings(IConfiguration)` liest 3 Env-Vars (`WORKFLOW_AUTOMATION_MAX_ATTEMPTS`, `WORKFLOW_AUTOMATION_FIRST_RETRY_DELAY_SECONDS`, `WORKFLOW_AUTOMATION_SUBSEQUENT_RETRY_DELAY_SECONDS`), Defaults erhalten Verhalten. 4 Test-Konstruktor-Aufrufe (3x WorkflowAutomationServiceTests, 1x PostgresWorkflowRepositoryAutomationIntegrationTests-Helper) um den 5. Parameter erweitert. **L3 (Rate-Limiting):** `services.AddRateLimiter` mit Policy `client-log-events`: partitionierter Fixed-Window-Limiter, 60 Requests/Minute pro `RemoteIpAddress`, Reject-Status 429. `app.UseRateLimiter()` zwischen `UseCors` und `UseAuthentication` eingehaengt. `ClientSystemLogEndpoints.cs` Endpoint-Definition um `.RequireRateLimiting("client-log-events")` und `.Produces(429)` erweitert. Imports: `System.Threading.RateLimiting`, `Microsoft.AspNetCore.RateLimiting`, `Microsoft.AspNetCore.Http`. **L5 (Permission-Audit-Reason):** Migration `db/_archive/68_permission_audit_reason.sql` (`ALTER TABLE auth_permission_audit_log ADD COLUMN IF NOT EXISTS reason text`). `db/01_schema.sql` `auth_permission_audit_log`-Tabelle ergaenzt um `reason text`. `AdminPermissionAuditEntryDto.Reason` (string?) hinzugefuegt. `LogPermissionAuditAsync` Signatur um `string? reason = null` erweitert (am Ende, optional → bestehende 5 Caller bleiben unveraendert). INSERT-SQL um `reason`-Spalte erweitert. `LoadAdminPermissionAudit` SELECT um `audit.reason` erweitert, Reader-Index 8 → 9 fuer `CreatedAt`, neuer `Reason`-Mapping. Catch-Klausel um `42703` (column missing) ergaenzt fuer Migration-Resilienz. **L6 (dev-sim Prod-Guard):** Kein Code-Change — Guard war bereits vorhanden (`LifecycleServiceCollectionExtensions:39-44` wirft `InvalidOperationException` bei `IsProduction && AuthMode != "entra"`). CODE_REVIEW.md auf "✓ erledigt mit Doku-Verifikation" gesetzt. | **CODE_REVIEW.md L1+L3+L5+L6 (Härtungs-Bündel):** Vier LOW-Priorisierungen in einem Rutsch erledigt. L1 macht Retry-Verhalten ops-anpassbar. L3 schliesst Log-Flood-Vector. L5 macht Audit-Trail vollstaendiger (warum, nicht nur was). L6 war Doku-Lücke, kein Code-Bug. **L2 + L7 bewusst offen gelassen:** L2 zu unscharf (Cleanup-Policy ist Produktentscheidung), L7 ist Feature-Removal das explizite Bestätigung braucht. Build clean, 380 Tests passed (identisch — keine Verhaltensänderung erwartet, neuer Reason-Defaultwert ist `null`). | **Risiko-Restwert sehr niedrig:** L1+L3+L6 sind reine Härtungs-Adds ohne Verhaltensänderung in Bestandscode. L5 hat eine DB-Migration — `IF NOT EXISTS` schuetzt vor Re-Apply, Catch um `42703` schuetzt vor noch nicht migrierten DB-Zuständen. Frontend-Konsumenten der `AdminPermissionAuditEntryDto` werden Reason nicht zeigen bis Frontend angepasst wird. |
| 2026-05-02 | CODE-REVIEW-H6 | done | **Neu:** `api/API/RotationTaskRegenerationEngine.cs` (pure-static-Domain-Engine, ~115 Zeilen, keine DB-Dependencies). Methoden: `Plan(existing, desired)` partitioniert in `ToCreate/ToUpdate/ToCancel/UnchangedCount`, `NeedsUpdate(existing, desired)` Vergleichs-Pruefung, `BuildMatchKey(stationId, templateId)` Schluesselbildung. Plan-Records: `RotationTaskRegenerationPlan`, `RotationTaskUpdate`. **api/API.Tests/RotationTaskRegenerationEngineTests.cs:** 12 Unit-Tests (alle ohne DB) — decken alle 4 Partitions, Terminal-Status-Schutz, NULL-vs-Empty-Description, Case-Insensitive-Trigger-Vergleich, Unsynchronisierte Existing-Records. **PostgresRotationRepository.TaskGenerationOperations.cs:** `RotationDesiredTaskRecord` + `RotationGeneratedTaskRecord` von `private sealed class` (geneste in Partial) zu Top-Level `internal sealed class` promotiert. `SynchronizeRotationGeneratedTasks` (~157 Zeilen) auf ~75 Zeilen reduziert: ruft `RotationTaskRegenerationEngine.Plan(...)` auf und iteriert ueber `plan.ToCreate` / `plan.ToUpdate` / `plan.ToCancel`. Alte `RotationTaskNeedsUpdate` + `BuildRotationTaskMatchKey`-Helper aus dem Partial entfernt (jetzt in der Engine). | **CODE_REVIEW.md H6 (Domain-Service-Extraktion):** Pure-Logic-Trennung der Regenerierungs-Entscheidung von der DB-Ausfuehrung. Pattern: Engine entscheidet ohne Seiteneffekte → Plan-Record → Repository fuehrt aus. Build clean, 380 passed (vorher 368, +12 neue Engine-Unit-Tests), 1 skipped, 0 failed. Erste echte Unit-Test-Coverage fuer das Rotation-Subsystem. | **Risiko-Restwert sehr niedrig:** Pure Refactoring ohne Verhaltensaenderung — die DB-Ausfuehrungslogik (Insert/Update/Cancel + Audit + PrimaryAssignment) ist 1:1 erhalten, nur die Reihenfolge der DB-Operationen ist deterministischer (erst alle Creates, dann alle Updates, dann alle Cancels statt durchmischt). Audit-Snapshot-Helper bleiben bewusst im Repository (sind JSON-Projektionen mit Audit-Format-Bezug, kein Domain-Logik-Wert). |
| 2026-05-02 | LEGACY-SCHRITT-8.C3 | done | **Neu:** `api/API/Repositories/IWorkflowNotificationReadRepository.cs` (2 Methoden `GetWorkflowCreatedNotificationDispatchTargets`, `GetWorkflowUidsWithDisabledNotifications`), `api/API/Repositories/PostgresWorkflowNotificationReadRepository.cs` (Standalone-Read-Klasse, ~365 Zeilen, implementiert sowohl `INotificationTemplatePreviewRepository` (3 Preview-Methoden) als auch das neue `IWorkflowNotificationReadRepository` (2 generische Reads)). **PostgresWorkflowRepository.NotificationOperations.cs:** 5 Read-Methoden entfernt — schrumpft von 534 auf 183 Zeilen, behaelt nur noch die 3 Write-Pfade (`CreateReadyTaskNotifications`, `CreateWorkflowCompletionNotifications`, `ApplyNotificationDispatchResults`). **IWorkflowRepository.cs:** Methoden `GetWorkflowCreatedNotificationDispatchTargets`, `GetWorkflowUidsWithDisabledNotifications` entfernt. **PostgresWorkflowRepository.cs:** Implements-Liste verkleinert um `INotificationTemplatePreviewRepository` (jetzt nur noch `IWorkflowRepository, IWorkflowAutomationRepository`). **WorkflowRuntimeService.cs:** Constructor um `IWorkflowNotificationReadRepository workflowNotificationReadRepository` erweitert; Aufruf an `GetWorkflowCreatedNotificationDispatchTargets` umgestellt. **AdminRuntimeConfigEndpoints.cs:** Endpoint-Signatur und `ReplayDisabledNotifications`-Helper um `IWorkflowNotificationReadRepository`-Parameter erweitert; Aufruf an `GetWorkflowUidsWithDisabledNotifications` umgestellt. **LifecycleServiceCollectionExtensions.cs:** Concrete-Type-Registrierung `services.AddScoped<PostgresWorkflowNotificationReadRepository>()` plus zwei Interface-Aliase (`INotificationTemplatePreviewRepository` + `IWorkflowNotificationReadRepository`), beide via `sp.GetRequiredService<PostgresWorkflowNotificationReadRepository>()` — gemeinsamer Scope-Lifetime fuer beide Rollen. **API.Tests/WorkflowEndpointsTests.cs:** `StubWorkflowRepository` implementiert nun zusaetzlich `IWorkflowNotificationReadRepository` (Methoden bleiben unveraendert), DI-Registrierung ergaenzt. **API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs:** 2 nicht mehr noetige Throw-Stub-Methoden (`GetWorkflowCreatedNotificationDispatchTargets`, `GetWorkflowUidsWithDisabledNotifications`) entfernt. | **Phase 8.C3 (Read-Pfad-Extraktion):** Letzte der drei optionalen 8.C-Sub-Phasen. Sauberster Schnitt bisher — eine Klasse implementiert zwei bestehende/neue Read-Interfaces. PostgresWorkflowRepository ist nach 8.C3 nur noch `IWorkflowRepository, IWorkflowAutomationRepository` (vorher 4 Interfaces). Build clean, 368 Tests passed (identisch). | **Risiko-Restwert sehr niedrig:** Standalone-Read-Extraktion. Die Preview-Methoden hatten bereits ein eigenes Interface (`INotificationTemplatePreviewRepository`), das jetzt von der eigenstaendigen Read-Klasse bedient wird statt vom Monolith-Repository. **Phase 8 (Repository-Schnitte) komplett abgeschlossen.** Nach 8.C3: Hauptklasse `PostgresWorkflowRepository` ist deutlich gestrafft (4 → 2 Interfaces, NotificationOperations.cs 534 → 183 Zeilen). |
| 2026-05-02 | LEGACY-SCHRITT-8.C2 | done | **Neu:** `api/API/Repositories/IWorkflowAutomationReadRepository.cs` (2 Public-Methoden `GetAdminActionDefinitions`, `GetAutomationJobs`), `api/API/Repositories/PostgresWorkflowAutomationReadRepository.cs` (Standalone-Klasse, nutzt `LifecycleRuntimeSettingsResolver` und `PostgresWorkflowAutomationOperations.IsMissingWorkflowBuilderAutomationSchema` als Static-Helper). **IWorkflowAutomationRepository.cs:** Read-Methoden entfernt — bleibt nur noch der Write-Pfad (`ClaimNextPendingAutomationJob`, `CompleteAutomationJobSuccess`, `CompleteAutomationJobFailure`). **PostgresWorkflowRepository.AutomationOperations.cs:** Zeilen 8-208 entfernt (die zwei Read-Methoden), Datei schrumpft von 510 auf ~310 Zeilen, behaelt nur noch den Write-Pfad. **WorkflowAutomationService.cs:** Constructor um `IWorkflowAutomationReadRepository readRepository` erweitert; `GetActionDefinitionsAsync` + `GetWorkflowAutomationJobsAsync` rufen jetzt `readRepository.X` statt `repository.X`. **LifecycleServiceCollectionExtensions.cs:** DI-Registrierung `services.AddScoped<IWorkflowAutomationReadRepository, PostgresWorkflowAutomationReadRepository>()` ergaenzt. **API.Tests/WorkflowAutomationServiceTests.cs:** `StubWorkflowAutomationRepository` implementiert nun zusaetzlich `IWorkflowAutomationReadRepository`. 3 `WorkflowAutomationService(...)`-Konstruktor-Aufrufe um den 2. Repository-Parameter erweitert (Stub uebernimmt beide Rollen). **API.Tests/PostgresWorkflowRepositoryAutomationIntegrationTests.cs:** 4 Aufrufe von `repository.GetAutomationJobs(...)` umgestellt auf `new PostgresWorkflowAutomationReadRepository().GetAutomationJobs(...)`; `CreateAutomationService`-Helper um Read-Repo erweitert. | **Phase 8.C2 (Read-Pfad-Extraktion):** Zweite der drei optionalen 8.C-Sub-Phasen. Standalone-Reads `GetAdminActionDefinitions` + `GetAutomationJobs` als eigene Klasse — keine Verhaltensaenderung, sauberer Schnitt. WorkflowAutomationService haelt jetzt bewusst zwei Repository-Dependencies: das Write-Repo (`IWorkflowAutomationRepository`, fuer Job-Lifecycle) und das Read-Repo (`IWorkflowAutomationReadRepository`, fuer Listing). Build clean, 368 Tests passed. | **Risiko-Restwert sehr niedrig:** Standalone-Read-Extraktion analog zu 8.C1. Erfreuliche Sub-Phase, weil die Read-Methoden keine Cross-Slice-Dependencies hatten — nur SharedHelpers + ein static Schema-Check auf `PostgresWorkflowAutomationOperations`. |
| 2026-05-02 | LEGACY-SCHRITT-8.C1 | done | **Neu:** `api/API/Repositories/IWorkflowAuditReadRepository.cs` (1 Public-Methode `GetWorkflowAuditLog`), `api/API/Repositories/PostgresWorkflowAuditReadRepository.cs` (Standalone-Klasse, nutzt `LifecycleRuntimeSettingsResolver.GetRequiredConnectionString()` direkt, keine Service-Dependencies). **Geloescht:** `api/API/Repositories/PostgresWorkflowRepository.AuditOperations.cs` (war nach 8.A1 nur noch ein Method-Wrapper, jetzt obsolet). **IWorkflowRepository.cs:** Methode `GetWorkflowAuditLog` entfernt (Zeile 45). **WorkflowRuntimeService.cs:** Constructor um `IWorkflowAuditReadRepository workflowAuditReadRepository` erweitert; `GetWorkflowAuditLogAsync` ruft jetzt `workflowAuditReadRepository.GetWorkflowAuditLog` statt `repository.GetWorkflowAuditLog`. **LifecycleServiceCollectionExtensions.cs:** DI-Registrierung `services.AddScoped<IWorkflowAuditReadRepository, PostgresWorkflowAuditReadRepository>()` ergaenzt. **API.Tests/WorkflowEndpointsTests.cs:** `StubWorkflowRepository` implementiert nun zusaetzlich `IWorkflowAuditReadRepository` (Methode `GetWorkflowAuditLog` bleibt unveraendert, gleiche Signatur), `CreateApp` registriert Stub auch fuer `IWorkflowAuditReadRepository`. **API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs:** `GetWorkflowAuditLog`-Methode aus Stub entfernt (war nur Throw-Stub, Endpoint nutzt sie nicht). **API.Tests/PostgresWorkflowRepositoryAuditLogIntegrationTests.cs:** 8 Aufrufe von `repository.GetWorkflowAuditLog(...)` umgestellt auf `new PostgresWorkflowAuditReadRepository().GetWorkflowAuditLog(...)`. | **Phase 8.C1 (Read-Pfad-Extraktion):** Erste der drei optionalen 8.C-Sub-Phasen. `GetWorkflowAuditLog` ist eine reine Read-Operation ohne Cross-Slice-Dependencies — perfekter Mini-Schnitt zum Kalibrieren des Patterns. Build clean (0 Errors), 368 Tests passed (identisch). | **Risiko-Restwert sehr niedrig:** Standalone-Read ohne Verhaltensaenderung. WorkflowRuntimeService hat jetzt eine Dependency mehr (`IWorkflowAuditReadRepository`), was 1x DI-Update + 1x Test-Stub-Anpassung kostete. Sauberes Vorbild fuer 8.C2 (PostgresWorkflowAutomationRepository Read-Pfade) und 8.C3 (PostgresWorkflowNotificationRepository Read-Pfade). |
| 2026-05-02 | LEGACY-SCHRITT-8.B1b | done | **Neu:** `api/API/Repositories/PostgresWorkflowRuntimeRepository.cs` (~2895 Zeilen, eigenstaendige Klasse, implementiert `IWorkflowDefinitionRuntimeRepository`). **Geloescht:** `api/API/Repositories/PostgresWorkflowRepository.WorkflowRuntimeOperations.cs`. **PostgresWorkflowRepository.cs:** Implements-Liste verkleinert um `IWorkflowDefinitionRuntimeRepository` (jetzt nur noch `IWorkflowRepository, IWorkflowAutomationRepository, INotificationTemplatePreviewRepository`). **PostgresWorkflowRepository.RequirementOperations.cs:** `ValidateSupervisorSelections` von `private static` zu `internal static`. **PostgresWorkflowRepository.AutomationOperations.cs:** 8 Cross-Partial-Calls auf `PostgresWorkflowRuntimeRepository.X` qualifiziert (`InsertWorkflowRuntimeEvent`, `UpdateNodeInstanceStatus`, `LoadRuntimeWorkflowHeader`, `AdvanceRuntimeUntilWaitOrTerminal`, `FailRuntimeWorkflow`, `CreateJsonbPayload`, `NodeInstanceStatusDone`, `NodeInstanceStatusFailed`). **PostgresWorkflowRepository.TaskOperations.cs:** 3 Cross-Partial-Calls qualifiziert (`CompleteRuntimeTaskNodeFromTaskStatusUpdate`, `TryAdvanceRuntimeSetupFromTaskStatusUpdate`, `ApplyRuntimeApprovalDecisionFromWorkflowTask`). **PostgresWorkflowRepository.LifecycleOperations.cs:** 1 Cross-Partial-Call qualifiziert (`CompleteRuntimeSupervisorGatekeeperStep`). **LifecycleServiceCollectionExtensions.cs:** DI-Registrierung `IWorkflowDefinitionRuntimeRepository` zeigt nun auf `PostgresWorkflowRuntimeRepository`. **API.Tests/PostgresWorkflowRepositoryAutomationIntegrationTests.cs + API.Tests/PostgresWorkflowRepositoryWorkflowDefinitionIntegrationTests.cs:** 9 Test-Setups erweitert um `var runtimeRepository = new PostgresWorkflowRuntimeRepository();` neben dem bestehenden `repository`. Aufrufe von `repository.PublishWorkflowDefinitionVersion` / `CreateWorkflowDefinitionInstance` / `GetWorkflowDefinitionRuntimeDetail` / `CompleteRuntimeTaskNode` umgestellt auf `runtimeRepository.X`. | **Phase 8.B1b (Runtime-Klassen-Extraktion):** Volle Trennung von `PostgresWorkflowRuntimeRepository` aus dem Partial-Class-Monolithen. Constructor injiziert 5 Services (`IWorkflowAuditWriteOperations`, `IWorkflowStatusCalculationService`, `IWorkflowNotificationDispatchOperations`, `IWorkflowAutomationOperations`, `IWorkflowDefinitionValidationService`). Cross-Partial-Calls in beide Richtungen sauber qualifiziert: Runtime-Klasse ruft `PostgresWorkflowRepository.X` (8.B1a-Promotions) fuer Definition-Admin-/Anforderungs-Helper, Partial-Klasse ruft `PostgresWorkflowRuntimeRepository.X` fuer Runtime-Helper. 12 Methoden + 2 Konstanten + 1 Record (`RuntimeWorkflowHeaderRecord`) zusaetzlich auf `internal` promotiert (Cross-Caller aus AutomationOperations + TaskOperations + LifecycleOperations). Build clean (0 Errors), 368 passed / 1 skipped / 0 failed (identisch zum Stand vor 8.B1b). | **Risiko-Restwert niedrig:** Das Pattern (eigene Klasse + internal-static-Wrapper fuer Cross-Caller) ist analog zu 8.A2-8.A5. Keine Verhaltensaenderung — ausschliesslich Strukturschnitt. Offene Folgearbeit: 8.C (Read-Pfad-Extraktionen `PostgresWorkflowAuditReadRepository` / `PostgresWorkflowAutomationRepository` / `PostgresWorkflowNotificationRepository`) bleibt offen aber optional. **Hinweis:** Die internal-static-Wrapper auf `PostgresWorkflowRuntimeRepository` (z.B. `InsertWorkflowRuntimeEvent`) sind ein temporaerer Trade-off — Cross-Partial-Aufrufe aus AutomationOperations sind die letzten Klammern, die noch in den Runtime-Internals fummeln. Wenn AutomationOperations spaeter ebenfalls extrahiert wird (Phase 8.D?), koennen viele dieser internals wieder zu private static werden. |
| 2026-05-02 | LEGACY-SCHRITT-6.3d-iv-FOLLOWUP | done | **db/02_dev_seed.sql:** Am Anfang `SET session_replication_role = replica;` (mit Kommentar zum pg_dump-Verhalten), am Ende `SET session_replication_role = origin;`. | Seed-Datei ist alphabetisches `pg_dump`-Output: `workflow_answer_definitions` (Zeile ~133) wird vor seinem FK-Ziel `workflow_definitions` (Zeile ~336) eingefuegt. Nach 6.3d-iv (FK-Migration) verletzte das beim Reseed `workflow_answer_definitions_workflow_definition_id_fkey`. Fix analog zu `pg_dump --disable-triggers`: Trigger-Auswertung waehrend des Loads abschalten, am Ende reaktivieren. Verifikation: vorher 323 passed / 45 failed (alle FK), nachher 368 passed / 1 skipped / 0 failed. | Keine. App-User darf `session_replication_role` setzen (PG16-Setup verifiziert). FK-Constraints sind nach dem Seed wieder aktiv. |
| 2026-05-01 | LEGACY-SCHRITT-8.B1a | done | **PostgresWorkflowRepository.cs:** `ProcessTypeCreateRecord` von `private sealed class` (geneste in Partial) zu Top-Level `internal sealed class` verschoben (am Datei-Ende). **WorkflowDefinitionAdminOperations.cs:** Konstanten `WorkflowDefinitionDraftStatus`, `WorkflowDefinitionPublishedStatus` von `private const` zu `internal const`. Methoden `GetWorkflowDefinitionVersionRecord`, `ResolveWorkflowDefinitionLegacyProcessTypeId` von `private static` zu `internal static`. `WorkflowDefinitionVersionRecord` von `private sealed class` zu `internal sealed class`. `GetAdminWorkflowDefinitionVersionDetailById` von `private async` (Instance) zu `internal static async` mit zwei zusaetzlichen Parametern: `IWorkflowAutomationOperations automation, IWorkflowDefinitionValidationService validationService`. `BuildWorkflowDefinitionValidationSnapshot` analog umgestellt. **WorkflowCreateOperations.cs:** `LoadProcessTypeForCreate`, `SplitDisplayName` von `private static` zu `internal static`. **RequirementOperations.cs:** `LoadAnswerDefinitionRecords`, `LoadRoleDefaultRecords`, `PersistWorkflowAnswers` von `private static` zu `internal static`. **TaskOperations.cs:** `LoadWorkflowStatusForUpdate` von `private static` zu `internal static`. **WorkflowRuntimeOperations.cs (Inverse-Richtung fuer LifecycleOperations-Caller):** `CompleteRuntimeSupervisorGatekeeperStep` von `private async` zu `internal static async` (keine Instance-Dependencies). **Call-Sites:** 9x `GetAdminWorkflowDefinitionVersionDetailById(...)` mit `_automation, _workflowDefinitionValidationService` ergaenzt (alle in Instance-Methoden). Innerhalb `BuildWorkflowDefinitionValidationSnapshot` `_automation.X` zu `automation.X` und `_workflowDefinitionValidationService.X` zu `validationService.X` umgeschrieben. | **Phase 8.B1a (Vorbereitungs-Slice fuer 8.B1b):** Cross-Partial-Dependencies fuer Runtime-Extraktion sichtbar gemacht. 11 Methoden + 2 Konstanten + 2 Records promotiert auf `internal`. Pattern analog zu Phase 8.0 (Vorarbeit fuer 8.A). Komplexitaet: `GetAdminWorkflowDefinitionVersionDetailById` brauchte Static-Conversion mit 2 zusaetzlichen Parametern, weil es Instance-Felder `_automation` und `_workflowDefinitionValidationService` (via `BuildWorkflowDefinitionValidationSnapshot`) verwendet hat. Andere Helper waren bereits static und brauchten nur Sichtbarkeitsanpassung. .NET Build clean (0 Errors), 323 Tests passing (45 DB-Connection-Failures wie immer ohne laufende Postgres). | **Vorbereitungs-Schritt fuer 8.B1b** — der eigentliche Klassen-Extrakt von `PostgresWorkflowRuntimeRepository` aus `WorkflowRuntimeOperations.cs` ist jetzt entkoppelt und unblockt. Die nicht-static-Variante von `GetAdminWorkflowDefinitionVersionDetailById` brachte einen Trade-off: 9 Call-Sites mussten 2 Extra-Parameter durchreichen — alternativ haette ich es auf instance lassen und einen Service-Wrapper bauen koennen. Static-mit-Params ist pragmatisch und passt zum Pattern aus 8.A2-8.A5. **Naechster Schritt:** 8.B1b = `PostgresWorkflowRuntimeRepository`-Klasse extrahieren (3-4h, opus high). Constructor injiziert `IWorkflowAuditWriteOperations`, `IWorkflowStatusCalculationService`, `IWorkflowNotificationDispatchOperations`. Cross-Partial-Caller in AutomationOperations + TaskOperations rufen die internal-static-Methoden des neuen Klasse auf. |
| 2026-05-01 | LEGACY-SCHRITT-8.A5 | done | **Neu:** `api/API/Repositories/IWorkflowAutomationOperations.cs` (2 Public-Methoden), `api/API/Repositories/PostgresWorkflowAutomationOperations.cs` (~610 Zeilen mit 2 Instance-Methoden + 13 `internal static`-Wrappern + 7 `private static`-Helpern + Top-Level `AutomationPayloadContextRecord` Klasse + 5 `internal const string`-Job-Status-Konstanten). **AutomationOperations.cs:** auf ~440 Zeilen geschrumpft (von 1194). Alle 22 privaten Statics + 1 nested Klasse + 5 Konstanten entfernt. Die 5 Public-Methoden (`GetAdminActionDefinitions`, `GetAutomationJobs`, `ClaimNextPendingAutomationJob`, `CompleteAutomationJobSuccess`, `CompleteAutomationJobFailure`) bleiben als Repository-Surface und rufen jetzt `PostgresWorkflowAutomationOperations.{LoadNextAutomationAttemptNumberAsync, InsertAutomationJobAttemptAsync, SetAutomationJobStatusAsync, CompleteAutomationAttemptAsync, InsertAutomationLogsAsync, LoadNextAutomationNodeActionAsync, BuildAutomationJobPayloadAsync, CreateAutomationJobAsync, CancelPendingAutomationJobsForNodeInstanceAsync, IsMissingWorkflowBuilderAutomationSchema}(...)` und `PostgresWorkflowAutomationOperations.AutomationJobStatus{Pending,Running,Succeeded,Failed,Cancelled}`. **PostgresWorkflowRepository.cs:** Feld `_automation` + neuer 7-Parameter-Konstruktor. **LifecycleServiceCollectionExtensions.cs:** DI fuer `IWorkflowAutomationOperations`. **Cross-Partial-Call-Sites umgestellt:** WorkflowRuntimeOperations.cs:1231/1238 (AdvanceRuntimeUntilWaitOrTerminal, static) — `PostgresWorkflowAutomationOperations.{BuildAutomationJobPayloadAsync,CreateAutomationJobAsync}`. WorkflowDefinitionAdminOperations.cs:690/804/1174 (instance) — `PostgresWorkflowAutomationOperations.IsMissingWorkflowBuilderAutomationSchema(ex)` (statisch, da pure utility) und `_automation.{ActionDefinitionExists, ResolveActionDefinitionIdByKey}(...)`. | **Phase 8.A5:** `IWorkflowAutomationOperations` extrahiert. Naming bewusst `Operations` statt `Service`, da `IWorkflowAutomationService` bereits in `Services/` existiert (anderer Zweck: Job-Polling-Loop + Action-Handler-Dispatch via `IWorkflowAutomationActionHandler`). Pattern wie 8.A2-8.A4: 2 Public-Interface-Methoden (`ActionDefinitionExists`, `ResolveActionDefinitionIdByKey` — die instance-callable Cross-Partial-Methoden) + 13 `internal static`-Wrapper, die von den verbliebenen Public-Methoden in AutomationOperations.cs (5 Stueck) sowie von WorkflowRuntimeOperations.AdvanceRuntimeUntilWaitOrTerminal genutzt werden. `IsMissingWorkflowBuilderAutomationSchema` als `internal static` ausserhalb des Interface (pure utility, nimmt nur `PostgresException`). Konstanten `AutomationJobStatusXxx` als `internal const string` im Service — alle Aufrufer referenzieren sie ueber `PostgresWorkflowAutomationOperations.AutomationJobStatusXxx`. `AutomationPayloadContextRecord` als Top-Level `internal sealed class` ans Ende der Service-Datei. .NET Build clean (0 Errors), 323 Tests passing (45 DB-Connection-Failures wie immer ohne laufende Postgres). | **Phase 8.A vollstaendig abgeschlossen.** Alle 5 Sub-Services extrahiert: `IWorkflowAuditWriteOperations` (8.A1), `IWorkflowTaskGenerationService` (8.A2), `IWorkflowStatusCalculationService` (8.A3), `IWorkflowNotificationDispatchOperations` (8.A4), `IWorkflowAutomationOperations` (8.A5). PostgresWorkflowRepository hat jetzt 5 `IXxxOperations`-Felder fuer cross-partial-Aufrufe + `IRotationRepository` + `IWorkflowDefinitionValidationService` (insgesamt 7 Konstruktor-Parameter). **Naechster Schritt:** 8.B1 = `PostgresWorkflowRuntimeRepository` als eigene Klasse extrahieren (4-6h, opus high) — die WorkflowRuntime-Public-Methoden + 15 file-interne Helper aus `WorkflowRuntimeOperations.cs` in eine eigene Klasse, die alle 5 Service-Interfaces aus 8.A injiziert bekommt. |
| 2026-05-01 | LEGACY-SCHRITT-8.A4 | done | **Neu:** `api/API/Repositories/IWorkflowNotificationDispatchOperations.cs` (1 Public-Methode), `api/API/Repositories/PostgresWorkflowNotificationDispatchOperations.cs` (~430 Zeilen mit Instance-Methode + 7 `internal static`-Wrappern). **NotificationOperations.cs:** auf 460 Zeilen geschrumpft (von 997). Alle 8 privaten Helper (`LoadWorkflowProcessTypeForNotifications`, `InsertWorkflowNotification`, `CreateWorkflowNotifications`, `ResolveWorkflowCreatedRecipients`, `BuildReadyTaskNotificationPreviewTargets`, `ResolveWorkflowCreatedResponsibilityId`, `LoadWorkflowCreatedResponsibilityIdFromProcessTasks`, `CreateReadyTaskNotifications`-private-static) entfernt. Public-Methoden (`GetWorkflowCreatedPreviewTargets`, `GetWorkflowCreatedNotificationDispatchTargets`, `GetTaskReadyPreviewTargets`, `CreateReadyTaskNotifications`-public, `GetWorkflowCompletedPreviewTargets`, `CreateWorkflowCompletionNotifications`, `GetWorkflowUidsWithDisabledNotifications`, `ApplyNotificationDispatchResults`) bleiben als Repository-Surface und rufen jetzt `PostgresWorkflowNotificationDispatchOperations.{LoadWorkflowProcessTypeForNotificationsAsync, ResolveWorkflowCreatedRecipientsAsync, BuildReadyTaskNotificationPreviewTargetsAsync, CreateReadyTaskNotificationsAsync, InsertWorkflowNotificationAsync}(...)`. **PostgresWorkflowRepository.cs:** Feld `_notificationDispatch` + neuer 6-Parameter-Konstruktor. **LifecycleServiceCollectionExtensions.cs:** DI fuer `IWorkflowNotificationDispatchOperations`. **Cross-Partial-Call-Sites umgestellt:** WorkflowCreateOperations.cs:251 (CreateWorkflow, instance) und WorkflowRuntimeOperations.cs:334 (CreateWorkflowDefinitionInstance, instance) — beide nutzen jetzt `_notificationDispatch.CreateWorkflowNotifications(...)`. | **Phase 8.A4:** `IWorkflowNotificationDispatchOperations` extrahiert. Pattern wie 8.A2/8.A3: 1 Public-Interface-Methode (`CreateWorkflowNotifications` — der Cross-Partial-Aufruf) + 7 `internal static`-Wrapper, die von den verbliebenen Public-Methoden in NotificationOperations.cs sowie von `CreateWorkflowNotificationsAsync` selbst genutzt werden. Naming bewusst `Operations` statt `Service`, da `IWorkflowNotificationDispatchService` bereits in `Services/` existiert (anderer Zweck: E-Mail-Dispatch via Graph). Trennung sauber: Repositories/`IWorkflowNotificationDispatchOperations` = Notification-Records anlegen + Recipients aufloesen (DB-Layer), Services/`IWorkflowNotificationDispatchService` = E-Mail tatsaechlich verschicken (Graph-API-Layer). .NET Build clean (0 Errors), 323 Tests passing (45 DB-Connection-Failures wie immer ohne laufende Postgres). | **Read/Preview-Pfade bleiben in Repository-Surface**: `GetWorkflowCreatedPreviewTargets`, `GetTaskReadyPreviewTargets`, `GetWorkflowCompletedPreviewTargets`, `GetWorkflowCreatedNotificationDispatchTargets`, `CreateReadyTaskNotifications` (public), `CreateWorkflowCompletionNotifications`, `GetWorkflowUidsWithDisabledNotifications`, `ApplyNotificationDispatchResults` sind weiterhin `IWorkflowRepository`/`INotificationTemplatePreviewRepository`-Methoden. Die Aufteilung "Read/Preview-Pfad vs. Create-during-Mutation-Pfad" (laut TODO.md-Plan) ist hier so realisiert: Cross-Partial-Aufrufer (Mutation) gehen ueber den Service-Interface (DI), Repository-Public-API-Methoden (auch Read/Preview + Read+Create) bleiben in der Partial und delegieren intern an die Service-Statics. Naechster Schritt: 8.A5 = `IWorkflowAutomationService` (groesste Sub-Phase, AutomationOperations.cs hat 1207 Zeilen). |
| 2026-05-01 | LEGACY-SCHRITT-8.A3 | done | **Neu:** `api/API/Repositories/IWorkflowStatusCalculationService.cs` (7 Methoden), `api/API/Repositories/PostgresWorkflowStatusCalculationService.cs` (~430 Zeilen mit Instance-Methoden + `internal static`-Wrappern). **Geloescht:** `api/API/Repositories/PostgresWorkflowRepository.StatusCalculationOperations.cs` (vollstaendig leer nach Verschiebung der 7 Methoden in den neuen Service). **PostgresWorkflowRepository.cs:** Feld `_statusCalculation` + neuer 5-Parameter-Konstruktor. **LifecycleServiceCollectionExtensions.cs:** DI fuer `IWorkflowStatusCalculationService`. **Call-Sites umgestellt (Instance):** `LifecycleOperations.cs`, `LinkOperations.cs`, `TaskOperations.cs`, `WorkflowCreateOperations.cs`, `TaskGenerationOperations.cs` (CompleteWorkflowTaskByKey instance), `WorkflowRuntimeOperations.cs` (CompleteRuntimeApprovalNode + CompleteRuntimeTaskNode instance) — alle nutzen jetzt `_statusCalculation.X(...)`. **Call-Sites umgestellt (Static):** `TaskGenerationOperations.cs` (BackfillLegacyInitialTasksForSupervisorCompletion static), `WorkflowRuntimeOperations.cs` (AdvanceRuntimeUntilWaitOrTerminal + EnsureRuntimeSetupTasksGenerated static) — alle nutzen jetzt `PostgresWorkflowStatusCalculationService.{TryLockWorkflowForTaskStatusUpdateAsync, LoadTaskStateForUpdateAsync, AreTaskDependenciesSatisfiedAsync, PersistTaskStatusAsync, SyncPrimaryAssignmentCompletionAsync, RecalculateWorkflowTaskAvailabilityAsync, RecalculateAndPersistWorkflowStatusAsync}(...)`. **Bonus-Cleanup:** In `WorkflowRuntimeOperations.cs` 2x Instance-Methoden, die noch `PostgresRepositorySharedHelpers.InsertAuditEntry(...)` aufriefen, auf `_auditWrite.InsertAuditEntry(...)` umgestellt (CompleteRuntimeApprovalNode + CompleteRuntimeTaskNode). | **Phase 8.A3:** `IWorkflowStatusCalculationService` extrahiert. Service exponiert 7 Public-Methoden inkl. `RecalculateAndPersistWorkflowStatus`, `RecalculateWorkflowTaskAvailability`, `PersistTaskStatus`, `SyncPrimaryAssignmentCompletion`, `LoadTaskStateForUpdate`, `AreTaskDependenciesSatisfied`, `TryLockWorkflowForTaskStatusUpdate`. Pattern wie 8.A2: Instance-Methoden delegieren an `internal static`-Wrapper, sodass statische Aufrufer in WorkflowRuntimeOperations + TaskGenerationOperations.BackfillLegacy ohne Konstruktor-Injection durchkommen. Tuple-Return-Type `(long, Guid, string, bool, string, bool, string, long?, bool)?` fuer `LoadTaskStateForUpdate` beibehalten — alle Caller nutzen Tuple-Destructuring `var (workflowId, _, status, ...) = taskRecord.Value;`, Klassen-Conversion waere unnoetiger Touch von ~15 Call-Sites. .NET Build clean (0 Errors), 323 Tests passing (45 DB-Connection-Failures wie immer ohne laufende Postgres). **Datei-Anzahl in `Repositories/`:** Status-Partial weg (1 Datei weniger), 2 neue Service-Dateien (1 Interface + 1 Impl). | **Cross-Service-Kopplung weiter geloest:** Der TaskGeneration-Service hat in 8.A2 die 5 file-internen Helper als statische Methoden, jetzt ruft `BackfillLegacyInitialTasksForSupervisorCompletion` (statisch in TaskGenerationOperations.cs) sowohl `PostgresWorkflowTaskGenerationService.GenerateWorkflowTasksAsync` als auch `PostgresWorkflowStatusCalculationService.{Persist,Sync,Recalculate}Async` — saubere statische Service-zu-Service-Aufrufe. **Naechster Schritt:** 8.A4 = `IWorkflowNotificationDispatchService` extrahieren (`CreateWorkflowNotifications` + ~10 Sub-Helper, aufgeteilt in Read/Preview-Pfade und Create-during-Mutation-Pfade). Erwarteter Aufwand 3-4h, opus high. |
| 2026-05-01 | LEGACY-SCHRITT-8.A2 | done | **Neu:** `api/API/Repositories/IWorkflowTaskGenerationService.cs`, `api/API/Repositories/PostgresWorkflowTaskGenerationService.cs` (~430 Zeilen). **PostgresRepositorySharedHelpers.cs:** +`LoadDepartmentLeadResponsibilityId`, +`ResolveDepartmentRequirementSelectionAssignment`, +`NormalizeAdminTaskTemplateIconKey`, +Top-Level-Klasse `WorkflowTaskGenerationContext`. **PostgresWorkflowRepository.cs:** Feld `_taskGeneration` + neuer 4-Parameter-Konstruktor. **LifecycleServiceCollectionExtensions.cs:** DI fuer `IWorkflowTaskGenerationService`. **TaskGenerationOperations.cs:** auf 130 Zeilen geschrumpft (`GenerateWorkflowTasks`+5 Helper+`WorkflowTaskGenerationContext`-Klasse entfernt; `BackfillLegacyInitialTasksForSupervisorCompletion` ruft jetzt `PostgresWorkflowTaskGenerationService.GenerateWorkflowTasksAsync`; `CompleteWorkflowTaskByKey` Instance-Methode unveraendert). **AssignmentOperations.cs:** `LoadDepartmentLeadResponsibilityId` und `ResolveDepartmentRequirementSelectionAssignment` entfernt; Datei nur noch ProcessArea-Helper. **TaskTemplateAdminOperations.cs:** `NormalizeAdminTaskTemplateIconKey` entfernt. **Call-Sites umgestellt:** `LinkOperations.cs`, `WorkflowCreateOperations.cs`, `LifecycleOperations.cs` rufen jetzt `_taskGeneration.GenerateWorkflowTasks(...)`. `WorkflowRuntimeOperations.cs` (statische Methoden) ruft `PostgresWorkflowTaskGenerationService.GenerateWorkflowTasksAsync/LoadWorkflowDueAtAsync/LoadWorkflowTaskGenerationContextAsync`. `NotificationOperations.cs`, `WorkflowRuntimeOperations.cs` rufen `PostgresRepositorySharedHelpers.ResolveDepartmentRequirementSelectionAssignment`. `ReadOperations.cs`, `RequirementOperations.cs`, `TaskTemplateAdminOperations.cs`, `WorkflowRuntimeOperations.cs` rufen `PostgresRepositorySharedHelpers.NormalizeAdminTaskTemplateIconKey`. | **Phase 8.A2:** `IWorkflowTaskGenerationService` extrahiert. Service exponiert 3 Public-Methoden: `GenerateWorkflowTasks`, `LoadWorkflowTaskGenerationContext`, `LoadWorkflowDueAt`. Implementierung im neuen `PostgresWorkflowTaskGenerationService` mit zusaetzlichen `internal static`-Wrappern (`GenerateWorkflowTasksAsync`, `LoadWorkflowTaskGenerationContextAsync`, `LoadWorkflowDueAtAsync`) — die Instanz-Methoden delegieren an die statischen, sodass statische Aufrufer (z.B. `EnsureRuntimeSetupTasksGenerated`, `CreateRuntimeWorkflowTask` in WorkflowRuntimeOperations) den Service ohne Konstruktor-Injection aufrufen koennen. Pattern erlaubt sauberen Uebergang in 8.B1 wenn die WorkflowRuntime-Statics in eigene Klasse gezogen werden. Cross-Slice-Helper `ResolveDepartmentRequirementSelectionAssignment`, `LoadDepartmentLeadResponsibilityId`, `NormalizeAdminTaskTemplateIconKey` aus den AssignmentOperations/TaskTemplateAdminOperations-Partials in `SharedHelpers` verschoben (ehemals private static, jetzt public static) — sie werden jetzt von 6+ Stellen quer durch die Partials und vom neuen Service genutzt. .NET Build clean (0 Errors), 323 Tests passing (45 DB-Connection-Failures wie immer ohne laufende Postgres). | **Cross-Service-Kopplung:** `BackfillLegacyInitialTasksForSupervisorCompletion` in TaskGenerationOperations.cs ruft weiterhin `RecalculateWorkflowTaskAvailability`, `PersistTaskStatus`, `SyncPrimaryAssignmentCompletion` (alle in StatusCalculationOperations.cs als `private static`) — diese werden in 8.A3 in den `IWorkflowStatusCalculationService` wandern; danach muss `BackfillLegacyInitialTasksForSupervisorCompletion` umgestellt werden (entweder Service-Aufruf via Statics oder die Methode selbst wandert in eine Service-Klasse). **Pattern fuer 8.A3-A5 etabliert:** Interface + Implementation-Klasse mit Instance-Methoden, die an `internal static`-Methoden delegieren, sodass statische Aufrufer ohne Threading durchkommen. **Naechster Schritt:** 8.A3 = `IWorkflowStatusCalculationService` extrahieren (`RecalculateAndPersistWorkflowStatus` + 6 Sub-Helper). |
| 2026-05-01 | LEGACY-SCHRITT-8.A1 | done | `api/API/Repositories/IWorkflowAuditWriteOperations.cs` (neu), `api/API/Repositories/PostgresWorkflowAuditWriteOperations.cs` (neu), `api/API/Repositories/PostgresWorkflowRepository.cs` (Feld `_auditWrite` + neue Konstruktor-Overloads), `api/API/Extensions/LifecycleServiceCollectionExtensions.cs` (DI-Registrierung `AddScoped<IWorkflowAuditWriteOperations>`), `api/API/Repositories/PostgresWorkflowRepository.LifecycleOperations.cs` (2 Call-Sites), `api/API/Repositories/PostgresWorkflowRepository.LinkOperations.cs` (3 Call-Sites), `api/API/Repositories/PostgresWorkflowRepository.TaskOperations.cs` (3 Call-Sites), `api/API/Repositories/PostgresWorkflowRepository.WorkflowCreateOperations.cs` (3 Call-Sites), `api/API/Repositories/PostgresWorkflowRepository.AutomationOperations.cs` (1 Call-Site), `api/API/Repositories/PostgresWorkflowRepository.TaskGenerationOperations.cs` (1 Call-Site in Instance-Methode). | **Phase 8.A1:** `IWorkflowAuditWriteOperations`-Interface + `PostgresWorkflowAuditWriteOperations`-Implementierung extrahiert. Pattern etabliert: Interface, DI-Registrierung, Constructor-Injection, `_auditWrite`-Feld. Instanz-Methoden (LifecycleOperations, LinkOperations, TaskOperations, WorkflowCreateOperations, AutomationOperations, TaskGenerationOperations-Instanz) rufen jetzt `_auditWrite.InsertAuditEntry(...)` statt statischer Helper-Methode. `InsertAuditEntry` verbleibt zusaetzlich als statische Methode in `SharedHelpers` fuer die zahlreichen statischen Methoden in Runtime/Status/Task-Partials — diese werden in 8.A2-8.A5 in eigene Service-Klassen verschoben, womit die statischen Aufrufe entfallen. .NET Build clean (0 Errors), 323 Tests passing (45 DB-Connection-Failures wie immer ohne laufende Postgres). | **Dopplung bewusst:** `InsertAuditEntry` existiert jetzt sowohl in `IWorkflowAuditWriteOperations` (fuer DI) als auch als static in `SharedHelpers` (fuer statische Methoden in Runtime/Status). Diese Dopplung haelt genau so lange, bis 8.A2-8.A5 die statischen Methoden in ihre Service-Klassen verschieben. Bei jedem A2-A5-Schritt werden die statischen Aufrufe durch den jeweiligen Service-Aufruf ersetzt. |
| 2026-05-01 | LEGACY-SCHRITT-8-SESSION1 | done | `api/API/Repositories/PostgresRepositorySharedHelpers.cs` (von 290 auf 766 Zeilen ausgebaut), `api/API/Repositories/PostgresWorkflowRepository.cs` (TargetPersonRecord + TaskGenerationStage entfernt), 7 Partials geleert von verlagerten Helpers (`AuditOperations`, `AssignmentOperations`, `LifecycleOperations`, `WorkflowCreateOperations`, `LinkOperations`, `WorkflowRuntimeOperations`, `AutomationOperations`, `WorkflowDefinitionAdminOperations`), 10 Partials mit umgebogenen Aufrufen via sed (rund 50 Call-Sites) — `AutomationOperations`, `LifecycleOperations`, `LinkOperations`, `NotificationOperations`, `PersonLifecycleOperations`, `StatusCalculationOperations`, `TaskGenerationOperations`, `TaskOperations`, `WorkflowCreateOperations`, `WorkflowRuntimeOperations`. | Session 1 (Vorarbeit fuer Schritt 8): SharedHelpers-Klasse von ~10 auf ~20 Methoden ausgebaut. Verlagert: `InsertAuditEntry` (9 Aufrufer), `BuildTaskStatusAuditDetail`, `LoadPrimaryTaskAssignmentAuditLabel`, `LoadResponsibilityIdByKey`, `EnsureValidPositionRole`, `LoadTargetPerson` + `TargetPersonRecord` (Top-Level internal), `LoadStoredAnswersByKey`, `LoadWorkflowDefinitionGraph` + 4 Records (`WorkflowDefinitionGraphRecord`, `WorkflowDefinitionNodeRecord`, `WorkflowDefinitionEdgeRecord`, `WorkflowNodeActionRecord` als Top-Level internal), `ParseJsonElement`, `TaskGenerationStage` enum. Cross-Slice-Kopplung damit deutlich reduziert. .NET Build clean (API + Tests, 0 Errors), TypeScript clean. | **Phase 2d bewusst nicht gemacht**: `GenerateWorkflowTasks` (5 File-interne Sub-Helper), `RecalculateAndPersistWorkflowStatus` (~7 File-interne Sub-Helper), `CreateWorkflowNotifications` (~10 File-interne Sub-Helper) sind effektiv eigene Subsysteme. Verschiebung nur sinnvoll mit allen Sub-Helpern (~600+ Zeilen) — gehört in Slice 2-4 (Session 2/3) wo dann die Architektur-Entscheidung pro Subsystem getroffen wird. **Naechster Schritt:** Session 2 = Slice 1 WorkflowRuntime extrahieren. **Records-Approach:** TargetPersonRecord und 4 Graph-Records sind jetzt Top-Level `internal sealed class` in `PostgresRepositorySharedHelpers.cs` — leicht von neuen Slice-Repos importierbar. |
| 2026-05-01 | LEGACY-SCHRITT-7B+7C | done | **7B:** `db/02_bootstrap.sql` und `db/02_dev_seed.sql` (Responsibility id=15: `hr_onboarding` → `hr_workflow_initiator`, Label "HR-Workflow-Initiierung", neutralisierte Description), `api/API/Repositories/PostgresWorkflowRepository.NotificationOperations.cs` (Fallback-Lookup `LoadResponsibilityIdByKey(..., "hr_onboarding")` → `"hr_workflow_initiator"`). **7C:** `api/API/Authorization/AuthorizationPermissions.cs` (ungenutztes hardcoded Array `WorkflowCreatePermissions` mit 6 Permission-Strings entfernt; Kommentar bei `WorkflowCreate(workflowDefinitionKey)` aktualisiert auf "definitionsgetrieben seit 6.3d-iv"). **Doku:** `MEMORY.md` (Active Risks reduziert), `KauthWorkflow/Stand/Onboarding-Entkopplung.md` (zwei Tabellen-Zeilen auf "abgeschlossen"), `KauthWorkflow/Architektur/Migrationspfad.md` (zwei neue Abschluss-Zeilen), `TODO.md` (Schritt 7 → "done"), `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` (Slice-Eintrag). | **7B:** `hr_onboarding` als Responsibility-Key war ein Naming-Lie — die Responsibility ist faktisch ein generischer Workflow-Initiator-Slot (HR startet Workflows aller Art, nicht nur Onboarding). Umbenennung in 4 Stellen: 2 Seeds + 1 Backend-Fallback. **7C:** Permission-Schema (`workflows.create.<definition_key>`) ist seit Slice 6.3d-iv vollstaendig definitionsgetrieben; das hardcoded Array `WorkflowCreatePermissions` mit 6 fest verdrahteten Strings war ungenutzter Dead Code (kein Lese-Zugriff im gesamten Repo). .NET Build clean (0 Errors), TypeScript Build clean. | **System nicht produktiv** — Daten-Migration für `hr_onboarding` → `hr_workflow_initiator` in `app_user_responsibilities` etc. nicht nötig. **Permission-Strings im Schema unverändert** (`workflows.create.onboarding` etc. bleiben in `app_permissions` Seed wie sie sind, weil Suffix = `definition_key`). **Schritt 7 vollständig abgeschlossen.** |
| 2026-05-01 | LEGACY-SCHRITT-7A | done | Backend: `api/API/Endpoints/WorkflowMasterDataEndpoints.cs` (Endpoint `/workflows/completed-onboardings` + `ToCompletedOnboardingSearchResult`-Mapper gelöscht), `api/API/Endpoints/RotationPlanningEndpoints.cs` (Endpoint `/rotation/completed-onboardings` + Mapper gelöscht), `api/API/Contracts/WorkflowDtos.cs` (`CompletedOnboardingSearchResultDto` gelöscht). Backend-Tests: `api/API.Tests/WorkflowEndpointsTests.cs` (`WorkflowTargetPersonSourcesAlias_PassesSearchAndLimitToRepository` gelöscht — testete den entfernten Legacy-Endpoint). Frontend: `web/src/types/rotation.ts` (`CompletedOnboardingSearchResult`-Type ersetzt durch `RotationEligiblePerson = WorkflowTargetPerson` als Alias), `web/src/types/workflow.ts` (Type-Alias `CompletedOnboardingSearchResult` gelöscht), `web/src/services/api/backendDtos.ts` (`BackendCompletedOnboardingSearchResultDto` gelöscht), `web/src/services/workflowApi.ts` (`searchCompletedOnboardings`-Wrapper gelöscht — war Thin-Forward zu `searchWorkflowTargetPersonSources`), `web/src/services/rotationApi.ts` (`searchCompletedRotationOnboardings` → `searchRotationEligiblePeople`, Type-Import umgestellt), `web/src/services/queries/rotationQueries.ts` (`useRotationCompletedOnboardings` → `useRotationEligiblePeople`), `web/src/services/queryKeys.ts` (`workflows.completedOnboardings` und `rotation.completedOnboardings` Query-Keys gelöscht), `web/src/pages/RotationPlanningPage.tsx` (Imports + Variable-Types umgestellt), `web/tests/RotationPlanningPage.test.tsx` (Mocks + Helper-Funktion umgestellt). Doku: `MEMORY.md` (Preserve-Liste reduziert), `KauthWorkflow/Stand/Onboarding-Entkopplung.md` (4 Tabellen-Zeilen auf "abgeschlossen" gesetzt), `KauthWorkflow/Architektur/Migrationspfad.md` (Tabellen-Zeile aktualisiert), `CODE_REVIEW.md` (`completed-onboardings`-Eintrag durchgestrichen mit Verweis), `TODO.md` (Schritt 7 → "partial (A done)"). | Slice 7A: Legacy-Endpoints `/workflows/completed-onboardings` und `/rotation/completed-onboardings` + DTO `CompletedOnboardingSearchResultDto` komplett entfernt. Beide Endpoints waren strukturidentische Wrapper für `/workflow-target-person-sources` (existiert bereits parallel) bzw. das Frontend rief bei `/rotation/...` ohnehin schon `/people/rotation-eligible` (existiert bereits parallel). Frontend-Type `RotationEligiblePerson` ist jetzt ein klarer Alias auf `WorkflowTargetPerson`. .NET Build clean (0 Errors), TypeScript Build clean. | **Repo-Methode `GetCompletedOnboardingSource` in `IRotationRepository` + Implementation bewusst beibehalten** — die SQL filtert tatsächlich auf `wd.definition_key = 'onboarding'` und ist fachlich onboarding-spezifisch (Validierung "abgeschlossenes Onboarding existiert" für Rotation-Plan-Erstellung). **Felder `latestCompletedOnboardingWorkflowUid` und `latestCompletedOnboardingAt` bleiben** — sie zeigen das letzte abgeschlossene Onboarding einer Person, fachlich korrekt. **Schritt 7 nur teilweise abgeschlossen (A):** B (`hr_onboarding`-Responsibility umbenennen) und C (`workflows.create.onboarding`-Permission umbenennen) bewusst nicht angefasst — beide würden Daten-Migrationen in produktiven Stammdaten/User-Rechten brauchen, Pay-Off klein gegenüber Risiko. |
| 2026-05-01 | LEGACY-SCHRITT-6.3d-iv | done | `db/01_schema.sql` (process_types-Tabelle + Sequence + 4 Constraints + 2 FK gedroppt, `workflow_definitions` um `requires_target_person`/`approval_task_template_key`/CHECK ergänzt, `workflows.process_type_id`→`workflow_definition_id` mit neuem FK auf `workflow_definitions(id)`, `workflow_definition_versions.primary_legacy_process_type_id` gedroppt), `db/02_bootstrap.sql` + `db/02_dev_seed.sql` (process_types INSERT-Block raus, Sequence-Setval raus, workflow_definitions INSERT um 2 neue Spalten erweitert mit Backfill, workflow_definition_versions INSERT 7. Spalte raus), 13 Repository-Files (LifecycleOperations, LinkOperations, NotificationOperations, PersonLifecycleOperations, MasterDataOperations, ReadOperations, StatusCalculationOperations, TaskGenerationOperations, WorkflowCreateOperations, WorkflowDefinitionAdminOperations, WorkflowQueryOperations, WorkflowRuntimeOperations, PostgresRotationRepository.PlanOperations) — alle `JOIN process_types pt ON pt.id = w.process_type_id` → `JOIN workflow_definitions pt ON pt.id = w.workflow_definition_id`, `pt.key` → `pt.definition_key`, `LEFT JOIN process_types vpt ON vpt.id = v.primary_legacy_process_type_id` → `LEFT JOIN workflow_definitions vpt ON vpt.id = v.workflow_definition_id`, `vpt.key` → `vpt.definition_key`, INSERT INTO workflows Spalten-Renaming, `LoadProcessTypeForCreate`-SELECT auf workflow_definitions umgestellt, `NormalizeWorkflowDefinitionId`-Bridge entfernt, `EnsureProcessTypeExists` vereinfacht, `IsActive`-Property + `record.IsActive`-Check entfernt (nicht mehr nötig). Tests: 6 Integration-Test-Files (Concurrency, AuditLog, Automation, WorkflowDefinition, AdminConfig, Link) + WorkflowDefinitionSqlArtifactsTests.cs umgestellt: alle CREATE/INSERT/DELETE-Statements + INSERT/SELECT INTO workflow_answer_definitions / workflow_answer_derivation_rules / workflow_definitions / workflows + IF NOT EXISTS-Schemas + Cleanup-Funktionen. `LifecycleStartupValidationExtensions`: 2 SQLs umgestellt, `LegacyProcessTypeExists` queryt jetzt workflow_definitions. .NET Build clean (0 Errors), TypeScript Build clean. | **Database-Migration ist destruktiv** — bestehende `process_types`-Tabelle und `workflows.process_type_id` werden bei nächstem Schema-Recreate unwiderruflich entfernt. Lokale/Test-DBs müssen frisch aufgesetzt werden. **Audit-Code-Strings wie `"missing_supervisor_gatekeeper_primary_process_type"`, `"unknown_form_legacy_process_type"`, `"supervisor_gatekeeper_process_type_mismatch"` bewusst nicht umbenannt** — könnten in Audit-Log-Datensätzen referenziert sein. **Workflow-Tabelle ist im Seed leer** — daher kein Daten-Backfill für `workflows.workflow_definition_id` nötig. **Integration-Tests** nicht ausgeführt (keine lokale Postgres). |
| 2026-05-01 | LEGACY-SCHRITT-6.3d-iii | done | Frontend: `web/src/types/auth.ts` (`AdminProcessType`-Type entfernt), `web/src/services/adminConfigApi.ts` (`getAdminProcessTypes`/`updateAdminProcessType` entfernt), `web/src/hooks/useAdminWorkflowBuilder.ts` (alle `processTypeId` → `workflowDefinitionId`, `builder.processTypes` → `builder.definitions`), `web/src/components/admin-config/{AdminWorkflowBuilderSection,WorkflowBuilderSidebar,BuilderInspectorPanel,BuilderInspectorFocusPanel}.tsx` (alle Props/Refs umgestellt, `processTypes`-Map aus `definitions` gebaut), `web/src/hooks/{useAdminAnswerDefinitionManagement,useAdminRoleAnswerDefaults,useAdminTaskTemplateData}.ts` analog. | Naming-Lie aus 6.3d-ii bereinigt: alle Frontend-Typen/Variablen/Props heißen jetzt `workflowDefinitionId`/`workflowDefinitions`, ganzheitlich konsistent mit DB-Schema. Backend-Seite hatte schon korrekt `workflow_definition_id`. TypeScript Build clean. | Bridge `NormalizeWorkflowDefinitionId` im Backend wird in 6.3d-iv entfernt (jetzt redundant, da Frontend nur noch wd.id schickt). |
| 2026-05-01 | LEGACY-SCHRITT-6.3d-ii-REALITY-CHECK | analysis | `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` | Sub-Slice 6.3d-ii (FK-Migration der 3 Stammdaten-Tabellen) eingeordnet als Multi-Session-Refactor. Befunde: `process_types.id` ≠ `workflow_definitions.id` in Seeds (ID-Mapping nötig 1→1, 3→2, 4→3, 5→7, 6→8, 7→9), `app_role_answer_defaults` hat composite FK auf `(answer_definition_id, process_type_id)` → Lock-Step-Migration mit `workflow_answer_definitions` zuerst, 557 Code-Treffer in 50 Files (viele aber `workflows.process_type_id` = Instance-FK, nicht Stammdaten). Empfehlung: 3 Sub-Sub-Slices, jeweils eine Tabelle pro Session: 6.3d-ii-A `workflow_answer_definitions` (Pilot), 6.3d-ii-B `app_role_answer_defaults` (composite FK), 6.3d-ii-C `task_templates` (größte Frontend-Surface). Im Plan dokumentiert mit Aufwand-Realität, Reihenfolge-Constraints und was es bringen würde. | 6.3d-ii ist mehrere Stunden konzentrierter Arbeit pro Tabelle, kann nicht halb gemacht werden (sonst Tests broken). Empfehlung Modell+Effort: opus + high pro Sub-Sub-Slice (Schema+Seed+Code ohne Pause). |
| 2026-05-01 | LEGACY-SCHRITT-6.3d-ii | done | `db/01_schema.sql`, `db/02_bootstrap.sql`, `db/02_dev_seed.sql` (252 Zeilen mit ID-Mapping), `api/API/Repositories/PostgresWorkflowRepository.{MasterData,WorkflowDefinitionAdmin,AnswerDefinitionAdmin,RoleAnswerDefaultAdmin,TaskTemplateAdmin,Lifecycle,Link,Notification,Requirement,WorkflowQuery,WorkflowRuntime,TaskGeneration,Read,PersonLifecycle,StatusCalculation,WorkflowCreate}Operations.cs`, `api/API/Extensions/LifecycleStartupValidationExtensions.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` | Sub-Slice 6.3d-ii: 4 Stammdaten-Tabellen (workflow_answer_definitions, task_templates, app_role_answer_defaults, workflow_answer_derivation_rules) FK-migriert von process_types zu workflow_definitions. Schema: 4 Spalten-Renames + 5 FK-Constraints + 1 composite FK + 2 UNIQUE + 2 Indexes + 1 CHECK angepasst. Seed: 252 Tupel-Zeilen automatisiert mit ID-Mapping (1→1, 3→2, 4→3, 5→7, 6→8, 7→9) via temporärem Python-Helper migriert. Backend: 34 SQL-Reader automatisch + INSERT/UPDATE manuell + Resolver `ResolveProcessTypeId`/`ResolveWorkflowDefinitionLegacyProcessTypeId` umgestellt auf workflow_definitions + neuer Helper `NormalizeWorkflowDefinitionId` als Brücke + `EnsureProcessTypeExists` umgebaut zu Normalize+Validate (10 Aufrufer angepasst) + `LifecycleOps.CompleteSupervisorStep` und `LinkOps.LoadWorkflowLinkLookup` SQL-JOIN auf workflow_definitions ergänzt + `LinkOps.GetDerivedAnswersInternal` 4-fach-JOIN-Brücke + `NotificationOps.LoadWorkflowCreatedResponsibilityIdFromProcessTasks` umgestellt. 330/330 Backend-Unit-Tests + 135/135 Frontend-Tests + Lint clean. | **Naming Lie:** Spalten sind sauber `workflow_definition_id`; C#-Variablen, DTO-Properties und Frontend-Types heißen weiter `processTypeId`/`ProcessTypeId`. Frontend sendet weiter pt.id, Backend normalisiert. Wird in 6.3d-iii bereinigt. **Workflow-Instance-FK** `workflows.process_type_id` bleibt auf process_types — separate Migration. **Integration-Tests** nicht ausgeführt (keine lokale Postgres) — Schema-Migration first-time-validate erfolgt erst dort. **Offen:** 6.3d-iii (Admin-Endpoints + 4 Frontend-Hooks bereinigen, Naming Lie weg), 6.3d-iv (process_types-Tabelle droppen, Workflow-Instance-FK migrieren). |
| 2026-05-01 | LEGACY-SCHRITT-6.3d-i | done | `db/01_schema.sql`, `db/02_bootstrap.sql`, `db/02_dev_seed.sql`, `api/API/Repositories/PostgresWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowCreateOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionAdminOperations.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` | Sub-Slice 6.3d-i: `requires_supervisor_step` auf `workflow_definitions` migriert (analog Slice 6.2 Option A). Schema neue Spalte mit Default false NOT NULL. Seed-Backfill: Onboarding → true, alle anderen false. Repo-Helper umbenannt von `LoadLegacyProcessTypeRequiresSupervisorStep` zu `LoadDefinitionRequiresSupervisorStep`, liest jetzt aus `workflow_definitions`. Bonus-Cleanup: `LegacyManagerCreatableProcessTypeKeys`-Hardcoded-Liste + `HasProcessTypeManagerCreationColumn`-Schema-Probe entfernt (waren nach Slice 6.2 toter Code, weil die `process_types.allows_manager_creation`-Spalte garantiert existiert). 330/330 Backend-Unit-Tests grün. | Sub-Slices 6.3d-ii bis 6.3d-iv offen — jeweils Mehrtage-Refactor: FK-Migration der 3 Stammdaten-Tabellen (`workflow_answer_definitions.process_type_id` etc.), 4 Frontend-Admin-Hooks + Backend-Endpoints raus, Schema-Drop von `process_types`. Designentscheidung-Punkt vor 6.3d-ii: FK-Umhängung auf `workflow_definitions.id` (Option A, einfachste Migration) vs. Neumodell. |
| 2026-05-01 | LEGACY-SCHRITT-6.3c | done | Backend: `api/API/Contracts/WorkflowDefinitionDtos.cs`, `api/API/Contracts/WorkflowRuntimeDtos.cs`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionAdminOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowRuntimeOperations.cs`, `db/01_schema.sql` (SQL-Funktion). Frontend: `web/src/types/workflow.ts`, `web/src/types/auth.ts`, `web/src/services/adminConfigApi.ts`, `web/src/hooks/adminWorkflowBuilderModel.ts`, `web/src/hooks/useWorkflowCreation.ts`. Tests: `api/API.Tests/{AdminWorkflowDefinitionConfigEndpointsTests, AdminWorkflowRuntimeEndpointsTests, PostgresWorkflowRepositoryAutomationIntegrationTests, PostgresWorkflowRepositoryWorkflowDefinitionIntegrationTests, WorkflowEndpointsTests}.cs`, `web/tests/AdminWorkflowBuilderSection.test.tsx`. Doku: `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`. | Slice 6.3c: DTO-Felder `PrimaryLegacyProcessTypeKey` aus 5 Backend-DTOs entfernt, 5 SQL-Reader-Stellen vom `JOIN process_types`-Lookup gesäubert (Indices nachgezogen), UPDATE-Statements schreiben nicht mehr in `primary_legacy_process_type_id` (Spalte bleibt nullable bis 6.3d), `WorkflowRuntimeService` Doppel-Lookup zu Single-Lookup vereinfacht (Hilfsmethoden gelöscht), SQL-Funktion `upsert_linearized_workflow_definition` umgeschrieben (kein process_types-Argument, kein Lookup mehr), `BuildWorkflowDefinitionValidationSnapshot` vereinfacht (missing/unknown-Validation entfällt). Frontend-Types geputzt, Builder-Datenmodell behält `primaryLegacyProcessTypeKey` als internes Feld (jetzt aus `detail.definitionKey` befüllt), Save-Payload schickt es nicht mehr. ~16 Backend-Test-Fixtures + 1 Frontend-Mock + 1 Integration-Assert angepasst. 330/330 Backend-Unit-Tests + 12/12 SQL-Artefakt-Tests + 135/135 Frontend-Tests grün, Lint clean. | **Slice 6.3d offen:** `process_types`-Tabelle + FK `workflow_definition_versions.primary_legacy_process_type_id` droppen, `LoadProcessTypeForCreate` / `LoadAnswerDefinitionRecords` / `LoadRoleDefaultRecords` umbiegen (FKs an `workflow_answer_definitions.process_type_id` etc.), 4 Admin-Hooks im Frontend, `/process-types` und `/admin/config/process-types/*` Endpoints, `LifecycleStartupValidationExtensions` Validation-Block. **Designentscheidung vor 6.3d:** `requires_supervisor_step` analog zu `allows_manager_creation` (Slice 6.2) auf `workflow_definitions` migrieren — Option A vermutlich konsistent. |
| 2026-05-01 | LEGACY-SCHRITT-6.3b | done | `api/API/WorkflowDefinitionSupervisorGatekeeperRules.cs`, `api/API/Services/WorkflowDefinitionValidationService.cs`, `api/API/Extensions/LifecycleStartupValidationExtensions.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionAdminOperations.cs`, `api/API.Tests/WorkflowDefinitionValidationServiceTests.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` | Slice 6.3b: Backend-Verhaltenslogik vom `primaryLegacyProcessTypeKey` auf `workflowDefinitionKey` umgestellt. `WorkflowDefinitionSupervisorGatekeeperRules.Evaluate` Parameter umbenannt + Doku-Kommentar (Datenidentität als Übergangsbedingung). `WorkflowDefinitionValidationService`: Dictionary `ExpectedMeasureNodeTypeByProcessTypeKey` → `ExpectedMeasureNodeTypeByDefinitionKey`, Helper `GetExpectedMeasureNodeTypeForProcessTypeKey` → `GetExpectedMeasureNodeTypeForDefinitionKey`, `ValidateMeasurePhaseProcessConsistency` Parameter+Fehlertext rename, `WorkflowDefinitionValidationContext.PrimaryLegacyProcessTypeKey` → `WorkflowDefinitionKey`. 2 Production-Aufrufer (`LifecycleStartupValidationExtensions`, `WorkflowDefinitionAdminOperations`) befüllen den Context jetzt aus `definition.DefinitionKey` statt `PrimaryLegacyProcessTypeKey`-Feld — funktional unverändert (Slice-6.2-Inventur bestätigt Identität). 9 Test-Stellen in `WorkflowDefinitionValidationServiceTests.cs` umgestellt. 330/330 Backend-Unit-Tests grün. | FailureCode-Strings (`missing_supervisor_gatekeeper_primary_process_type`, `supervisor_gatekeeper_process_type_mismatch`, `measure_flow_process_type_mismatch`) bewusst nicht umbenannt — könnten in Audit-Log-Datensätzen referenziert sein. **Slice 6.3c offen:** DTO-Felder `PrimaryLegacyProcessTypeKey` auf 4+ Workflow-Definition-DTOs, 10 SQL-Reader-Stellen, Doppel-Lookup in `WorkflowRuntimeService.HasUnscopedWorkflowCreatePermission`, Frontend-Type `auth.ts` mit `primaryLegacyProcessTypeKey`, SQL-Funktion `upsert_linearized_workflow_definition`. **Slice 6.3d offen:** FK + Spalte `workflow_definition_versions.primary_legacy_process_type_id` droppen. |
| 2026-05-01 | SCHRITT-8-REALITY-CHECK + LEGACY-SCHRITT-6.3a | done | `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `web/src/components/admin-config/AdminWorkflowBuilderSection.tsx`, `web/src/components/admin-config/BuilderInspectorPanel.tsx` | **Schritt 8 Reality-Check:** Detaillierter Code-Scan vor Slice-1-Start ergab, dass die Plan-Annahme "1–2 Tage pro Slice, läuft parallel" so nicht stimmt. Befunde dokumentiert in `Legacy-Abbau-Plan.md`: `WorkflowRuntimeOperations.cs` (3030 LoC) ist *der* Workflow-Kern, ruft dutzende Helper aus 5+ anderen Partials; `InsertAuditEntry` aus 8 Files inline transaktional gerufen — Audit/Notification sind keine echten Subsysteme. Echter Schritt 8 brauchte 3–5 Tage Vorarbeit für `PostgresRepositorySharedHelpers`-Ausbau, dann 2–3 Tage pro Slice → ~10 Tage statt geplante 4. Empfehlung: Schritt 8 erst angehen wenn operativer Druck (Build-Zeit, Test-Zeit, Merge-Konflikte) entsteht. **Slice 6.3a (statt Schritt 8):** Frontend-Builder-UI: Eingabefeld für `versionDraft.primaryLegacyProcessTypeKey` in `AdminWorkflowBuilderSection.tsx` entfernt; "Prozessgrundlage"-Override-Dropdown am form-Node in `BuilderInspectorPanel.tsx` entfernt + ungenutzte `processTypeOptions`-Variable weg. Datenmodell unverändert (Wert kommt vom Backend, geht zurück). Bestehende Override-Werte in `workflow_node_configs` bleiben Read-aktiv (`resolveEffectiveProcessTypeKey` bleibt). 135/135 Web-Tests grün, Build clean, Lint clean. | **Verhaltensänderung am form-Node:** Override-Editing entfernt — bestehende Override-Werte bleiben wirksam aber nicht mehr admin-editierbar. Falls produktive Override-Datensätze existieren, vor Slice 6.3b/c Inventur-SQL fahren. **Schritt 8 als "Cold Storage":** Im Plan dokumentiert dass tatsächlicher Aufwand ~10 statt 4 Tage wäre und der Pay-Off architektonisch (nicht operativ) ist. |
| 2026-04-30 | LEGACY-SCHRITT-6.2 | done | `db/01_schema.sql`, `db/02_bootstrap.sql`, `db/02_dev_seed.sql`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` | Slice 6.2 von Schritt 6: Manager-Creation-Flag auf `workflow_definitions` migriert (Option A nach User-Entscheidung). Schema neue Spalte `workflow_definitions.allows_manager_creation boolean DEFAULT false NOT NULL`. Seed-Backfill aus den `process_types`-Werten (4 änderbare → true, on/offboarding → false) in beiden Seed-Files. Repo-Methode `IsManagerCreatableProcessType(processTypeKey)` umbenannt zu `IsManagerCreatableDefinition(workflowDefinitionKey)`, liest jetzt direkt aus `workflow_definitions.allows_manager_creation` (kein Fallback nötig). Beide Service-Aufrufer (`WorkflowRuntimeService`, `WorkflowCatalogService`) übergeben jetzt `selectedDefinition.DefinitionKey` statt `PrimaryLegacyProcessTypeKey`. Test-Fakes in 2 Files angepasst (Property/Method-Renames). 330/330 Backend-Unit-Tests + 12/12 SQL-Artefakt-Tests grün. | `process_types.allows_manager_creation` bleibt als Datenduplikat bis Slice 6.4 (wird noch von `/admin/config/process-types`-Admin-UI gelesen). `LegacyManagerCreatableProcessTypeKeys`-Hardcoded-Liste bleibt (genutzt von `GetActiveProcessTypes` für `/process-types`). Falls jemand das Manager-Flag im Admin-UI ändert, divergieren `process_types` und `workflow_definitions` bis Slice 6.4 — aktuell editiert aber kein UI das Flag, nur Seed setzt es; kein akutes Risiko. |
| 2026-04-30 | LEGACY-SCHRITT-6.1 | done | `web/src/services/queries/processTypeQueries.ts` (gelöscht), `web/src/services/queries/workflowDefinitionQueries.ts` (neu), `web/src/services/lookupApi.ts`, `web/src/services/queryKeys.ts`, `web/tests/lookupApi.test.ts`, `web/src/components/dashboard/DashboardOverview.tsx`, `web/src/hooks/useWorkflowCreation.ts`, `web/src/pages/workflowListPageModel.ts`, `web/src/pages/WorkflowSearchPage.tsx` | Slice 6.1 von Schritt 6: Frontend-Lookup-Stack für Process-Types entfernt. Inventur ergab: `useProcessTypes` (in `processTypeQueries.ts`) hatte nach Schritt 4 keinen Konsumenten mehr; `getProcessTypes()`-Service nur noch von dem toten Hook + dessen Test referenziert. Aktion: `processTypeQueries.ts` aufgelöst; `useStartableWorkflowDefinitions` in neue Datei `workflowDefinitionQueries.ts` verschoben (4 Importe migriert); `getProcessTypes` aus `lookupApi.ts` raus, ungenutzten `BackendProcessTypeDto`-Import dort weg; `queryKeys.processTypes()` entfernt; verwaisten `getProcessTypes`-Test in `lookupApi.test.ts` gelöscht. 135/135 Web-Tests grün, Build clean, Lint clean. | Die 4 Admin-Hooks (`useAdminAnswerDefinitionManagement`, `useAdminRoleAnswerDefaults`, `useAdminTaskTemplateData`, `useAdminWorkflowBuilder`) nutzen weiter `getAdminProcessTypes()` — gehören zu Slice 6.4, weil sie Stammdaten verwalten, die per FK an `process_types` hängen (`workflow_answer_definitions.process_type_id` etc.). `ProcessType`-Typ + `BackendProcessTypeDto` bleiben — werden noch als DTO-Property auf `WorkflowSummary`, `LinkedWorkflow` u.a. geliefert. Backend-Endpunkt `/process-types` und Service `WorkflowCatalogService.GetProcessTypesAsync` bleiben (Slice 6.4). |
| 2026-04-30 | LEGACY-SCHRITT-5 | done | `api/API/Authorization/AuthorizationPermissions.cs`, `api/API/Authorization/IAuthorizationPolicyService.cs`, `api/API/Authorization/AuthorizationPolicyService.cs`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API.Tests/AuthorizationPolicyServiceTests.cs`, `KauthWorkflow/Architektur/Migrationspfad.md`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `TODO.md` | Permission-System auf `definition_key` umgestellt. Inventur ergab: alle `workflow_definitions.definition_key`-Werte sind 1:1 identisch mit den `process_types.key`-Werten und mit den Suffixen in `app_permissions` (`workflows.create.<key>`). → **Keine DB-Migration nötig.** Code-Umstellung: `AuthorizationPermissions.WorkflowCreate(processTypeKey)` Parameter zu `workflowDefinitionKey` umbenannt + XML-Kommentar; `IAuthorizationPolicyService.CanCreateWorkflowForProcessType` → `CanCreateWorkflowForDefinition` (auch Implementation + 3 Tests in `AuthorizationPolicyServiceTests.cs` auf `managerCreatableDefinition`-Parameter umgestellt). `WorkflowRuntimeService.HasUnscopedWorkflowCreatePermission` / `HasScopedWorkflowCreatePermission` Doppel-Lookup (Definition+Legacy) bleibt mit Kommentar als Brücke bis Schritt 6. 330/330 Backend-Unit-Tests + 110 AuthorizationPolicy-Tests grün. | `IsManagerCreatableProcessType` liest weiter aus `process_types.allows_manager_creation` (Schritt 6). Doppel-Lookup gegen `legacyProcessTypeKey` in `WorkflowRuntimeService` ist heute redundant (Daten identisch), bleibt aber als Schutz für künftige Definitionen mit abweichendem Key bis Schritt 6 die `process_types`-Tabelle entfernt. Keine Permission-Strings geändert → MEMORY.md Preserve-Liste (`workflows.create.onboarding`) weiterhin korrekt. |
| 2026-04-30 | LEGACY-SCHRITT-4 | done | `web/src/services/workflowApi.ts`, `web/src/services/queryKeys.ts`, `web/src/pages/workflowListPageModel.ts`, `web/src/pages/WorkflowListFilters.tsx`, `web/src/pages/WorkflowListPage.tsx`, `web/src/pages/WorkflowSearchPage.tsx`, `web/src/components/dashboard/DashboardOverview.tsx`, `web/src/components/dashboard/dashboardInsights.shared.ts`, `web/src/components/dashboard/dashboardInsightsLoaders.ts`, `web/src/components/dashboard/dashboardQueries.ts`, `web/src/components/dashboard/useDashboardInsights.ts`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowQueryOperations.cs`, `api/API/Services/IWorkflowRuntimeService.cs`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API/Endpoints/WorkflowEndpoints.cs` | Filter-UIs (Workflow-Liste, Suche, Dashboard) auf `workflowDefinitionKey` umgestellt. Backend: `WorkflowListQuery.WorkflowDefinitionKey` neu, SQL-Filter via EXISTS-Subquery auf `workflow_definition_versions`/`workflow_definitions`. Frontend: `useProcessTypes()` → `useStartableWorkflowDefinitions()` in allen drei Filter-UIs, `DashboardInsightsOptions` von `processTypeKey`/`selectedProcessType` auf `workflowDefinitionKey`/`selectedWorkflowDefinition` (Typ `StartableWorkflowDefinition`). `processTypeKey` als Query-Param bleibt am Backend noch erhalten (kein Breaking Change). 332/332 Backend- + 136/136 Frontend-Tests grün. | `processTypeKey`-Filterparameter bleibt noch am Backend-Endpoint verfügbar, wird aber von keiner Frontend-UI mehr gesendet. Kann in Schritt 6 gemeinsam mit der `process_types`-Tabelle entfernt werden. Dashboard-Manager-Filter nutzt `workflow.processType.key === workflowDefinitionKey` als Proxy — funktioniert solange Keys identisch sind (bis Schritt 6). |
| 2026-04-30 | LEGACY-SCHRITT-3 | done | `web/src/types/workflow.ts`, `web/src/hooks/workflowCreationModel.ts`, `web/src/hooks/useWorkflowCreationSubmission.ts`, `web/src/hooks/useWorkflowCreation.ts`, `web/tests/CreateWorkflowPage.test.tsx`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API.Tests/WorkflowEndpointsTests.cs` | `processTypeKey` aus Workflow-Create-Pfad vollständig entfernt. Frontend sendet nur noch `workflowDefinitionKey`. Backend-Legacy-Branch (process_type-Lookup + `repository.CreateWorkflow`) entfernt — Service wirft jetzt `InvalidOperationException` wenn keine startbare Definition gefunden. `ResolveRequestedWorkflowDefinition` kennt nur noch `WorkflowDefinitionKey`. 4 Backend-Tests und 2 Frontend-Tests auf neuen Pfad umgestellt. 319/319 Backend- + 136/136 Frontend-Tests grün. | `ProcessTypeKey`-Property bleibt noch in `CreateWorkflowRequest` DTO (kein Breaking Change nach außen). Kann in Schritt 6 mit dem Process-Types-Endpunkt gemeinsam entfernt werden. |
| 2026-04-30 | LEGACY-SCHRITT-2 | done | `web/src/hooks/adminWorkflowBuilderModel.ts`, `web/src/components/admin-config/AdminWorkflowBuilderSection.tsx`, `web/src/components/admin-config/BuilderInspectorPanel.tsx`, `web/src/components/admin-config/workflowBuilderLabels.ts`, `web/src/components/admin-config/workflowBuilderStructuredLayout.ts`, `api/API/Services/WorkflowDefinitionValidationService.cs` | `"setup"` Node-Type aus dem Frontend-Union entfernt. `LEGACY_SETUP_TITLES` + `normalizeWorkflowLegacyStatus`-Parallele entfernt. `normalizeLifecycleMeasureNodeType` konvertiert jetzt `"setup"` immer zu `measure_provision` (Fallback) oder dem prozess-spezifischen Typ. Titel bei `setup`-Nodes werden beim Laden immer durch den Default-Titel des neuen Typs ersetzt. Backend: `"setup"` bleibt in `ValidNodeTypes` + `MeasureGenerationNodeTypes` + Runtime-Handler für bestehende Definitionen und laufende Instanzen. Validierungs-Fehlermeldung nennt `setup` nicht mehr als empfohlene Option. 136/136 Web-Tests grün. | Prod-Inventur (`SELECT` auf `workflow_nodes` nach `node_type = 'setup'`) sollte vor dem Deploy bestätigen, dass keine published Definitions noch `setup`-Nodes haben — Backend akzeptiert sie weiterhin, aber der Builder würde sie automatisch zu `measure_*` konvertieren. Backend-Cleanup (`"setup"` aus ValidNodeTypes + Runtime) erst nach Inventur sinnvoll. |
| 2026-04-30 | LEGACY-SCHRITT-1 | done | `web/src/utils/workflowStatus.ts`, `web/src/services/api/mappers.ts`, `web/src/services/api/backendDtos.ts`, `api/API/Contracts/WorkflowDtos.cs`, `api/API/Repositories/PostgresWorkflowRepository.ReadOperations.cs`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API/Services/WorkflowVisibilityService.cs`, `api/API.Tests/AuthorizationPolicyServiceTests.cs` | `WorkflowLegacyStatus` aus `TaskWorkflowContextDto` entfernt (Backend + Frontend). Mapper-Funktionen `toWorkflowLegacyStatus` / `normalizeWorkflowLegacyStatus` in `workflowStatus.ts` entfernt. Frontend leitet `status` ("open"/"completed") jetzt aus `workflowStatus` ab statt aus dem Legacy-Feld. Backend `ToLegacyStatus()` bleibt für `Status`-Feld in anderen DTOs (`WorkflowListItemDto` etc.) — das ist Schritt 4 Vorarbeit. 136/136 Web-Tests, 319/319 Backend-Unit-Tests grün. | `ToLegacyStatus()` und `Status`-Feld auf Nicht-TaskContext-DTOs bleiben bis Schritt 3/4. |
| 2026-04-30 | LEGACY-ABBAU-PLAN | done | `KauthWorkflow/Stand/Legacy-Abbau-Plan.md` (neu), `KauthWorkflow/Architektur/Zielarchitektur.md`, `KauthWorkflow/Architektur/Migrationspfad.md`, `KauthWorkflow/00 Start.md`, `TODO.md` | 8-Schritte-Roadmap zum vollstaendigen Legacy-Abbau angelegt (Status-Feld, `setup`-Node, `processTypeKey`, Filter-UIs, Permissions, `process_types`-Tabelle, Onboarding-Vertragsnamen, Repository-Schnitte). Pro Schritt: Effort + Modell-Empfehlung + Aufwand + Risiko + Voraussetzungen. Doku-Inkonsistenz `setup`-Node in Zielarchitektur korrigiert (`setup` aus dem Soll-Set entfernt, `maßnahmen-block` als korrekter Begriff). Migrationspfad-Tabelle um sechs weitere Legacy/Neu-Paare erweitert. TODO.md hat aktiven Zyklus mit den 8 Schritten. | Plan ist Vorschlag — Schritte 5–7 brauchen Produkt- bzw. Stakeholder-Entscheidungen vor Umsetzungsbeginn. Schritte 1–4 sind freigegeben, sobald jemand sie startet. |
| 2026-04-30 | CLEANUP-TIER-BC | done | Frontend: `web/src/services/api/client.ts`, `web/src/services/{adminApi,adminConfigApi,authApi,peopleApi,rotationApi,taskApi,workflowApi}.ts`, `web/src/types/auth.ts`, `web/src/services/api/backendDtos.ts`, `web/src/hooks/{useAdminWorkflowBuilder,useAdminAnswerDefinitionManagement,useAdminRoleAnswerDefaults,adminWorkflowBuilderModel}.ts`, `web/src/components/admin-config/BuilderInspectorFocusPanel.tsx`, `web/src/auth/IdentityProvider.ts`. Backend: `api/API/Services/{ConfigurationValidationResult,GraphApplicationConfigurationValidator,NotificationEmailConfigurationValidator,GraphApplicationRuntimeConfiguration,NotificationEmailRuntimeConfiguration,GraphWorkflowEmailNotificationSender,NotificationEmailConfigurationService}.cs`. | Folge-Cleanup nach CLEANUP-TIER-A: B3 (client.ts JSON-Parse-Swallow durch echtes Logging ersetzt), B2 (config/inputMapping/parameterSchema von `unknown` auf `Record<string, unknown>` verschärft, `parseOptionalJsonObject` validiert jetzt Objekt-Shape), C6 (redundanter Inner-Catch in `useAdminWorkflowBuilder` entfernt), C2 (`identityProvider`-Singleton hinter Lazy-Proxy gelegt — bricht Modul-Init-Reihenfolge zwischen `IdentityProvider` ↔ `EntraIdentityProvider` ↔ `client.ts`), C3 (drei Silent `.catch(() => [])`-Fallbacks in Admin-Hooks reichen Fehler jetzt an `onError` weiter), B1 (neuer `encodeId(id)`-Helper in `client.ts`, ersetzt 50 `encodeURIComponent(String(...))`-Stellen in 6 Service-Dateien), C1 (gemeinsame `ConfigurationValidationResult`-Klasse + `ConfigurationValidationStatus`-Konstanten ersetzen `GraphApplicationConfigurationValidationResult`/`NotificationEmailConfigurationValidationResult`; `CanSend`/`IsConfigured` zusammengeführt zu `IsReady`-Property). Web: Build/Tests/Lint grün (136/136). Backend: Build 0 Warning/0 Error, Unit-Tests 240/240 grün; DB- und SQL-Artefakt-Tests scheitern wie gewohnt mangels lokaler Postgres bzw. wegen archivierter Migrationsdateien (bekannte Active Risks). | Aufgeschoben: Tier-C `OperationState`-Konsolidierung (drei Hooks haben absichtlich abweichende Shapes — generische Factory wäre premature abstraction); HIGH-6 (`RotationTaskRegenerationEngine` aus Repository extrahieren) bleibt offen wie in `CODE_REVIEW.md` Abschnitt 6. |
| 2026-04-30 | CLEANUP-TIER-A | done | `web/src/hooks/useRows.ts` (gelöscht), `web/src/pages/myTasksPageModel.ts`, `web/src/services/{authApi,adminApi,adminConfigApi,rotationApi,workflowApi}.ts` | 7-Track-Cleanup-Analyse durchgeführt; Tier-A-Fixes: ungenutzten `useRows`-Hook entfernt, eine narrative AI-Slop-Kommentarzeile entfernt, 16 void-API-Aufrufe von `requestJson<unknown>` auf `requestJson<void>` verschärft. Build/Tests/Lint im Web grün. | — siehe CLEANUP-TIER-BC für Folge-Arbeit. |
| 2026-04-27 | QA-CONSOLIDATED-DB-GATES | done | `.github/workflows/quality-gates.yml`, `api/API.Tests/*`, `KauthWorkflow/Domäne/Workflow.md`, `KauthWorkflow/Domäne/Rotation.md`, `KauthWorkflow/Domäne/Automation.md`, `KauthWorkflow/Domäne/Identity.md` | Quality-Gates und Backend-Tests auf den konsolidierten DB-Stand (`01_schema.sql`, `02_bootstrap.sql`, `02_dev_seed.sql`) umgestellt, alte Einzelmigrations-Referenzen aus Tests/CI entfernt und das Integrationstest-Fixture auf sauberes DB-Recreate plus minimalen Test-Support-Seed für `IT`/`position_developer`/Abteilungsleitung gehoben. | Drei Nullable-Warnungen in `PostgresWorkflowRepositoryConcurrencyTests.cs` bleiben bestehen; sie blockieren die Gates aktuell nicht. |
| 2026-04-24 | OPS-VM-PROCESS-CLEANUP | done | `scripts/start-vm.sh`, `KauthWorkflow/Betrieb/Setup.md` | Dev-Stop bereinigt jetzt auch Child-Prozesse und haengengebliebene Port-Binder auf `5001` und `5173`, damit Vite/API-Neustarts auf der VM nicht an alten npm/node-Prozessen haengen bleiben. | Wenn Fremdprozesse bewusst dieselben Ports nutzen, werden sie beim Dev-Stop ebenfalls beendet. |
| 2026-04-24 | OPS-VM-HEALTH-GUARD | done | `scripts/start-vm.sh`, `KauthWorkflow/Betrieb/Setup.md` | Dev-Startscript wartet jetzt auf API-`/health/ready` und erreichbares Web, damit abgestuerzte Hintergrundprozesse nicht mehr faelschlich als erfolgreicher Start gemeldet werden. | Wenn die API inhaltlich fehlschlaegt, muss der konkrete Grund weiter aus `.tmp-vm-dev/api.log` gelesen werden. |
| 2026-04-24 | OPS-VM-NODE-GUARD | done | `scripts/start-vm.sh`, `web/package.json`, `KauthWorkflow/Betrieb/Setup.md` | Node-Version-Guard fuer den Linux-Dev-Start eingebaut und Frontend auf `node >= 20.19.0` dokumentiert, damit Vite-Starts auf alten VM-Setups frueh und klar scheitern. | Auf bestehenden VMs mit Node 18 muss Node vor dem naechsten Dev-Start manuell aktualisiert werden. |
| 2026-04-24 | OPS-VM-START | done | `.gitignore`, `scripts/start-vm.sh`, `PROJECT_STRUCTURE.md`, `KauthWorkflow/Betrieb/Setup.md`, `KauthWorkflow/Betrieb/Deployment-Checkliste.md` | Linux-VM-Startscript fuer `dev` und `prod` inkl. `status/logs/stop/restart` hinzugefuegt und Betriebsdoku auf den neuen Startpfad umgestellt. | `dev` auf der VM setzt neben Docker auch `dotnet` und `npm` voraus; bei externer Nutzung sollte `DEV_PUBLIC_BASE_URL` bewusst gesetzt werden. |
| — | — | — | — | Kein aktiver Zyklus — Review 2026-04-23 vollstaendig abgeschlossen (COD-1..6, CLA-1..4) | — |

---

## Hinweis fuer Codex

Nach jeder Aufgabe:
1. Eintrag in die Tabelle oben einfuegen.
2. Status in `TODO.md` auf `[done]` setzen.
3. Wenn sich durch die Aenderung Annahmen aus `CODE_REVIEW.md` aendern, dort den betroffenen Abschnitt aktualisieren.

## Hinweis fuer Claude

Am Sitzungsanfang:
1. `CODEX_SYNC.md` lesen.
2. Pruefen ob neue `PARTIAL`- oder `done`-Eintraege vorliegen, die CLA-*-Aufgaben beeinflussen.
3. Wenn ja: betroffene Dateien lesen bevor eigene Arbeit beginnt.
