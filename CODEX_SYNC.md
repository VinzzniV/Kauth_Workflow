# CODEX_SYNC.md

## Zweck

- aktiver Handoff zwischen Codex und Claude
- kurze Uebersicht ueber juengste, fuer die naechste Session relevante Aenderungen
- kein Ersatz fuer `git log`

## Primaerquelle fuer

- juengste relevante Repo-Aenderungen zwischen Claude und Codex
- Doku- oder Prozessaenderungen, die die naechste Session direkt beeinflussen

## Nicht verwenden fuer

- vollstaendige Projekt-Historie
- detaillierte abgeschlossene Review-Archivierung

Dafuer sind `git log`, `CODEX_SYNC_ARCHIVE.md` und `CODE_REVIEW_ARCHIVE.md` da.

## Wann aktualisieren

- nach jeder abgeschlossenen Aufgabe
- wenn sich durch eine Aufgabe der naechste Arbeitskontext fuer die andere KI aendert

## Verwandte Dateien

- `MEMORY.md`
- `TODO.md`
- `CLAUDE_CONTROL.md`
- `CODEX_SYNC_ARCHIVE.md`

---

## Regeln

- Eintrag pro abgeschlossener Aufgabe, nicht pro Commit.
- Kurz und arbeitsrelevant schreiben.
- Sobald Eintraege fuer die naechste Session nicht mehr wichtig sind, nach `CODEX_SYNC_ARCHIVE.md` verschieben.

---

## Aktiver Sync-Log

| Datum | Aufgabe | Status | Betroffene Dateien | Zusammenfassung | Offene Risiken |
| --- | --- | --- | --- | --- | --- |
| 2026-05-11 | CLAUDE-LEGACY-COMPLETEONBOARDING-SLICE (CompletedOnboarding* → SourceWorkflow) | done | `api/API/Repositories/IRotationRepository.cs`, `api/API/Repositories/PostgresRotationRepository.PlanOperations.cs`, `api/API/Services/RotationPlanningService.cs`, `api/API/Contracts/WorkflowDtos.cs`, `api/API/Repositories/PostgresWorkflowRepository.ReadOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.PersonLifecycleOperations.cs`, `api/API.Tests/RotationPlanningServiceTests.cs`, `api/API.Tests/RotationTemplateAdminServiceTests.cs`, `api/API.Tests/RotationNotificationServiceTests.cs`, `web/src/types/workflow.ts`, `web/src/services/api/backendDtos.ts`, `web/src/services/api/mappers.ts`, `web/src/components/workflows/TargetPersonSelection.tsx`, `web/src/pages/CreateWorkflowPageSections.tsx`, `web/src/pages/PersonWorkflowHistoryPage.tsx`, `web/src/pages/RotationPlanningPage.tsx`, `web/tests/CreateWorkflowPage.test.tsx`, `web/tests/PersonWorkflowHistoryPage.test.tsx`, `web/tests/RotationPlanningPage.test.tsx`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `KauthWorkflow/Stand/Onboarding-Entkopplung.md`, `KauthWorkflow/Architektur/Migrationspfad.md`, `CODEX_SYNC.md` | Reiner Naming-Slice ohne Semantik-Änderung. `CompletedOnboarding*`-Fachbegriff vollständig auf `SourceWorkflow`-Terminologie umgestellt. BE: `IRotationRepository.GetCompletedOnboardingSource` → `GetSourceWorkflow`; DTO-Properties `LatestCompletedOnboardingWorkflowUid`/`LatestCompletedOnboardingAt` → `LatestSourceWorkflowUid`/`LatestSourceWorkflowCompletedAt` (in `WorkflowTargetPersonDto` und `PersonWorkflowHistoryDto`); SQL-CTEs `latest_completed_onboarding` → `latest_source_workflow` in 3 Repository-Partials; Fehlermeldungen in `RotationPlanningService` entlegacyt; Repository-Parameter `requireCompletedOnboarding` → `requireSourceWorkflow`. FE: `WorkflowTargetPerson.latestSourceWorkflowUid`/`latestSourceWorkflowCompletedAt`; Backend-DTO und Mapper synchron; UI-Labels in 4 Komponenten/Seiten auf „Letzter Quell-Workflow" umgestellt; RotationPlanningPage-Fehlermeldungen entlegacyt. Test-Fixtures (BE + FE) aktualisiert. Grepped: 0 Treffer für `CompletedOnboarding|completedOnboarding` in `.cs`/`.ts`/`.tsx`. Verifikation: `dotnet build api/API/API.csproj` → 0 Fehler, 0 Warnungen; `npm run build` → ✓ built; FE-Tests 12/12 grün. | 7 vorbestehende Compile-Fehler in `API.Tests` (Stub-Drift `IWorkflowCatalogService.GetPeopleDirectoryAsync`, `GetPeopleDirectory`-Signaturen in mehreren Stubs) blockieren Unit-Test-Lauf — per `git stash`-Baseline als vor diesem Slice existierend bestätigt. Keine Slice-Regression. `legacyProcessTypeKey`-Fallback-Brücke (Builder/Runtime) bewusst unberührt. |
| 2026-05-11 | CLAUDE-LEGACY-INTERNAL-NAMING-SLICE (processType* intern → workflowDefinition*) | done | `api/API/WorkflowStatusRules.cs`, `api/API/Repositories/PostgresWorkflowStatusCalculationService.cs`, `api/API/Repositories/PostgresRepositorySharedHelpers.cs`, `api/API/Repositories/PostgresWorkflowTaskGenerationService.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowQueryOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowCreateOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.cs`, `api/API/Services/NotificationEmailTemplateBuilder.cs`, `api/API/Services/NotificationTemplateService.cs`, `api/API/Services/WorkflowLifecycleService.cs`, `api/API.Tests/NotificationEmailTemplateBuilderTests.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `KauthWorkflow/Architektur/Migrationspfad.md`, `CODEX_SYNC.md` | Reiner Naming-Slice ohne Semantik-Änderung. Bereinigt die verbliebenen internen `processType*`-Namen in den Backend-Workflow-Kernpfaden: `WorkflowStatusRules`-Parameter und Fehlermeldungen (`processTypeName` → `workflowDefinitionName`); `PostgresWorkflowStatusCalculationService` lokale Variable; `WorkflowTaskGenerationContext.ProcessTypeName` → `WorkflowDefinitionName` (SharedHelpers + TaskGenerationService); `WorkflowQueryOperations` anonyme Tuple-Felder und `FilteredWorkflowRow`-Record (`LegacyProcessTypeKey`/`ProcessTypeName`/`ProcessTypeRequiresTargetPerson` → `WorkflowDefinitionKey`/`WorkflowDefinitionName`/`WorkflowDefinitionRequiresTargetPerson`); `ProcessTypeCreateRecord` → `WorkflowDefinitionCreateRecord` + `LoadProcessTypeForCreate` → `LoadWorkflowDefinitionForCreate` (CreateOperations + Caller WorkflowLifecycleService); `NotificationEmailTemplateBuilder`: `ProcessTypeEmailContext` → `WorkflowDefinitionEmailContext`, `ResolveProcessTypeEmailContext` → `ResolveWorkflowDefinitionEmailContext` (+ Caller NotificationTemplateService + Test). Alle deutschen Fehlermeldungen von „Prozesstyp" auf „Workflow-Definition" umgestellt. Build: `dotnet build api/API/API.csproj` → 0 Fehler, 0 Warnungen. Tests: 490 bestanden, 3 vorbestehende Fehlschläge (verifiziert per `git stash`-Baseline), 1 übersprungen. | 3 vorbestehende Integrationstests in `PostgresWorkflowRepositoryWorkflowDefinitionIntegrationTests` schlagen unabhängig von diesem Slice fehl — kein Regressionsrisiko. Verbleibende Legacy-Benennung: `TemporaryProcessType`-Hilfsklassen in Integrationstests (bewusst nicht angetastet — Test-only, kein Bezug zu umbenannten C#-Typen) und `legacyStatus`-SQL-Parameter in `WorkflowLifecycleService`/`PostgresWorkflowRuntimeRepository` (eigenständiger Folgeschnitt). |
| 2026-05-11 | CLAUDE-LEGACY-DOKU-KONSISTENZ (Entscheidungen.md) | done | `KauthWorkflow/Architektur/Entscheidungen.md`, `CODEX_SYNC.md` | Rein dokumentarischer Konsistenz-Fix nach CLAUDE-LEGACY-NOTIFICATION-SLICE. Abschnitt „LegacyProcessTypeKey ist der kanonische Name" in `Entscheidungen.md` korrigiert: `WorkflowDefinitionKey` ist jetzt korrekt als aktiver kanonischer Anker beschrieben; `legacyProcessTypeKey` als read-only-Fallback für Altbestand klar abgegrenzt. Historische Umbenennung (HQ1-Z4, 2026-05-02) bleibt als Hintergrundinfo erhalten. Kein Code-Change. | Kein offener Risikopfad. |
| 2026-05-11 | CLAUDE-LEGACY-NOTIFICATION-SLICE (LegacyProcessTypeKey/ProcessTypeName in Notification-/Link-DTOs) | done | `api/API/Contracts/WorkflowDtos.cs`, `api/API/Services/INotificationTemplateService.cs`, `api/API/Services/NotificationTemplateService.cs`, `api/API/Services/GraphWorkflowEmailNotificationSender.cs`, `api/API/Repositories/PostgresWorkflowNotificationDispatchOperations.cs`, `api/API/Repositories/PostgresWorkflowNotificationReadRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.NotificationOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.LinkOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowCreateOperations.cs`, `KauthWorkflow/Architektur/Migrationspfad.md`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `CODEX_SYNC.md` | Reiner Naming-Slice ohne Semantik-Änderung. `WorkflowNotificationDispatchTarget.LegacyProcessTypeKey` → `WorkflowDefinitionKey`, `ProcessTypeName` → `WorkflowDefinitionName`. Gleiche Umbenennung in `WorkflowNotificationRenderContext` (Interface + Service) und `WorkflowLinkLookupRecord` (file-private Record in LinkOperations). Alle 8 Consumer-Dateien (Service, Sender, 3 Dispatch/Read/Notification-Repos, CreateOperations) synchron nachgezogen. Bewusst nicht geändert: `WorkflowTaskGenerationContext.ProcessTypeName` (SharedHelpers), `WorkflowQueryOperations`-Tupel-Felder (breite processType*-Umbenennung). Verifikation: `dotnet build api/API/API.csproj` → 0 Fehler, 0 Warnungen. | Kein offener Risikopfad. Einzig verbleibender Legacy-Rest in diesem Cluster: `WorkflowQueryOperations`-Tupel (`LegacyProcessTypeKey`/`ProcessTypeName` als anonymous-tuple-Felder) und `WorkflowTaskGenerationContext.ProcessTypeName` — diese sind bewusst deferred, da sie breitere processType*-Umbenennung erfordern würden. |
| 2026-05-11 | CLAUDE-LEGACY-KEY-SLICE-3 (Naming-Folge-Slice PrimaryLegacyProcessTypeKey) | done | `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionAdminOperations.cs`, `KauthWorkflow/Architektur/Migrationspfad.md`, `KauthWorkflow/Architektur/Schritt7-Runtime-TaskSystem-Skizze.md`, `KauthWorkflow/Architektur/WorkflowBuilder-FormEditor-Skizze.md`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `CODEX_SYNC.md` | Reiner Naming-/Doku-Folgeslice nach CLAUDE-LEGACY-KEY-SLICE-2. Parameter `primaryLegacyProcessTypeKey` in `ResolveWorkflowDefinitionLegacyProcessTypeId` → `workflowDefinitionKey`; Fehlermeldungen von „legacy process type" → „workflow definition" entlegacyt. Skizzen-Doku: `PrimaryLegacyProcessTypeKey` in `WorkflowContextRecord`-Beschreibung (Schritt7-Skizze) → `WorkflowDefinitionKey`; `primaryLegacyProcessTypeKey` in Builder-FormEditor-Skizze → `workflowDefinitionKey`. Abbau-Plan: Restinventur-Zeile + Abarbeitungsschritt 5 als erledigt markiert; Einleitungstext entlegacyt. Migrationspfad: Zeile für `PrimaryLegacyProcessTypeKey` auf ✓ erledigt gesetzt. Keine Semantikänderung, kein Behavior-Change. Verifikation: `dotnet build api/API/API.csproj` — 0 Fehler. | Kein offener Risikopfad. `PrimaryLegacyProcessTypeKey`-Cluster vollständig abgebaut. Einziger verbleibender Legacy-Key-Rest: `legacyProcessTypeKey` als read-only-Fallback in Builder/Runtime für persistierten Altbestand — bewusst behalten, kein Handlungsbedarf. |
| 2026-05-11 | CLAUDE-LEGACY-KEY-SLICE-2 (node.config Key-Cut) | done | `api/API/Services/WorkflowRuntimeEngine.cs`, `api/API/Services/WorkflowDefinitionValidationHelpers.cs`, `api/API/Services/WorkflowDefinitionSnapshotValidator.cs`, `api/API/Services/WorkflowDefinitionDraftValidator.cs`, `api/API/WorkflowDefinitionSupervisorGatekeeperRules.cs`, `api/API/Extensions/LifecycleStartupValidationExtensions.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionGraphMappingOperations.cs`, `api/API/Repositories/PostgresWorkflowRuntimeRepository.cs`, `web/src/hooks/useAdminWorkflowVersionReferenceData.ts`, `web/src/hooks/useAdminWorkflowBuilder.ts`, `web/src/components/admin-config/WorkflowBuilderStepCard.tsx`, `web/tests/adminWorkflowBuilderModel.test.ts`, `db/02_dev_seed.sql`, `db/02_bootstrap.sql`, `api/API.Tests/WorkflowRuntimeEngineTests.cs`, `api/API.Tests/StartupValidationTests.cs`, `api/API.Tests/WorkflowDefinitionValidationServiceTests.cs`, `api/API.Tests/PostgresWorkflowRepositoryWorkflowDefinitionIntegrationTests.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `CODEX_SYNC.md` | Zweiter, semantischer Legacy-Key-Slice abgeschlossen. Aktiver Anker für Form-/Gatekeeper-Node-Konfig ist jetzt überall `workflowDefinitionKey`; `legacyProcessTypeKey` bleibt nur als read-only-Fallback für bereits persistierte Definitionen stehen. Zentrale Helfer in `WorkflowRuntimeEngine` und `WorkflowDefinitionValidationHelpers` lesen prefer-new-then-fallback, Publish-/Draft-Validatoren erzwingen den neuen Schlüssel als Pflicht-Property; Supervisor-Gatekeeper-Record umbenannt (`LegacyProcessTypeKey` → `WorkflowDefinitionKey`), Fehlercode `unknown_form_legacy_process_type` → `unknown_form_workflow_definition_key`. FE-Hint und Builder-Hook-Kommentar folgen jetzt der definitionsbasierten Sprache; der Referenzdaten-Hook liest beide Schlüssel mit Fallback. Seeds (`db/02_dev_seed.sql`, `db/02_bootstrap.sql`) und sämtliche BE/FE-Fixtures schreiben den neuen Schlüssel. Verifikation: `dotnet build api/API/API.csproj` 0/0; `npm run build` im `web/` erfolgreich; `npx tsc --noEmit` 0/0; Vitest `adminWorkflowBuilderModel.test.ts` 15/15, `WorkflowBuilderStepCard.test.tsx`+`WorkflowBuilderPage.test.tsx`+`AdminConfigPage.test.tsx` 17/17. | `dotnet test --filter "FullyQualifiedName~WorkflowRuntimeEngineTests"` lief nicht bis zu den Zieltests, weil das Test-Projekt weiterhin an dem bereits vor dem Slice dokumentierten Stub-/Interface-Drift hängt (`IWorkflowCatalogService.GetPeopleDirectoryAsync`, `IWorkflowRepository.GetPeopleDirectory`, Admin-Workflow-Definition-Rückgabetypen in mehreren Test-Stubs) — keine Slice-Regression, der Drift war auch im vorigen Slice-Eintrag schon explizit als Risiko vermerkt. Offen für Folge-Slice: reine Property-Naming-Aufräumung der `PrimaryLegacyProcessTypeKey`-Properties auf C#-DTOs/Records (keine Semantik). |
| 2026-05-11 | CODEX-LEGACY-KEY-SLICE-1 | done | `web/src/hooks/adminWorkflowBuilderModel.ts`, `web/src/hooks/useAdminWorkflowBuilder.ts`, `web/src/hooks/useAdminWorkflowVersionReferenceData.ts`, `web/src/components/admin-config/AdminWorkflowBuilderFormSection.tsx`, `web/src/components/admin-config/WorkflowBuilderMeasurePreview.tsx`, `web/src/components/admin-config/workflowBuilderMeasureAnalysis.ts`, `web/tests/adminWorkflowBuilderModel.test.ts`, `web/tests/WorkflowBuilderMeasurePreview.test.tsx`, `web/tests/WorkflowBuilderPage.test.tsx`, `web/tests/WorkflowBuilderStepCard.test.tsx`, `web/tests/AdminConfigPage.test.tsx`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/WorkflowRuntimeEngineSnapshot.cs`, `api/API/Services/WorkflowRuntimeEngine.cs`, `api/API/Services/WorkflowLifecycleService.cs`, `api/API/Repositories/PostgresWorkflowRuntimeRepository.cs`, `api/API/Repositories/PostgresWorkflowRuntimeRepository.EngineAdapter.cs`, `api/API/Extensions/LifecycleStartupValidationExtensions.cs`, `api/API.Tests/WorkflowRuntimeEngineTests.cs`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `CODEX_SYNC.md` | Erster technischer Legacy-Abbau-Slice nach der Inventur umgesetzt. Fokus war bewusst der risikofreie Naming-Teil des Key-Clusters: Builder-internes `primaryLegacyProcessTypeKey` wurde auf `workflowDefinitionKey` umgestellt; UI-Wording im Builder und in der Measure-Vorschau folgt jetzt ebenfalls dem Definition-Layer. Parallel dazu wurden reine Backend-Naming-Reste reduziert: `IWorkflowRepository.GetRequirements/GetWorkflowConfig` sprechen jetzt auch in der Signatur von `workflowDefinitionKey`; Runtime-/Startup-Validation-Records verwenden `WorkflowDefinitionKey` statt `PrimaryLegacyProcessTypeKey`, ohne den fachlichen Restpfad (`node.config.legacyProcessTypeKey` + Answer-Loading) anzutasten. Verifikation: `npm run build` im `web/` erfolgreich; gezielte Vitest-Suite fuer Builder/AdminConfig 35/35 gruen; `dotnet build api/API/API.csproj -p:OutputPath=bin/Verify/` erfolgreich. | `dotnet test api/API.Tests/API.Tests.csproj --filter "FullyQualifiedName~WorkflowRuntimeEngineTests" -p:OutputPath=bin/Verify/` lief nicht bis zu den Zieltests, weil `API.Tests` bereits unabhaengig von diesem Slice an breiterem Stub-/Interface-Drift haengt (`GetPeopleDirectory`, Admin-Workflow-Definition-Methoden in mehreren Stub-Repos). Der echte Legacy-Rest des Clusters bleibt bewusst offen: `node.config.legacyProcessTypeKey`, Form-/Gatekeeper-Validierung und Runtime-Answer-Loading. |
| 2026-05-11 | CODEX-LEGACY-INVENTUR-DOCS | done | `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `KauthWorkflow/Architektur/Migrationspfad.md`, `KauthWorkflow/Architektur/Zielarchitektur.md`, `KauthWorkflow/Domäne/Workflow.md`, `CODEX_SYNC.md` | Reine Doku-Inventur zum verbliebenen Legacy-Bestand. `Legacy-Abbau-Plan.md` ist auf den echten Ist-Stand umgeschrieben: physisch abgebautes Altmodell (`process_types`, `task_templates`, Alias-Endpoints, Permission-Hardcodes) klar von den noch offenen Restbruecken getrennt. Restbestand jetzt als konkrete Cluster dokumentiert: `legacyProcessTypeKey`/`PrimaryLegacyProcessTypeKey`, Legacy-Status im Runtime-Pfad, `processType`-Benennung in Read-DTOs, `setup`-Rest als Daten-/Doku-Thema sowie veraltete ERD-/Seed-/Test-Artefakte. `Migrationspfad.md` Parallelzustand entsprechend gestrafft und auf Mai-2026-Stand gezogen. `Zielarchitektur.md` und `Domäne/Workflow.md` behaupten nicht mehr pauschal, dass `setup` noch aktiv im Code lebt; stattdessen ist klar dokumentiert, dass der offene Rest primär die Dateninventur ist. | Kein offener Risikopfad. ERD wurde im Folgepass 2026-05-11 (CLAUDE-LEGACY-CLEANUP) regeneriert; `setup`- und `process_types`-Cluster sind darin nicht mehr enthalten. |
| 2026-05-11 | CODEX-MD-CLEANUP-Z20 | done | `CODE_REVIEW.md`, `CODE_REVIEW_ARCHIVE.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md` | Die aktiven Steuerdateien nach dem Z20-Abschluss gestrafft. `TODO.md` zeigt wieder nur offenen Arbeitskontext statt abgeschlossener Z20- und Feature-Bloecke. `MEMORY.md` ist auf „kein aktiver Review-Slice" reduziert. `CODE_REVIEW.md` enthaelt nur noch die kompakte Z20-Abschlussfassung; die ausfuehrliche Slice-Map und Inventur liegen jetzt in `CODE_REVIEW_ARCHIVE.md`. Praktisch bedeutet das: der naechste Einstieg landet wieder direkt bei aktueller offener Arbeit statt in bereits erledigten Detailbloecken. | Kein technischer Risikopfad. Relevante offene Arbeit bleibt unveraendert nur zyklusuebergreifend (`R8`, `R10`, `L2`, `Z8-3.2/#8`). |
| 2026-05-11 | CODEX-Z20-B2-B3-B4 (READ CONTRACTS COMPLETE) | done | `api/API/Endpoints/AdminDirectorySyncEndpoints.cs`, `api/API/Services/EntraDirectorySyncService.cs`, `api/API/Endpoints/AdminNotificationTemplateEndpoints.cs`, `api/API/Services/NotificationTemplateService.cs`, `api/API/Endpoints/AdminRotationConfigEndpoints.cs`, `api/API/Services/RotationTemplateAdminService.cs`, `api/API/Endpoints/AdminWorkflowRuntimeEndpoints.cs`, `api/API/Repositories/PostgresWorkflowRuntimeRepository.cs`, `api/API/Repositories/PostgresWorkflowAutomationReadRepository.cs`, `api/API/Endpoints/AdminOrgEndpoints.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository*.cs`, `web/src/services/adminApi.ts`, `web/src/services/adminConfigApi.ts`, `web/src/services/rotationApi.ts`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z20 vollstaendig abgeschlossen.** B2: `/admin/directory/identities` liefert P1; `/admin/directory/responsibility-gaps` und `/admin/directory/pending-imports` sind als P1-Listen gesplittet, FE-Adapter rekonstruiert die bisherigen Domain-Objekte kompatibel. B3: `/admin/notification-templates` und `/admin/rotation/action-templates` liefern P1; `/admin/runtime/workflow-instances/{uid}/events` und `…/automation-jobs` liefern P2-Cursor-Seiten. B4: `/admin/auth/{users,roles,groups,permissions}` liefern P1; `/admin/directory/unlinked-identities` nutzt den P1-Query-Parser. Verifikation: `dotnet test api/API.Tests/API.Tests.csproj --artifacts-path .codex-artifacts` → 566 bestanden, 1 uebersprungen; `npm run build` im `web/` → erfolgreich. | Kein offener Z20-Slice. Bestehende UI-Consumer lesen teils weiterhin kompatible Arrays ueber Service-Adapter; echte UI-Pagination fuer Auth-/Settings-Listen bleibt Produkt-/UX-Folge bei Bedarf, kein Contract-Rest. |
| 2026-05-11 | CLAUDE-Z20-B1 (P3-LOOKUP-ADAPTER) | done | `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionAdminOperations.cs`, `api/API/Services/IWorkflowCatalogService.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API/Endpoints/WorkflowMasterDataEndpoints.cs`, `api/API/Endpoints/AdminNotificationTemplateEndpoints.cs`, `api/API/Services/INotificationTemplateService.cs`, `api/API/Services/NotificationTemplateService.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/AdminPeopleEndpointsTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `api/API.Tests/WorkflowLifecycleServiceTests.cs`, `web/src/services/api/lookupPage.ts` (NEU), `web/src/services/lookupApi.ts`, `web/src/services/adminApi.ts`, `web/src/services/queries/workflowDefinitionQueries.ts`, `CODE_REVIEW.md`, `TODO.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z20-B1 abgeschlossen.** P3-Lookup-Adapter fuer 3 Endpunkte live: `GET /workflow-definitions/startable` (Server-`search`/`limit` NEU), `GET /admin/notification-templates/preview-targets/workflows` und `…/rotation-plans` (URL-Param umbenannt: `query` → `search` fuer Vertragskonsistenz). Neues FE-Modul `services/api/lookupPage.ts` mit `LookupQueryOptions`/`buildLookupQuery` als P3-Typisierung. FE-Wrapper in `lookupApi.ts` und `adminApi.ts` auf `buildLookupQuery` umgestellt. `workflowDefinitionQueries.ts` nutzt Arrow-Function als `queryFn` statt direkter Funktionsreferenz. Backend: 509/509 Unit-Tests gruen. Frontend: 282/282 Tests gruen. Alle Builds 0 Fehler. Praktisch: P1, P2, P3 sind jetzt alle drei produktiv und typisiert; B2..B4 koennen auf das vollstaendige Pattern-Set aufbauen. | Kein Risiko. Die `query`→`search`-Umbenennung ist eine reine interne Vertragsaenderung (FE + BE aendern synchron). Keine alten Clients, die noch `?query=` senden. |
| 2026-05-11 | CLAUDE-Z20-EROEFFNUNG (READ CONTRACTS PHASE 2) + Z20-S1 (DOKU-ONLY) | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z20 eroeffnet als breiter Read-Vertrags-Folgeblock zu Z10/Z11.** Thema: die nach Z10/Z11 bewusst zurueckgestellten Read-/Listen-Vertraege (Block A Identity-Listen, C `/admin/directory/identities`, C Gaps/Pending Split, E Notification-Templates, F Rotation Action-Templates, G Runtime-Sub-Resources, H `/workflow-definitions/startable`) werden als zusammenhaengender Folgeblock neu priorisiert. **Z20-S1 (Inventur + Vertrags-Skizze + Slice-Plan) Doku-only abgeschlossen.** Findings je Block in `CODE_REVIEW.md` § „Aktiver Zyklus 20" mit Schreibregel-Bloecken (praktisch / lohnenswert / Nutzen). Slice-Map: B1 (P3-Lookup-Pattern: Block H + Block E Preview-Lookups) → B2 (Block C `/admin/directory/identities` P1 + Composite-Split Gaps/Pending, `claude-opus-4-7` + `high`) → B3 (Block E Liste + F + G: Templates P1 + Runtime-Sub-Resources P2/Cursor) → B4 (Block A `/admin/auth/*` + `/admin/directory/unlinked-identities` P1). Reihenfolge-Begruendung: P3 zuerst (einzig fehlendes Pattern), Composite-Risiko mittig mit Opus, Bulk-Adaption danach, Hygiene zum Schluss. Bewusst NICHT: Schreibpfade, Architektur-Umbau Definition-/Runtime-/Automation, Permission-Aenderungen, Mobile-Layout, Z16-S4, Z8-3.2/#8, `/admin/people`-Vertragsaenderung, Builder-Tab-Filter-URL-Persistenz, `GET /admin/directory/{status,groups,audit}`. | Keine. Doku-only, keine Code-Aenderung. B1..B4 stehen klar geschnitten zur Umsetzung an; Codex erzwingt Modell/Effort per CLI-Flag pro Bundle. |
| 2026-05-11 | CODEX-Z19-CLOSEOUT-CLEANUP | done | `CODE_REVIEW.md`, `CODE_REVIEW_ARCHIVE.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Nach dem Claude-S6-Commit waren `MEMORY.md` und `CODEX_SYNC.md` noch auf „S6 offen", waehrend `CODE_REVIEW.md` den Zyklus zwar als abgeschlossen markierte, aber noch den vollen aktiven Z19-Detailblock trug. Cleanup zieht die aktiven Steuerdateien wieder auf ihren eigentlichen Zweck: keine offene Slice-Arbeit, kompakter Abschlusszustand, Detailverlauf von Z19 ins Archiv bzw. in den Statusspiegel verschoben. Praktisch bedeutet das: der naechste Einstieg startet wieder an der echten Gegenwart statt in alten Review-Zwischenstaenden. | Kein neuer technischer Risikopfad. Offene Watchouts bleiben nur noch zyklusuebergreifend: `db/manual`-Paritaet weiter pflegen, tiefe `CancellationToken`-Resthebel nur bei konkretem Lastbefund anfassen. |
| 2026-05-11 | CLAUDE-Z19-S6 (AUTHZ-REPO-SPLIT) | done | `api/API/Repositories/PostgresUserAuthorizationRepository.AdminDepartmentOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminPositionOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminResponsibilityOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminUserOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminGroupOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminDepartmentReadOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminPositionReadOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminResponsibilityReadOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminReadOperations.cs`, `CODE_REVIEW.md`, `TODO.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z19-S6 abgeschlossen; Z19 damit fachlich fertig.** Claude hat den letzten MEDIUM-Slice ohne Vertragsaenderung umgesetzt und die grossen AuthZ-Sammeldateien nach Z9-Pattern in fokussierte Department/Position/Responsibility/User/Group- und Read-Partials geschnitten. Build `0/0`; `dotnet test api/API.Tests/API.Tests.csproj -p:OutputPath=bin/Verify/ --filter "Category!=Integration&Category!=Concurrency" --no-build`: 507/507 gruen. Praktisch bedeutet das: kuenftige Berechtigungs- oder Admin-Master-Data-Aenderungen treffen nicht mehr dieselben zwei 1000+ LOC-Dateien und erzeugen weniger Merge-/Review-Reibung. Commit: `ac5800d`. | Kein funktionales Risiko aus dem Split selbst; Restarbeit war nur Doku-Konsistenz in `MEMORY.md` und `CODEX_SYNC.md`, nicht im Code. |
| 2026-05-11 | CODEX-Z19-DOC-CLEANUP | done | `CODE_REVIEW.md`, `MEMORY.md`, `CODEX_SYNC.md` | Nach den gebuendelten Z19-Implementierungslaufen waren Kopf-/Kurzstatus von `CODE_REVIEW.md` und `MEMORY.md` noch auf einem Zwischenstand vor S5/S7/S8/S9. Cleanup zieht beide aktiven Steuerdateien auf den echten Ist-Stand: Z19 ist fast abgeschlossen, einziger Rest ist S6 (`PostgresUserAuthorizationRepository`-Split). Praktisch bedeutet das: der naechste Einstieg fuer Claude/Codex ist wieder eindeutig, ohne dass man zuerst mehrere Ergebnisbloecke gegen den Dateikopf aufloesen muss. | Kein neuer technischer Risikopfad. Offener Arbeitsrest bleibt unveraendert Z19-S6; tiefe `CancellationToken`-Resthebel und der `db/manual`-Workflow bleiben wie bereits dokumentiert. |
| 2026-05-11 | CLAUDE-Z19-S5+S7+S8+S9/L2+L3 (BUNDLED MEDIUM-SLICE) | done | `api/API/Services/SystemEventLogService.cs`, `api/API/Services/ISystemEventLogService.cs`, `api/API/Endpoints/AdminSystemLogEndpoints.cs`, `api/API/Services/AdminRuntimeHealthService.cs`, `api/API/Services/WorkflowAutomationService.cs`, `api/API/Repositories/IWorkflowAutomationRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.AutomationOperations.cs`, `api/API.Tests/SystemEventLogServiceTests.cs` (neu, 67 Tests), `api/API.Tests/WorkflowAutomationServiceTests.cs`, `api/API.Tests/EntraDirectorySyncServiceTests.cs`, `api/API.Tests/PostgresWorkflowRepositoryAutomationIntegrationTests.cs`, `api/API.Tests/RotationNotificationServiceTests.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/PostgresWorkflowRepositoryProcessTypeIntegrationTests.cs` (geloescht), `web/src/services/adminApi.ts`, `web/src/components/admin-config/AdminSystemLogSection.tsx`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z19-S5 + S7 + S8 + S9/L2 + S9/L3 als gebuendelter Slice abgeschlossen.** S5: `SystemEventLogService` umgebaut — `WriteAsync` haelt UndefinedTable-Catch aber loggt jetzt `logger.LogError` statt still zu schlucken; `GetAdminLogsAsync` + `GetAdminLogSummaryAsync` lassen UndefinedTable als HTTP 500 propagieren (Operator-Signal bei Schema-Drift); Keyset-Cursor-Pagination (Z11-Pattern, `CursorPageDto<AdminSystemLogEntryDto>`) ersetzt LIMIT/OFFSET; private Methoden als `internal static` fuer Testbarkeit; 67 neue Unit-Tests in `SystemEventLogServiceTests.cs` (Redaktion, Normalisierung, Filter, DecodeCursor). FE: `AdminSystemLogQueryOptions.offset` → `cursor`; `AdminSystemLogSection` auf Cursor-State + vereinfachte Pagination-UI umgestellt. S7: `WorkflowAutomationService.TryProcessNextPendingJobAsync` — `CompleteAutomationJobFailure` in try-catch; bei Exception → `UnclaimAutomationJobAsync` (neuer Repo-Pfad, setzt Job auf `pending`); `systemEventLogService.WriteAsync` ebenfalls best-effort in try-catch. `IWorkflowAutomationRepository.UnclaimAutomationJobAsync` + Impl in `PostgresWorkflowRepository.AutomationOperations.cs` neu. Neuer Test `TryProcessNextPendingJobAsync_UnclaimsJobWhenCompleteFailureThrows`. S8: Z8-3.2/#8 (`RegenerateDepartmentPlansAsync` Schleife) + Z16-S4 (Automation-Snapshot-Vertrag) formal deferred mit Begruendung in `CODE_REVIEW.md` — kein Code, bewusste Entscheidung. S9/L2: `AdminRuntimeHealthService` — `BuildHostHealthAsync`-Task vor `Task.WhenAll` gestartet, doppeltes `WhenAll` aufgeloest; jetzt ein einzelnes `Task.WhenAll` mit allen 6 Tasks. S9/L3: 4-Zeilen-Tombstone `PostgresWorkflowRepositoryProcessTypeIntegrationTests.cs` per `git rm` geloescht. Stub-Return-Typen in 4 Test-Dateien auf `CursorPageDto<AdminSystemLogEntryDto>` nachgezogen. Build: 0 Fehler. `dotnet test --filter "!Integration&!Concurrency"`: 511/511 gruen. FE `tsc --noEmit`: 0 Fehler. **Naechster Schritt: Z19-S6** (`PostgresUserAuthorizationRepository` AdminOperations-/AdminReadOperations-Split nach Z9-Pattern). | UndefinedTable-Asymmetrie ist bewusst: `WriteAsync` schluckt weiterhin (Background-Job darf Sweep nicht unterbrechen), List/Summary propagieren als HTTP 500 (Operator-Dashboard muss Schema-Drift sichtbar machen). Cursor-Pagination: `DecodeCursor` gibt `null` bei ungueltiger Base64 — fuehrt zu erstem Seitenladen statt Fehler; kein Nutzer-Risiko. FE `npm run test` hat 1 vorbestehenden Fehler in `MyTasksPage.test.tsx` (ausserhalb des Slice-Scopes). `dotnet build` braucht weiterhin `OutputPath=bin/Verify/`, weil laufender Dev-API-Prozess `API.exe` sperrt. |
| 2026-05-11 | CLAUDE-Z19-S3+L1 (SCHEMA-PARITAETS-CHECK + STRAY-DIR) | done | `db/manual/manifest.json` (neu), `db/manual/README.md` (neu), `api/API.Tests/SchemaParityTests.cs` (neu), `db/init/prod;C/` (entfernt), `KauthWorkflow/Betrieb/Setup.md`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z19-S3 + S9/L1 als gebuendelter Slice abgeschlossen.** S3: Schema-Paritaets-Check ueber Manifest + xUnit-Tests. `db/manual/manifest.json` registriert jede `*.sql`-Datei in `db/manual/` mit `expect_in_schema` (Substrings, die nach der Migration in `db/01_schema.sql` stehen muessen) und `forbid_in_schema` (Substrings, die nach Rename/Loeschung dort nicht mehr auftauchen duerfen). Aktuell registriert: `2026-05-08_rename_approval_task_template_key_to_approval_spec_key.sql` (expect: `approval_spec_key character varying(120)` + `workflow_definitions_supervisor_step_requires_approval_spec`; forbid: `approval_task_template_key` + `workflow_definitions_supervisor_step_requires_approval_task`) und `2026-05-08_add_directory_identities_job_title.sql` (expect: `job_title character varying(180)`). `SchemaParityTests.cs` hat 4 Tests: 1:1-Abdeckung zwischen Manifest und Verzeichnis, Existenz der referenzierten Dateien, Praesenz aller `expect_in_schema`-Marker in `db/01_schema.sql`, Absenz aller `forbid_in_schema`-Marker. `db/manual/README.md` dokumentiert Konvention + Workflow fuer neue Schema-Aenderungen. `KauthWorkflow/Betrieb/Setup.md` § „Bestehende Datenbanken bei Schema-Renames" verweist auf Manifest + Test. L1: leeres `db/init/prod;C/`-Stray-Verzeichnis (Shell-Typo, nicht git-tracked) entfernt. `dotnet test --filter "SchemaParityTests" -p:OutputPath=bin/Verify/`: 4/4 gruen. `dotnet test --filter "!Integration&!Concurrency" -p:OutputPath=bin/Verify/`: 444/444 gruen (440 vorher + 4 neu). **Naechster Schritt: Z19-S5** (`SystemEventLogService`: UndefinedTable-Swallow ersetzen, Cursor-Pagination, eigene Unit-Tests). | **Bewusst NICHT umgesetzt:** kein voller Postgres-Syntax-Parser; der Check ist bewusst substring-basiert und prueft nur Soll-Endzustand, nicht SQL-Syntax. Kein Migrations-Framework-Umbau (Flyway/EF Migrations); manuelle Migrationen bleiben bewusst manuell. Keine automatische Inplace-Anwendung in Compose. Z19-S9-Resthebel: L2 (`AdminRuntimeHealthService` doppeltes `Task.WhenAll`) und L3 (Tombstone-Datei) weiterhin offen. Build/Test brauchte erneut `OutputPath=bin/Verify/`, weil laufender Dev-API-Prozess `API.dll` in Default-`bin/Debug` sperrt — kein Code-Problem. Pflicht fuer kuenftige Schema-Aenderungen: gleicher Pass `01_schema.sql` + `db/manual/<datum>_*.sql` + `manifest.json`-Eintrag mit mindestens einem Marker, sonst wird `SchemaParityTests` rot. |
| 2026-05-11 | CLAUDE-Z19-S2+S4+L4 (BUNDLED HIGH-SLICE) | done | `api/API/Services/DirectorySyncHostedService.cs`, `api/API/Services/IWorkflowLifecycleService.cs`, `api/API/Services/WorkflowLifecycleService.cs`, `api/API/Services/WorkflowDefinitionRuntimeService.cs`, `api/API/Services/WorkflowRuntimeService.cs`, `api/API/Services/TaskApplicationService.cs`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/IWorkflowLifecycleScopedRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowQueryOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.TaskMetadataOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.RequirementOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.TaskOperations.cs`, `api/API/Endpoints/WorkflowEndpoints.cs`, `api/API/Endpoints/TaskEndpoints.cs`, `api/API/Endpoints/AdminWorkflowRuntimeEndpoints.cs`, `api/API.Tests/WorkflowLifecycleServiceTests.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/WorkflowAutomationServiceTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z19-S2 + S4 + S9/L4 als gebuendelter Slice abgeschlossen.** S2: `DirectorySyncHostedService` bekommt `SweepTimeout = 2h` analog `RotationNotificationHostedService`; pro Sweep verlinkter `CancellationTokenSource` mit `CancelAfter`; Timeout wird als Warnung + Hosted-Log `scheduled_directory_sync_timeout` geloggt + 5-Minuten-Backoff. S4: `IWorkflowLifecycleService` traegt optionalen `CancellationToken cancellationToken = default` auf allen 8 Methoden; Impl reicht Token in `OpenAsync`/`BeginTransactionAsync`/`CommitAsync` und an scoped-Repo durch; `IWorkflowLifecycleScopedRepository` reicht Token optional auf 5 In-Scope-Methoden durch. Endpoints (`/tasks/{id}/status` PATCH, `/tasks/ref/{taskRef}/status` PATCH, `/tasks/{id}/approval-decision` POST, `/tasks/ref/{taskRef}/approval-decision` POST, `/admin/runtime/workflow-instances` POST, `…/form-completions` POST, `…/approval-completions` POST, `…/task-completions` POST, `/workflows` GET) binden Request-Token. L4: `IWorkflowRepository.GetFilteredWorkflows` traegt `CancellationToken`; durchgereicht ueber `WorkflowQueryOperations`, `TaskMetadataOperations`, `RequirementOperations` bis in `Read`/`ExecuteReader`. Test-Stubs in `WorkflowEndpointsTests`, `WorkflowAutomationServiceTests`, `WorkflowLifecycleServiceTests`, `AdminWorkflowDefinitionConfigEndpointsTests` auf neue Signaturen mit optionalem Default-Token aktualisiert. `dotnet build` (Verify-Output): 0 Warnungen, 0 Fehler. `dotnet test --filter "WorkflowLifecycleServiceTests|WorkflowEndpointsTests|AdminWorkflowDefinitionConfigEndpointsTests|DirectorySyncHostedServiceTests"`: 51/51 gruen. `dotnet test --filter "!Integration&!Concurrency"`: 440/440 gruen. **Naechster Schritt: Z19-S3** (Schema-Paritaets-Check `db/01_schema.sql` vs `db/manual/*.sql`). | **Bewusst NICHT veraendert:** tiefe statische Repository-Helfer (`PostgresWorkflowRuntimeRepository.AdvanceRuntimeUntilWaitOrTerminal`, `CreateWorkflowNodeInstance`, `InsertWorkflowRuntimeEvent`, `LoadPublishedWorkflowDefinitionVersion`, einige Helfer in `PostgresRepositorySharedHelpers.*`). Praktisch: ein abgebrochener Request schneidet jetzt Connection und Transaktion am Commit-Punkt; statische Sub-Queries innerhalb laufen bis zur naechsten `CommandText`-Grenze weiter. Eine vollstaendige Ende-zu-Ende-Token-Propagation wuerde mehrere Hundert Signaturen anfassen und liegt explizit ausserhalb des Z19-Schnitts — wenn relevant, eigenstaendiger Folgeslice mit konkretem Lock-/Latenz-Befund als Anlass. Z19-S9-Resthebel: L1 (stray `db/init/prod;C/`), L2 (`AdminRuntimeHealthService` doppeltes `Task.WhenAll`), L3 (Tombstone-Datei) weiterhin offen. Build/Test waren nur ueber `OutputPath=bin/Verify/` moeglich, weil laufender Dev-API-Prozess `API.exe`/`API.dll` im Default-`bin/Debug` sperrt — kein Code-Problem. |
| 2026-05-11 | CLAUDE-Z19-S1 (BACKEND FULL REVIEW — AUDIT-PASS) | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Z19-S1 abgeschlossen.** Audit-Pass ueber `api/API/Endpoints`, `api/API/Repositories`, `api/API/Services`, `Auth/`, `Authorization/`, `Services/Directory/`, Background-/Sweep-Jobs, `db/01_schema.sql`, `db/init/`, `db/manual/` und `api/API.Tests`. Keine Code-Aenderungen. Ergebnis: **3 HIGH, 4 MEDIUM, 4 LOW Findings** mit Schreibregel-Triplet (Praktisch/Lohnenswert/Nutzen). HIGH: **H1** `DirectorySyncHostedService` hat keinen Sweep-Timeout (vs `RotationNotificationHostedService.SweepTimeout = 2h`) — haengender Graph-Call blockt Sync-Loop bis Restart; **H2** `db/manual/`-Workflow ohne Schema-Paritaetspruefung gegen `db/01_schema.sql` — Drift faellt erst per Laufzeitfehler auf; **H3** `WorkflowLifecycleService.CreateWorkflowInstanceAsync`/`UpdateTaskStatusAsync` ohne `CancellationToken`, `WorkflowRuntimeService.GetWorkflowsAsync` reicht Token nicht ans Repository weiter. MEDIUM: **M1** `SystemEventLogService` schluckt `UndefinedTable` still + LIMIT/OFFSET-Pagination + keine Unit-Tests; **M2** `PostgresUserAuthorizationRepository.AdminOperations.cs` (1886 LOC) + `…AdminReadOperations.cs` (1002 LOC) sind naechster Repo-Split-Kandidat nach Z9-Pattern; **M3** `WorkflowAutomationService.TryProcessNextPendingJobAsync` Failure-of-Failure leakt Job-Claim; **M4** deferred Z8-3.2/#8 + Z16-S4 brauchen Z19-Verdikt. LOW: **L1** stray `db/init/prod;C/`-Verzeichnis; **L2** `AdminRuntimeHealthService` doppeltes `Task.WhenAll` (Z. 37–41) + serielles `BuildHostHealthAsync`; **L3** 4-Zeilen-Tombstone `PostgresWorkflowRepositoryProcessTypeIntegrationTests.cs`; **L4** `WorkflowRuntimeService.GetWorkflowsAsync` schluckt `CancellationToken`. Slice-Schnitt: Z19-S2 (Sweep-Timeout) → S3 (Schema-Paritaet) → S4 (Cancellation) → S5 (SystemEventLog) → S6 (AuthZ-Split) → S7 (Automation-Failure-Failure) → S8 (Deferred-Verdikt) → S9 (Hygiene-Batch). | **Naechster Schritt: Z19-S2** (Sweep-Timeout fuer `DirectorySyncHostedService`) per CLI mit `--model claude-opus-4-7 --effort high`. Pattern existiert bereits in `RotationNotificationHostedService` — kleiner Slice mit klarem Vorbild. Bis Z19-S3 muessen neue Schema-Aenderungen weiterhin als `db/manual/<datum>_*.sql` abgelegt werden, sonst latentes Drift-Risiko. |
| 2026-05-11 | CLAUDE-Z19-EROEFFNUNG (BACKEND FULL REVIEW) | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Neuer aktiver Review-Zyklus **Z19 — Backend Full Review / Holistic Audit** eroeffnet, analog zu Z18 (Frontend Full Review). Doku-/Priorisierungs-Slice, kein Code-Change. Begruendung: Skalierbarkeit (B-) und Testbarkeit (B) sind die schwaechsten Noten der Gesamtbewertung; deferred Hotspots (Z8-3.2/#8, Z16-S4) und die juengsten DB-Drift-Vorfaelle (manuelle SQL-Helfer fuer `approval_spec_key` + `directory_identities.job_title`) deuten auf systemische Resthebel hin, die nach den punktuellen Backend-Zyklen Z8/Z9/Z11/Z12/Z13 noch nicht zusammenhaengend untersucht wurden. Slice-Plan: **Z19-S1** Audit-Pass ueber `api/API/Endpoints`, `api/API/Repositories`, `api/API/Services`, `Authorization/`, `Auth/`, `Services/Directory/`, Background-/Sweep-Jobs, Schema-/Migrations-Hygiene und Test-Coverage; liefert priorisierte Findings (HIGH/MEDIUM/LOW) im Z18-Stil. **Z19-S2..N** Umsetzungsslices nach Findings-Verteilung. Empfehlung Codex-CLI fuer S1: `--model claude-opus-4-7 --effort high`. Bewusst NICHT in Z19: breite Architektur-Umbauten am Definition-/Runtime-/Automation-Layer, neue FE-Findings, Berechtigungsmodell-Aenderungen ohne konkretes Risiko. Praktisch: nach dem geschlossenen Frontend-/Entra-Block schafft Z19 die priorisierte Backend-Backlog, an der die naechsten Wartbarkeits-/Skalierbarkeits-/Test-Coverage-Slices andocken — billiger als wiederholte Hotfixes unter Last. | Naechster Schritt: Codex erzwingt Z19-S1 per CLI mit `--model claude-opus-4-7 --effort high`. Z19 muss den DB-Drift-Pfad (`db/manual/`-Workflow) explizit als Audit-Bereich benennen, sonst werden Migrations-/Schema-Hygiene-Findings stillschweigend uebersehen. Slice-Drift in Richtung Architektur-Umbau muss aktiv abgewehrt werden. |
| 2026-05-08 | OPS-VM-DEV-WEB-PORT-FIX | done | `scripts/start-vm.sh`, `CODEX_SYNC.md` | Linux-VM-Dev-Startscript an aktuellen Vite-Stand angepasst. Ursache des False-Negative: `web/vite.config.ts` nutzt standardmaessig Port `8080`, `scripts/start-vm.sh` pruefte/wartete aber weiterhin hart auf `5173`. Fix: Web-Port wird jetzt zentral aus `VITE_PORT` bzw. Default `8080` abgeleitet, in `.tmp-vm-dev/web.port` persistiert und konsistent fuer `PUBLIC_BASE_URL`, Vite-Start (`--port`), Reachability-Check, `status` sowie Stop-Cleanup verwendet. Wenn der Wunsch-Port belegt ist und kein fixes `VITE_PORT` gesetzt wurde, weicht das Script auf einen kleinen Fallback-Port-Satz aus. | Shell-Syntax lokal nicht mit `bash -n` verifiziert, weil im Windows-Arbeitskontext kein `/bin/bash` vorhanden war. Der Fix ist aber rein textuell gegen den bestehenden Scriptfluss angepasst. |
| 2026-05-08 | OPS-DB-JOB-TITLE-HINWEIS (DIRECTORY_IDENTITIES) | done | `db/manual/2026-05-08_add_directory_identities_job_title.sql` (neu), `KauthWorkflow/Betrieb/Setup.md`, `CODEX_SYNC.md` | Zweiter Bestands-DB-Drift nach dem FE-8-Rename dokumentiert: bestehende Dev-/Server-DBs koennen noch ohne `directory_identities.job_title` laufen. Seit dem Entra-Import-/Jobtitel-Slice schreibt der Directory-Sync diese Spalte beim Bulk-Upsert; fehlt sie, laufen Gruppen zwar an, der Sync endet aber `partial` und `/auth/sim-users` bleibt leer, weil keine aktuelle Directory-Projektion gepflegt werden kann. Neuer manueller SQL-Helfer fuegt die Spalte idempotent hinzu; Setup-Doku um den typischen Fehlertext erweitert. | Auch dieses Script ist bewusst manuell. Bei produktionsnahen Umgebungen vor Ausfuehrung wie ueblich Backup/Snapshot ziehen und danach API/Sync neu anstossen. |
| 2026-05-08 | OPS-DB-RENAME-HINWEIS (APPROVAL_SPEC_KEY) | done | `db/manual/2026-05-08_rename_approval_task_template_key_to_approval_spec_key.sql` (neu), `KauthWorkflow/Betrieb/Setup.md`, `CODEX_SYNC.md` | Laufzeitfehler beim API-Start mit bestehender DB geklaert: Code erwartet seit FE-8 `workflow_definitions.approval_spec_key`, bestehende Dev-/Server-DBs koennen aber noch `approval_task_template_key` haben. Neuer idempotenter SQL-Helfer fuer Inplace-Rename von Spalte + Check-Constraint angelegt; Setup-Doku ergaenzt: Restart allein hilft nicht, Volume-Loeschen nur fuer wegwerfbare Dev-Daten, produktionsnahe/befuellte Umgebungen per gezieltem SQL-Migrationsschritt aktualisieren und danach API neu starten. | Script ist bewusst manuell und nicht automatisch in Compose verdrahtet. Vor Ausfuehrung auf produktionsnahen Umgebungen wie ueblich DB-Backup/Snapshot ziehen. |
| 2026-05-08 | CODEX-C (PEOPLE DIRECTORY UNION + INLINE-IMPORT) | done | `api/API/Contracts/PeopleDirectoryDtos.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API.Tests/AdminPeopleEndpointsTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `api/API.Tests/EntraDirectorySyncOperationsIntegrationTests.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/WorkflowLifecycleServiceTests.cs`, `web/src/types/workflow.ts`, `web/src/utils/employmentStatus.ts`, `web/src/pages/PeopleDirectoryPage.tsx`, `TODO.md`, `CODE_REVIEW.md`, `CODEX_SYNC.md`, `MEMORY.md`, `web/README.md` | **Feature C** nach Claude-Rate-Limit von Codex sauber abgeschlossen. `GetPeopleDirectory` liefert jetzt per `UNION ALL` auch aktive `directory_identities` ohne `people`-Record. Der Listenvertrag erlaubt dafuer `personId = null` plus `directoryIdentityId`; Status `directory_only` ist in Backend und Frontend sichtbar (`employmentStatus` + `directoryLinkStatus`). `PeopleDirectoryPage` rendert fuer echte Mitarbeiterkarten weiter Links auf `/people/:personId`, fuer directory-only-Eintraege stattdessen einen Inline-Import-Button pro Karte/Tabellenzeile. Die Mutation nutzt den bestehenden A1-Endpoint `POST /admin/people/import-from-directory`, zeigt Toast-Feedback und invalidiert People-/Unlinked-Queries. Backend-Build und 494 Backend-Tests gruen; Frontend-Build gruen. Voller FE-Testlauf hat 1 Fehler in `tests/MyTasksPage.test.tsx`, ausserhalb des C-Slices. Praktisch bedeutet das: nicht importierte Entra-Mitarbeitende sind jetzt sofort im Mitarbeiterverzeichnis sichtbar und koennen ohne Umweg einzeln importiert werden. Das lohnt sich, weil Admins nicht zwischen separaten Bereichen springen oder unsichtbare Personen erraten muessen. Besser wird dadurch die Sichtbarkeit des Bestands, die Nachpflegegeschwindigkeit und die Konsistenz zwischen Verzeichnis und Mitarbeiterakte. | Claude Code war am 2026-05-08 um 15:02:06 +02:00 fuer weitere CLI-Laeufe rate-limited; der erste C-Anlauf hinterliess halben WIP, den Codex im selben Arbeitsgang sauber zu Ende gefuehrt hat. Offener Test-Hinweis: `npm run test` scheitert aktuell an `tests/MyTasksPage.test.tsx` („Vorgänge mit Aufgaben" waehrend Loading-State), nicht an `PeopleDirectoryPage`. |
| 2026-05-08 | CLAUDE-A3 (MITARBEITERKARTE INLINE-EDIT) | done | `api/API/Contracts/PeopleDirectoryDtos.cs`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/IWorkflowCatalogService.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API/Endpoints/AdminPeopleEndpoints.cs`, `web/src/services/peopleApi.ts`, `web/src/pages/PersonWorkflowHistoryPage.tsx`, `TODO.md`, `CODE_REVIEW.md`, `CODEX_SYNC.md` | **A3 Mitarbeiterkarte** abgeschlossen. Neuer Endpunkt `PATCH /admin/people/{personId}` (Admin-only; aktualisiert `entry_date` + `badge_number`, null loescht den Wert, 204 on success, 404 wenn Person nicht gefunden). Backend: `UpdatePersonRequest`-DTO, `UpdatePersonCoreFields` im Repository (parametrisiertes UPDATE mit NpgsqlParameter<T?>), Service-Delegation ohne Logik. Frontend: `updatePerson(personId, data)` in `peopleApi.ts`. `PersonOverviewSection` hat neues Inline-Edit-Mode: Bearbeiten-Button (nur wenn `capabilities.hasAdminRole`), lokaler State fuer `editEntryDate`/`editBadgeNumber`, `useMutation` + `useQueryClient.invalidateQueries` nach Save. Luecken-Warnung (panel-note, warning-color) wenn `entryDate` oder `badgeNumber` null. Badge „Retroaktiv importiert" (`badge badge--default`) in Status-Pills wenn kein Onboarding-Vorgang vorhanden (`latestCompletedOnboardingWorkflowUid === null && !workflows.some(onboarding-key)`). `PersonWorkflowsListSection` zeigt bei leerem Vorgangsbereich fuer retroaktiv importierte Personen einen erklaerenden Hinweistext mit Verweis auf „Neuen Vorgang anlegen". Build: 0 CS-Fehler (BE-Binaries durch laufenden API-Prozess gesperrt, Compilation erfolgreich). FE Build: 0 TS-Fehler. FE Tests: 280/280 gruen. | Kein offener Risikopfad. BE-Binary-Lock durch laufenden API-Dev-Prozess verhindert `.exe`-Copy beim Build, aber kein CS-Compilefehler. PATCH-Endpoint: setzt immer beide Felder; null loescht den Wert (kein Partial-PATCH). Naechster Schritt: C (Verzeichnis UNION) oder neuer Zyklus. |
| 2026-05-08 | CLAUDE-B (ENTRA-STELLENBEZEICHNUNGEN) | done | `api/API/Contracts/AuthModels.cs`, `api/API/Repositories/IUserAuthorizationRepository.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminOperations.cs`, `api/API/Endpoints/AdminOrgEndpoints.cs`, `web/src/services/queryKeys.ts`, `web/src/services/adminApi.ts`, `web/src/components/admin-config/AdminDepartmentEntraImportSection.tsx` (neu), `web/src/components/admin-config/AdminOrganizationDepartmentEditor.tsx`, `TODO.md`, `CODEX_SYNC.md` | **Feature B** abgeschlossen. Neuer Endpunkt `GET /admin/master-data/departments/{id}/entra-job-titles` (DISTINCT job_title per ILIKE-Match auf `departments.name`). Neuer Endpunkt `POST …/positions/import-from-entra` (importiert ausgewählte Bezeichnungen, überspringt bereits vorhandene Namen case-insensitiv, gibt `created`/`skipped` zurück). Neue selbstständige Komponente `AdminDepartmentEntraImportSection` direkt in `AdminOrganizationDepartmentEditor` eingebettet: lädt Entra-Titel per useQuery (staleTime 60s), zeigt Checkboxen (bereits vorhandene markiert+deaktiviert), Select-All für importierbare Titel, Ergebnisbanner nach Import mit Dismiss. Build: 0 BE-Fehler. BE-Tests: 494/494 grün. FE-Tests: 280/280 grün. | Match zwischen `directory_identities.department_name` und `departments.name` ist ILIKE — setzt voraus, dass der Abteilungsname im Entra-Directory zeichengetreu gespeichert ist. Wenn kein Match, zeigt die UI einen Hinweis. |
| 2026-05-08 | CLAUDE-A2 (IMPORT-UI) | done | `web/src/types/auth.ts`, `web/src/services/peopleApi.ts`, `web/src/services/queryKeys.ts`, `web/src/components/admin-config/AdminEntraImportSection.tsx` (neu), `web/src/components/admin-config/adminWorkspaceModel.ts`, `web/src/components/admin-config/AdminConfigWorkspaceContent.tsx`, `web/src/components/admin-config/AdminConfigWorkspaceSections.tsx`, `TODO.md`, `CODE_REVIEW.md`, `CODEX_SYNC.md` | **A2 Import-UI** abgeschlossen. Neue Admin-Sektion `entra_import` unter „Personen & Zugriff". Selbststaendige Komponente `AdminEntraImportSection` mit eigenem useQuery/useMutation (kein Bundle-Prop-Threading). Funktionen: Gruppierung nach Abteilung, pro-Gruppe und global Select-All mit indeterminate-State, Checkbox-Auswahl pro Zeile, Toggle deaktivierte Entra-Konten (standardmaessig ausgeblendet), Spalten Name/UPN + App-Konto-Badge, Stelle (job_title), Konto-Status. Import-Button mit Beladezustand. Ergebnisbanner (angelegt/verknuepft/uebersprungen) mit Dismiss. Neue Typen `UnlinkedDirectoryIdentity` und `ImportPeopleFromDirectoryResult` in `auth.ts`. Neue API-Funktion `getUnlinkedDirectoryIdentities` und `importPeopleFromDirectory` in `peopleApi.ts`. Neuer Query-Key `admin.unlinkedIdentities`. Build: 0 TS-Fehler. Tests: 280/280 gruen. | Kein offener Risikopfad. Backend-seitig: max. 500 Identitaeten pro Request — ausreichend fuer den vorgesehenen Anwendungsfall. Naechster Schritt: A3 (Mitarbeiterkarte fehlende Felder) oder B (Entra-Stellenbezeichnungen) oder C (Verzeichnis UNION). |
| 2026-05-08 | CLAUDE-A1 (PEOPLE IMPORT BACKEND) | done | `api/API/Contracts/PeopleDirectoryDtos.cs`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/IWorkflowCatalogService.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API/Endpoints/AdminPeopleEndpoints.cs`, `api/API/Endpoints/AdminDirectorySyncEndpoints.cs`, `TODO.md`, `CODEX_SYNC.md` | **A1 Backend-Fundament** abgeschlossen. Enthaltene job_title-WIP-Durchleitung (IEntraGraphClient, EntraGraphClient, EntraDirectorySyncOperations, Schema, PersonDirectoryItemDto, workflow.ts) wurde mitgelifert. Neue Endpoints: `POST /admin/people/import-from-directory` (erstellt nur `people`-Record, keinen `app_user`; Dedup via `directory_identity_id`, `employee_number`, `app_user_id`; Auto-Link zu bestehendem `app_user` via `entra_object_id`; Audit-Log in `person_match_audit_log`; Outcome: created/linked/skipped) und `GET /admin/directory/unlinked-identities` (Entra-Identitaeten ohne `people`-Record, filterbar nach Abteilung und account_enabled, paginiert). Implementiert in Repository/Service/Endpoints-Layer. Build: 0 CS-Fehler. Tests: 424/424 gruen. Feature C kann jetzt direkt auf `POST /admin/people/import-from-directory` aufsetzen. | Kein offener Risikopfad. DB-seitig: `people.app_user_id` ist UNIQUE; Race-Condition bei parallelem Import wird per Exception-Catch abgefangen und als `skipped` zurueckgegeben. Naechster Schritt: A2 (Import-UI) oder C (Verzeichnis UNION). |
| 2026-05-08 | DOCS-FE-CLOSEOUT-FOLLOW-UP | done | `MEMORY.md`, `CODEX_SYNC.md` | Nach dem FE-8-Rename war `MEMORY.md` noch auf dem alten Z18-Aktivstand. Der Kurzfokus wurde auf den echten Abschlussstand gezogen: kein aktiver Zyklus offen, Z18 vollstaendig abgeschlossen, FE-8 abgeschlossen und `FRONTEND_TODO.md` aktuell leer. | Kein neuer fachlicher Befund. Naechster Schritt bleibt die Priorisierung eines neuen Zyklus ausserhalb des abgearbeiteten Frontend-Backlogs. |
| 2026-05-08 | CLAUDE-FE-8 (APPROVAL_SPEC_KEY RENAME) | done | `db/01_schema.sql`, `api/API/Contracts/WorkflowDtos.cs`, `api/API/Repositories/PostgresRepositorySharedHelpers.cs`, `api/API/Repositories/PostgresWorkflowRepository.cs`, `api/API/Services/WorkflowRuntimeEngineSnapshot.cs`, `api/API/Services/WorkflowRuntimeEngine.cs`, `api/API/Repositories/PostgresWorkflowRuntimeRepository.EngineAdapter.cs`, `api/API/Repositories/PostgresWorkflowTaskGenerationService.cs`, `api/API/Repositories/PostgresWorkflowStatusCalculationService.cs`, `api/API/Repositories/PostgresWorkflowNotificationDispatchOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.LifecycleOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.ReadOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowCreateOperations.cs`, `api/API/Extensions/LifecycleStartupValidationExtensions.cs`, `api/API/WorkflowStatusRules.cs`, `api/API.Tests/PostgresWorkflowRepositoryLinkIntegrationTests.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/WorkflowRuntimeEngineTests.cs`, `FRONTEND_TODO.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `KauthWorkflow/Stand/Legacy-Abbau-Plan.md`, `KauthWorkflow/Architektur/LA5-TaskSpezifikation-Skizze.md`, `KauthWorkflow/Architektur/Schritt7-Runtime-TaskSystem-Skizze.md` | **FE-8**: `approval_task_template_key` → `approval_spec_key` vollstaendig umbenannt. Schema-Spalte + Constraint (`workflow_definitions_supervisor_step_requires_approval_task` → `…_approval_spec`), alle SQL-Strings in Repositories/Extensions, C#-Property `ApprovalTaskTemplateKey` → `ApprovalSpecKey` in 7 Klassen, Parameter/Variablen in `WorkflowStatusRules.cs`. Test-Fixtures angepasst. Vault-Docs nachgezogen. Build: gruen. Tests: gruen. FRONTEND_TODO offene Items: keine mehr. | Bestehende Dev-DBs brauchen manuelles `ALTER TABLE workflow_definitions RENAME COLUMN approval_task_template_key TO approval_spec_key` + Constraint-Rename. Test-Containers initialisieren frisch aus `01_schema.sql` — Integration-Tests nicht betroffen. |
| 2026-05-08 | CLAUDE-Z18-S4 (WORKFLOW SEARCH CONSOLIDATION) | done | `web/src/App.tsx`, `web/src/pages/WorkflowSearchPage.tsx` (geloescht), `web/src/pages/WorkflowListFilters.tsx`, `web/src/pages/PersonWorkflowHistoryPage.tsx`, `web/src/navigation/useRoleAwareNavigation.ts`, `web/src/auth/roleModel.ts`, `web/tests/WorkflowSearchPage.test.tsx`, `FRONTEND_TODO.md`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `web/README.md` | **Z18-F4**: `/search` ist jetzt ein `SearchParamsRedirect` auf `/workflows`; `q`, `dept`, `type`, `status` werden erhalten. `WorkflowSearchPage.tsx` entfernt. `WorkflowListFilters.tsx`: „Zur gezielten Suche"-Link entfernt. `PersonWorkflowHistoryPage.tsx`: Breadcrumb `/search` → `/workflows`. `useRoleAwareNavigation.ts`: `workflowSearch`-Action, `magnifyingGlassIcon` und `surface`-Parameter aus `collectActionKeys` entfernt (war nur fuer workflowSearch-Guard noetig). `roleModel.ts`: `workflowSearch` aus `AppFeature` und `canAccessFeature`-Switch entfernt. `WorkflowSearchPage.test.tsx`: 6 Redirect-Tests (param-Erhalt, leerer Pfad, Einzel-/Mehrfach-Params). Build gruen. 280/280 Tests gruen. **Z18 vollstaendig abgeschlossen.** | Kein offener Risikopfad. `workflowSearch` als Feature-Key vollstaendig entfernt — keine Abwaertskompatibilitaet noetig (intern nur). |
| 2026-05-08 | CLAUDE-Z18-S3 (FRONTEND BEHAVIOR BATCH B) | done | `web/src/hooks/useSupervisorWorkflows.ts` (neu), `web/src/pages/SupervisorStepPage.tsx`, `web/src/pages/WorkflowListResults.tsx`, `web/tests/WorkflowListResults.test.tsx` (neu), `web/tests/WorkflowListPage.test.tsx`, `FRONTEND_TODO.md`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **F6**: Neuer Hook `useSupervisorWorkflows` (`useQuery` fuer Queue mit `staleTime: 60s`, `select`-Filter auf `waiting_for_supervisor`, `useMutation` mit `queryClient.invalidateQueries` nach Save) und `useSupervisorStep` (`useQuery` fuer Requirements, `enabled: uid !== null`, `staleTime: 30s`). `SupervisorStepPage` vollstaendig auf React Query umgestellt: kein manuelles `setQueueLoading`/`setQueueError`/`setIsSaving` mehr; Selections-Initialisierung per `useEffect` + `useRef`-Guard (`initializedForUid`), sodass Refetches die Nutzereingaben nicht ueberschreiben. **F7**: Auto-Select-Fallback `?? sortedRows[0] ?? null` aus `WorkflowListResults` entfernt; `useEffect([rows, selectedWorkflowUid])` ergaenzt: setzt `selectedWorkflowUid` auf `null`, wenn die uid nach einem Seitenwechsel nicht mehr in `rows` enthalten ist. Bestehender Test `WorkflowListPage.test.tsx` (auto-select-Erwartung) auf neues Verhalten aktualisiert. Neu: `tests/WorkflowListResults.test.tsx` mit 4 Tests (kein Auto-Select, Klick zeigt Preview, Reset bei Seitenwechsel, Persistenz bei Refresh). Build: gruen (4.68s). Tests: 278/278 gruen. | Z18-F4 (HIGH — WorkflowSearchPage 1000er-Cap) verbleibt als naechste Prioritaet. |
| 2026-05-08 | CLAUDE-Z18-S2 (FRONTEND CLEANUP BATCH A) | done | `web/src/styles/components.css`, `web/src/utils/employmentStatus.ts` (neu), `web/src/pages/PeopleDirectoryPage.tsx`, `web/src/pages/PersonWorkflowHistoryPage.tsx`, `web/src/pages/CreateWorkflowPageSections.tsx`, `web/src/pages/RotationPlanningPage.tsx`, `web/src/components/workflows/TargetPersonSelection.tsx`, `web/src/components/rotation/RotationStationFormCard.tsx`, `web/src/components/admin-config/AdminDepartmentsSection.tsx`, `web/src/components/admin-config/AdminUsersSection.tsx`, `web/src/components/admin-config/AdminResponsibilitiesAndRequirementsSection.tsx`, `web/src/components/admin-config/AdminWorkspaceNavigation.tsx`, `FRONTEND_TODO.md`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `web/README.md` | Z18-S2 (Batch A) abgeschlossen. **F1**: Neue `.card-form`-Klasse in `components.css` (flex-column, kein cursor/hover); `.dashboard-card` auf 5 Formular-Containern entfernt (RotationStationFormCard, RotationPlanningPage, AdminResponsibilitiesAndRequirementsSection, AdminDepartmentsSection, AdminUsersSection). **F2**: Ghost-Klasse `rotation-form-card` aus allen 3 TSX-Dateien entfernt (gebuendelt mit F1). **F3**: `web/src/utils/employmentStatus.ts` mit `formatEmploymentStatus`, `formatDirectoryLinkStatus`, `formatDirectoryLinkStatusShort`, `getEmploymentStatusClass` erstellt; Duplikate aus 5 Dateien (PeopleDirectoryPage, PersonWorkflowHistoryPage, CreateWorkflowPageSections, TargetPersonSelection, RotationPlanningPage) entfernt. **F5**: `scope="col"` auf alle 6 `<th>` in `PeopleDirectoryPage` gesetzt. **F8**: `offset` wird als URL-Param `page` (1-basiert) persistiert und beim Mount initialisiert — konsistent mit WorkflowListPage-Muster. **F9**: `aria-controls={subNavId}` auf Area-Tab-Buttons in `AdminWorkspaceNavigation` verdrahtet; Sub-Nav-Container erhaelt passendes `id`; `AdminWorkspaceSubNavigation` nimmt optionales `id`-Prop. Build: gruen (5.40s). Tests: 274/274 gruen. | Z18-F4 (HIGH — WorkflowSearchPage 1000er-Cap) verbleibt als naechste Prioritaet. Z18-F6 (SupervisorStepPage RQ) und Z18-F7 (Split-Workspace Auto-Select) ebenfalls noch offen. |
| 2026-05-08 | DOCS-Z18-FOLLOW-UP | done | `MEMORY.md`, `CODE_REVIEW.md`, `CODEX_SYNC.md` | Nachpruefung des von Claude dokumentierten Z18-Review-Standes: `MEMORY.md` war noch auf dem Vor-Z18-Stand („kein aktiver Zyklus / Z17 als naechstes") und wurde auf den echten aktiven Fokus Z18 mit Verweis auf `FRONTEND_TODO.md` gezogen. In `CODE_REVIEW.md` wurde die Frontend-Gesamtbewertung sprachlich korrigiert: statt einer falschen 3/3-Zusammenfassung spiegelt sie jetzt die tatsaechlichen Z18-Prioritaeten (1 HIGH, 5 MEDIUM, 3 LOW) und macht den praktischen Schwerpunkt Search-Performance, Accessibility und UI-Semantik sichtbar. | Kein neuer fachlicher Review-Befund. Offener naechster Schritt bleibt die Priorisierung der ersten Z18-Umsetzungsslices. |
| 2026-05-08 | CLAUDE-Z18-S1 (FRONTEND FULL REVIEW) | done | `FRONTEND_TODO.md`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z18-S1 abgeschlossen. Vollstaendiger Frontend-Review unter Enterprise-/Admin-Frontend-Leitplanken (Kontrast, Labels, Feedback, Responsive, Light/Dark-Konsistenz). Keine Produktivcode-Aenderungen. 9 priorisierte Findings: Z18-F1 (dashboard-card Cursor/Hover auf Formular-Container, MEDIUM), Z18-F2 (rotation-form-card Ghost-Klasse, LOW), Z18-F3 (formatEmploymentStatus 4x dupliziert, MEDIUM), Z18-F4 (WorkflowSearchPage limit 1000 ohne Paging, HIGH), Z18-F5 (PeopleDirectoryPage th ohne scope=col, MEDIUM), Z18-F6 (SupervisorStepPage manuelles async statt React Query, MEDIUM), Z18-F7 (Split-Workspace auto-select ohne Nutzer-Geste, MEDIUM), Z18-F8 (PeopleDirectoryPage offset nicht in URL, LOW), Z18-F9 (AdminWorkspaceNavigation aria-expanded ohne aria-controls, LOW). Z17-S2 wurde als Z18-F1 aufgenommen und aus Z17 rausgezogen. | Kein offener Risikopfad aus Z18-S1 selbst. Groesster Handlungsbedarf: Z18-F4 (SearchPage Perf). Empfohlene erste Slices: Z18-F3 (reines Refactor, kein Seiteneffekt) oder Z18-F5 (Accessibility, minimaler Aufwand). |
| 2026-05-08 | CLAUDE-Z17-S1 (THEME-LEAK card-primary) | done | `web/src/styles/components.css`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `web/README.md` | Z17-S1 abgeschlossen. Theme-Leak: `.card-primary` nutzte `background: var(--surface-hero-background)` (dunkelblauer Verlauf `#103a8a → #2563eb → #3b82f6`, auch im Light Mode). Betroffen waren 5 Stellen: `RotationStationFormCard.tsx`, `RotationPlanningPage.tsx`, `AdminResponsibilitiesAndRequirementsSection.tsx`, `AdminDepartmentsSection.tsx`, `AdminUsersSection.tsx`. Vertragsentscheidung: `--surface-hero-background` ist ausschliesslich fuer Hero/Banner-Sections reserviert; `.card-primary` nutzt jetzt `--surface-focus-background` (soft brand tint, theme-konsistent: helles Blau im Light Mode, dunkel mit Brand-Toenung im Dark Mode). Ein einziger CSS-Fix korrigiert alle 5 Stellen. Page-Grammar-Kommentar in `components.css` aktualisiert, um den Token-Vertrag zu dokumentieren. Build gruen. | `rotation-form-card` ist eine verwaiste Klasse ohne CSS-Regeln — harmlos, kein Scope-Z17. `dashboard-card` hat `cursor: pointer` und Hover-Effekte, was fuer Formular-Container semantisch falsch ist; eigener kuenftiger Slice, nicht Z17-S1. |
| 2026-05-08 | DOCS-Z16-CLOSEOUT | done | `CODE_REVIEW.md`, `CODE_REVIEW_ARCHIVE.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Doku-Closeout Z16: detaillierter Z16-Block aus `CODE_REVIEW.md` nach `CODE_REVIEW_ARCHIVE.md` verschoben; aktive Steuerdateien auf Abschlusszustand zurueckgefuehrt; `TODO.md` auf „kein aktiver Zyklus offen" bereinigt; `MEMORY.md` Z16-spezifische Watchouts entfernt, dauerhafte Guardrails erhalten. Kein Code-Change. | Z16-S4 (Automation-Snapshot `snapshotAt`) bleibt deferred — Produkt-Entscheidung Snapshot-Persistenz ausstehend. Naechster Schritt: Z17 — Codex priorisiert. |
| 2026-05-08 | CLAUDE-Z16-S3 (PEOPLE-DIRECTORY FE) | done | `web/src/auth/roleModel.ts`, `web/src/types/workflow.ts`, `web/src/services/peopleApi.ts`, `web/src/services/queryKeys.ts`, `web/src/services/queries/peopleQueries.ts`, `web/src/navigation/useRoleAwareNavigation.ts`, `web/src/App.tsx`, `web/src/pages/PeopleDirectoryPage.tsx` (neu), `web/src/pages/PersonWorkflowHistoryPage.tsx`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `web/README.md` | Z16-S3 abgeschlossen. Neues `AppFeature "peopleDirectory"` in `roleModel.ts` (HR + Admin); `canAccessFeature` liefert `true` genau fuer `hasHrRole \|\| hasAdminRole`. Neue `PeopleDirectoryPage` unter `/people`: Sucheingabe (debounced, URL-param `q`), paginated Tabelle (50 pro Seite) aus `GET /admin/people`, Status-Badge fuer Beschaeftigungsstatus, Verzeichnis-Link-Status-Spalte, jede Zeile verlinkt auf `/people/:personId`. Navigationseintrag "Mitarbeiter" fuer HR + Admin im Header + Dashboard. Breadcrumb in `PersonWorkflowHistoryPage` differenziert jetzt nach Feature-Zugang: HR/Admin sehen `/people` als Ruecklink, Manager/Reader sehen `/search` — kein Link auf einen Bereich, den sie nicht oeffnen duerfen. Neuer Query-Key `people.directory`, Hook `usePeopleDirectory`, API-Funktion `getPeopleDirectory`. Lint: 2 pre-existing Fehler (PersonaSwitcher + useActiveView aus Z15), keine neuen. Build: gruen, 5.15s. | pre-existing Lint-Fehler in `PersonaSwitcher.tsx` (react-refresh) und `useActiveView.ts` (unused-disable) stammen aus Z15; nicht Z16-Scope. Z16-S4 (Automation-Snapshot) bleibt deferred — Produkt-Entscheidung ausstehend. |
| 2026-05-08 | CLAUDE-Z16-S1 (Z16 EROEFFNET + S1 DONE) | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `KauthWorkflow/Architektur/Entscheidungen.md` | Z16 eroeffnet als aktiver Zyklus. Thema: Mitarbeiterakte (`/people/:personId`) existiert als vollwertige 360°-Akte, hat aber weder einen eigenen Navigationseinstieg noch einen sauberen Permission-Vertrag. Z16-S1 abgeschlossen: vollstaendige Inventur des Ist-Zustands (Route/Nav/Einstiege, BE-Zugriffsvertrag, Identity/Directory-Vertrag) und klare Vertragsentscheidungen fuer Z16: (1) eigene `/people`-Listenseite + Navigationseintrag fuer HR + Admin JA; (2) neues FE-Feature `peopleDirectory`, BE-Policy `CanAccessPeopleDirectory` (Admin + HR); (3) `/people/search` von `CanCreateWorkflow` auf `CanAccessWorkflowOverview` korrigieren; (4) Person bleibt fachlicher Anker, `directoryIdentityId` bleibt getrennt; (5) Automation-Snapshot braucht `snapshotAt`-Zeitstempel (heute fehlend). Slice-Plan Z16: S2 BE-Permission + Admin-People-Endpunkt (offen), S3 FE-Navigation + PersonSearchPage (offen), S4 Automation-Snapshot (deferred). Keine Code-Aenderung in `web/src` oder `api/API`. | Naechster Schritt: Z16-S2 mit `--model claude-sonnet-4-6 --effort medium` — BE-Permission-Vertrag vor FE. `/people/search` von `CanCreateWorkflow` auf `CanAccessWorkflowOverview` umstellen ist breaking fuer Nutzer, die zwar `CanAccessWorkflowOverview` nicht, aber `CanCreateWorkflow` haben (Manager ohne ViewAll/ViewDepartment) — muss geprueft werden ob das Praxisfaelle trifft. |
| 2026-05-08 | MD-CLEANUP-Z15-ARCHIVE | done | `TODO.md`, `CODE_REVIEW.md`, `CODE_REVIEW_ARCHIVE.md`, `DOCS_CONTROL.md`, `CLAUDE_CONTROL.md`, `CODEX_SYNC.md` | Aktive Steuerdateien erneut auf ihren eigentlichen Zweck zurueckgezogen: abgeschlossene Z14-/Z15-Detailbloecke aus `TODO.md` entfernt, `CODE_REVIEW.md` von der ausfuehrlichen Z15-Darstellung bereinigt und Z15 nach `CODE_REVIEW_ARCHIVE.md` verschoben. Gleichzeitig die Orchestrator-Regel verbindlich nachgezogen: Wenn Codex Claude steuert, muessen abgeschlossene Slices/Zyklen aus aktiven Markdown-Steuerdateien entfernt und in Archiv-/Statusdateien ueberfuehrt werden statt dort liegen zu bleiben. | `KauthWorkflow/Stand/Code-Review-Status.md` bleibt bewusst als ausfuehrlicher menschlicher Statusspiegel bestehen; falls auch dieser kuenftig staerker verdichtet werden soll, braucht das einen separaten Cleanup-Durchgang. |
| 2026-05-08 | DOCS-Z15-CLOSEOUT | done | `MEMORY.md`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md` | Abschluss-Cleanup nach den Claude-Z15-Commits: `MEMORY.md` von „Z15 aktiv / S3 offen" auf den echten Ist-Stand „Z15 vollstaendig abgeschlossen, kein aktiver Folgezyklus" gezogen; in `CODE_REVIEW.md` den Z15-Block von „Aktiver Zyklus" auf „Abgeschlossener Zyklus" umbenannt und die Archiv-Aussage wieder mit dem echten Zustand synchronisiert (`CODE_REVIEW_ARCHIVE.md` enthaelt weiterhin Z8–Z14, nicht Z15). `TODO.md` ebenfalls auf „kein aktiver Folgezyklus offen" bereinigt. | `web/vite.config.ts` bleibt weiterhin als uncommittete lokale Aenderung ausserhalb dieser Review-/Frontend-Arbeit liegen. |
| 2026-05-08 | CLAUDE-Z15-S3 (PERSONA-SWITCHER) | done | `web/src/components/dashboard/PersonaSwitcher.tsx` (neu), `web/src/styles/dashboard.css`, `web/src/components/dashboard/DashboardOverview.tsx`, `web/tests/testUtils.tsx`, `web/tests/PersonaSwitcher.test.tsx` (neu), `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z15-S3 abgeschlossen. Neuer `PersonaSwitcher` rendert nur wenn `capabilities.hasMultipleRoles === true`; leitet verfuegbare Optionen ueber `buildPersonaOptions(capabilities)` aus den tatsaechlichen Rollencapabilities ab (keine erfundenen Personas); schreibt ueber `setActiveView` aus `useCurrentUser`-Context; aktive Ansicht wird per `aria-pressed` und `btn-primary` markiert; Single-Role-Nutzer sehen nichts. `DashboardOverview` rendert `<PersonaSwitcher />` als ersten Abschnitt im `content-stack`. `testUtils` um optionale `activeView?: DashboardPersona` und `setActiveView?` in `RenderOptions` erweitert. 12 neue Tests (4 Unit `buildPersonaOptions`, 8 Komponenten-Tests inkl. `fireEvent`-Click-Assertion); 274/274 Tests gruen. **Zyklus 15 vollstaendig abgeschlossen; kein aktiver Folgezyklus offen.** `web/vite.config.ts` weiterhin nicht committet. | Kein aktiver Folgezyklus. Browser-Smoke nicht ausfuehrbar (kein Browser-Kontext verfuegbar); FE-Tests sind gruen. Styling (`persona-switcher` in `dashboard.css`) haengt am Designsystem; bei Designaenderungen Klassen pruefen. `web/vite.config.ts` bleibt weiterhin als ungcommittete lokale Aenderung. |
| 2026-05-08 | CLAUDE-Z15-S2-FOLLOW-UP (DASHBOARDCONTEXT-RESTFEHLER) | done | `web/src/navigation/useRoleAwareNavigation.ts`, `web/tests/useRoleAwareNavigation.test.ts` | Schmaler Follow-up nach Z15-S2-Review-Finding: `dashboardContext` in `useRoleAwareNavigation` lieferte fuer Mehrrollen-Nutzer weiterhin pauschal „Ihre Arbeitsbereiche" statt den zur effektiven `activeView` passenden Titel. Ursache: `hasMultipleRoles`-Guard hat die Persona-Zweige nie erreicht. Fix: Pure-Funktion `deriveNavigationContext(persona: DashboardPersona)` extrahiert; `dashboardContext`-useMemo liest nun direkt `dashboardPersona` (= `activeView`) ohne `hasMultipleRoles`-Sonderfall. Dependency-Array von `[caps.dashboardPersona, caps.hasMultipleRoles]` auf `[dashboardPersona]` bereinigt. 7 neue Tests (alle Personas + zwei Mehrrollen-Integrationen); 13/13 gruen. Commit: `eb5424f`. `web/vite.config.ts` weiterhin nicht commitet. | S3 (sichtbarer Switcher) bleibt naechster offener Schritt. |
| 2026-05-08 | CLAUDE-Z15-S2 (OVERRIDE-STELLEN) | done | `web/src/auth/roleModel.ts`, `web/src/auth/useCurrentUser.ts`, `web/src/auth/CurrentUserContext.tsx`, `web/src/navigation/useRoleAwareNavigation.ts`, `web/tests/testUtils.tsx`, `web/tests/roleModel.test.ts`, `web/tests/useRoleAwareNavigation.test.ts` (neu), `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z15-S2 abgeschlossen. Beide Override-Stellen aus Z14-1.1 gemeinsam auf `useActiveView` umgestellt. (1) `roleModel.ts:210` `getDefaultRoute` nimmt jetzt optionales `activePersona?: DashboardPersona`-Param; der `hasMultipleRoles`-Shortcut auf `/` entfernt; Persona-spezifisches Routing fuer admin → `/admin/config`, manager → `/supervisor`, worker → `/tasks/my` gilt jetzt auch fuer Mehrrollen-Nutzer. (2) `useRoleAwareNavigation.ts:252` `dashboardPersona` wird nicht mehr lokal aus `capabilities.hasMultipleRoles` auf `"generic"` kollabiert, sondern liest direkt `activeView` aus dem Context. (3) `CurrentUserContext.tsx` verdrahtet `useActiveView(currentUser?.username ?? null, capabilities)` und reicht `activeView` + `setActiveView` durch den Context; `defaultRoute` haengt jetzt an `activeView`. (4) `useCurrentUser.ts` Context-Typ um `activeView: DashboardPersona` + `setActiveView` erweitert. (5) `testUtils.tsx` erhaelt `activeView: deriveDefaultPersona(capabilities)` + `setActiveView: () => undefined`. Storage-Schluessel ist `kauth.activeView.<username>` (kein `personId` im `/me`-Modell). 6 neue Tests in `roleModel.test.ts`, 6 neue Tests in `useRoleAwareNavigation.test.ts`; 255/255 Tests gruen. **Fachlicher Fehler behoben: Mehrrollen-Nutzer sehen nach Login ihre Default-Persona statt `generic`.** | ~~`dashboardContext` zeigt noch „Ihre Arbeitsbereiche"~~ → behoben in Z15-S2-FOLLOW-UP (Commit `eb5424f`). S3 (sichtbarer Switcher) ist naechster offener Schritt. |
| 2026-05-08 | CLAUDE-Z15-S1 (Z15 EROEFFNET + S1 DONE) | done | `web/src/hooks/useActiveView.ts` (neu), `web/tests/useActiveView.test.ts` (neu), `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z15 als aktiver Implementierungszyklus eroeffnet auf Basis Z14-1.3; Z15-S1 abgeschlossen. Neuer Hook `useActiveView(personId, capabilities)` in `web/src/hooks/useActiveView.ts`: Fallback-Kaskade `localStorage → Default-Persona (Vorrangskette admin > hr > manager > worker > reader > generic) → generic`; Persistenzschluessel `kauth.activeView.<personId>`; Setter mit Validierung gegen aktuelle Capabilities; SSR-sicher (localStorage nur clientseitig per `typeof window` Guard). Exportierte Pure-Funktionen `deriveDefaultPersona` und `isValidActiveView` fuer direktes Unit-Testing. Vitest-Mock fuer localStorage (`vi.stubGlobal`) notwendig, da jsdom-Konfiguration `.clear()` nicht vollstaendig exponiert. 24/24 Tests gruen. `useRoleAwareNavigation.ts`, `roleModel.ts`, Loader, Query-Key, `DashboardOverview`-Schalter und `loadGenericInsights` wurden in S1 **nicht** angefasst — Slice I ist sicht-konsumentenfrei. `web/vite.config.ts` war lokal veraendert und wurde bewusst nicht committet. **Praezisierung fuer S2:** Das `/me`-Modell hat kein `personId`-Feld. In S2 wird `username` als user-gebundene Identitaet fuer den Storage-Schluessel verwendet (`kauth.activeView.<username>`). Der Hook-Parameter `personId: string | null` bleibt semantisch korrekt — er nimmt die beste verfuegbare Nutzeridentitaet entgegen. | Z15-S2 muss `useRoleAwareNavigation.ts:253` und `roleModel.ts:210` **gemeinsam** umstellen — nicht einzeln, sonst divergieren Sicht und Login-Routing. `capabilities`-Stabilitaet im Provider beachten (ist per `useMemo` stabil, aber Z15-S2-Tests benoetigen passende Provider-Wrappers). Persistenz bleibt client-only (Cross-Device-Hebung ausserhalb Z15). |
| 2026-05-07 | CODE-REVIEW-ARCHIVE-CLEANUP | done | `CODE_REVIEW.md`, `CODE_REVIEW_ARCHIVE.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | `CODE_REVIEW.md` wieder auf seine eigentliche Rolle als kompakte aktive Steuerdatei reduziert: Z14-Detailebene aus der aktiven Datei entfernt, Archivstatus auf Z8 bis Z14 angehoben, nur Gesamtbewertung, offene zyklusuebergreifende Befunde und grobe Historie bleiben live. `CODE_REVIEW_ARCHIVE.md` um Zyklus 14 ergaenzt (Inventur, Vertrags-/UX-Entscheidung, Folge-Slice-Plan). Verweise in `TODO.md`, `MEMORY.md` und `KauthWorkflow/Stand/Code-Review-Status.md` auf das Archiv umgebogen, damit keine Detail-Verweise mehr ins aktive Review-File zeigen. | Der menschliche Status-Spiegel in `KauthWorkflow/Stand/Code-Review-Status.md` bleibt bewusst ausfuehrlicher als `CODE_REVIEW.md`; wenn auch dieser Spiegel spaeter stark gekuerzt werden soll, braucht das einen eigenen Cleanup-Durchgang statt stiller Nebenbei-Verdichtung. |
| 2026-05-07 | CLAUDE-Z14-1.3 (Z14 ABGESCHLOSSEN) | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Z14-1.3 Slice-Plan Folgezyklus abgeschlossen; Zyklus 14 damit vollstaendig durchgeplant und geschlossen. Drei Umsetzungsslices in fester Reihenfolge geschnitten: **Slice I** „aktive Ansicht" als reines Datenmodell + Persistenz + Fallback (neuer Hook, `localStorage`-Schluessel `kauth.activeView.<personId>`, Fallback-Kaskade aktive → Default → `generic`; **keine** Sicht-Konsumenten umgestellt; weder `useRoleAwareNavigation.ts:253` noch `roleModel.ts:210` angefasst); **Slice II** zwei Override-Stellen aus Z14-1.1 (`useRoleAwareNavigation.ts:253` + `roleModel.ts:210`) gemeinsam auf die neue Quelle umstellen, sodass Mehrrollen-Nutzer wieder ihre Default-Sicht und das passende Login-Routing bekommen; **Slice III** sichtbarer Persona-Switcher nur fuer `hasMultipleRoles === true`. Reihenfolge-Begruendung: Datenmodell zuerst → vermeidet doppeltes Umbiegen; Routing/Sicht in der Mitte → Vertragspflicht „beide Overrides aus derselben Quelle" wird im selben Slice erfuellt; UI zuletzt → Slice III ist Komfort, kein Korrekturpfad. Pro Slice Risiken benannt (SSR/Hydration in I, Test-Provider in II, A11y/Wording in III), Testfokus konkret (Vitest-Unit fuer Hook in I; `roleModel.test.ts` erweitern in II; Komponententest in III). Pro Slice Empfehlung: `claude-sonnet-4-6` + `--effort medium`; Slice III darf bei breiterer UX-Skizze auf `claude-opus-4-7` eskalieren. Bewusst NICHT in Slice I: UI-Element, Aenderung an Override-Stellen, Loader, Query-Key, drei `DashboardOverview`-Schalter, Backend, Server-Persistenz, Telemetrie. Z14 als Zyklus geschlossen; CODE_REVIEW.md Stand-Zeile, Zyklus-Historie und Tabelle gespiegelt; Status-Spiegel `KauthWorkflow/Stand/Code-Review-Status.md` mit „Abgeschlossener Zyklus 14" nachgezogen. **Doku-only, keine Code-Aenderung in `web/src` oder `api/`.** | Folgezyklus erst starten, wenn Codex Slice I per CLI mit `--model claude-sonnet-4-6` und `--effort medium` beauftragt — nicht stillschweigend in Z14 vorgreifen. Slice II muss die zwei Override-Stellen gemeinsam versorgen, sonst laufen Sicht und Routing auseinander. Persistenz-Hebung nach Backend (Cross-Device) bleibt explizit ausserhalb des Folgezyklus. Vorzugsrichtung A+C aus Z14-1.2 ist Vertragsentscheidung, nicht Implementierungsfreigabe — Codex kann sie vor Slice I noch kippen, ohne den Vertrag neu zu schreiben. |
| 2026-05-07 | CLAUDE-Z14-1.2 | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Z14-1.2 Vertrags-/UX-Entscheidung Mehrrollen-Persona-Behandlung abgeschlossen. Begriffsraster verbindlich: Rolle (persistente Berechtigung, additives Capability-Set), Persona (deterministischer Default aus Vorrangskette `admin > hr > manager > worker > reader > generic`), aktive Ansicht (vom Nutzer beeinflussbare, persistierte Praeferenz, die ausschliesslich Sicht-Konsumenten steuert). Vier Optionen mit Pro/Contra geschnitten: (A) Persona-Switcher mit Default + Persistenz; (B) Aggregations-Persona statt `generic`; (C) Admin-Vorrang fuer Admin+X; (D) explizite Login-Auswahl. Vorzugsrichtung als Vertragsentscheidung: **A als tragende Entscheidung + C als Default-Regel + harte Fallback-Regel auf `generic`**. Vertrag liefert: sichtbarer Schalter nur fuer `hasMultipleRoles === true`; Default aus Vorrangskette; Persistenz in `localStorage` mit person-/user-gebundenem Schluessel (Cross-Device-Hebung ausserhalb Z14); Fallback-Kaskade aktive → Default → `generic`; **getrennte** Versorgung der zwei Override-Stellen aus Z14-1.1 (Dashboard-Sicht in `web/src/navigation/useRoleAwareNavigation.ts:253` *und* Login-Routing in `web/src/auth/roleModel.ts:210` lesen aus derselben Quelle "aktive Ansicht"). Capabilities, Header, Schnellaktionen, Routen-Guards bleiben explizit unberuehrt. Andockpunkte fuer Z14-1.3 ohne Logik-Aenderung: `loadDashboardInsights(persona, options)`, `queryKeys.dashboard.insights(persona, definitionKey)`, drei `DashboardOverview`-Schalter (`isAdminDashboard`, `supportsProcessTypeFilter`, Manager-Liste); `loadGenericInsights` bleibt als Fallback-Pfad. **Doku-only, keine Code-Aenderung in `web/src` oder `api/`.** | Z14-1.3 muss zwei Override-Stellen getrennt versorgen — ein Persona-Switcher reicht nicht automatisch fuer das Login-Routing. Persistenz-Edgefaelle (Rolle entzogen, Fremdwert) muessen sauber durch die Fallback-Kaskade laufen. Vorzugsrichtung A+C ist Vertragsentscheidung, kein Implementierungsfreigabe — Codex kann sie in Z14-1.3 nochmal bestaetigen oder kippen. |
| 2026-05-07 | CLAUDE-Z14-1.1 | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Z14-1.1 Inventur Persona-/Mehrrollen-Kollisionen abgeschlossen. Override-Punkte sauber benannt: `web/src/navigation/useRoleAwareNavigation.ts:253` (Sicht-Kollaps auf `generic`) und `web/src/auth/roleModel.ts:210` (Login-Routing zwingt auf `/`). Vier Sicht-Konsumenten haengen an der effektiven Persona: `DashboardOverview` (Admin-Block, Prozesstyp-Filter, Manager-Mitarbeitendenliste), `DashboardPage` (Seitenkopf + optionaler „Neuen Vorgang"-Button), `useDashboardInsights`/`useDashboardInsightsQuery` (Cache-Schluessel persona-getrieben), Insight-Lader-Switch (`loadDashboardInsights` faellt bei `generic` auf `loadGenericInsights` mit leerem Datensatz). Header-Navigation, Schnellaktionen und Routen-Guards bleiben capability-getrieben und sind nicht betroffen — der Effekt ist eine **Sicht**-Falle, keine **Aktions**-Falle. Generic-Faelle und ihre Nutzerfolge dokumentiert: Admin+X verliert Admin-Betriebsblock; HR+Manager verliert HR-Engpaesse + Manager-Freigaben; Manager+Worker verliert Freigaben + eigene Aufgaben; jeder Mehrrollen-Login landet auf einer leeren Generic-Seite. Vertragsanker fuer Z14-1.2: `loadDashboardInsights(persona, options)` und `queryKeys.dashboard.insights(persona, definitionKey)` cachen bereits pro Persona — ein kuenftiger Switcher dockt hier an, ohne Loader oder Capability-Schicht zu beruehren. | Z14-1.2 muss die Begriffstrennung Rolle/Persona/aktive Ansicht definieren, bevor die Override-Stellen veraendert werden. Zwei Overrides sind unabhaengig zu betrachten (Sicht vs. Login-Routing); ein Persona-Switcher reicht nicht automatisch fuer beide. |
| 2026-05-07 | Z14 ZYKLUSEROEFFNUNG MEHRROLLEN-PERSONA-KOLLISIONEN | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z14 als reiner Review-/Planungszyklus eroeffnet. Thema: Nutzer mit mehreren Rollen (Admin + Fachbereich/Manager/HR) verlieren ihre fachlich erwartete Sicht, weil die Persona-Ableitung sie auf `generic` zwingt. Praktisch: wer als Admin arbeiten will, sieht den Admin-Betriebsblock nicht; wer als Manager arbeiten will, verliert Manager-Aktionen. Trifft die Power-User. Technischer Anker (nicht Befund): `web/src/navigation/useRoleAwareNavigation.ts` setzt bei `hasMultipleRoles === true` deterministisch `dashboardPersona = "generic"`; in `web/src/auth/roleModel.ts` wird `dashboardPersona` aus der hoechsten Rolle abgeleitet (admin > hr > manager > worker > reader) und dann durch die Mehrfachrollen-Regel ueberschrieben. Slice-Plan: Z14-1.1 Inventur aller Stellen mit `dashboardPersona`/`hasMultipleRoles`-Verzweigung; Z14-1.2 Vertrags-/UX-Entscheidung mit Begriffsklaerung Rolle vs. Persona vs. aktive Ansicht und Optionen-Skizze (Switcher + Persistenz / Aggregat statt `generic` / Admin-Vorrang / Login-Auswahl); Z14-1.3 Slice-Plan Folgezyklus. Keine Implementierung in Z14. | Z14 ist Doku-only — kein Code-Change an `roleModel.ts`, `useRoleAwareNavigation.ts`, `DashboardOverview.tsx` oder `DashboardAdminRuntimeHealthBlock.tsx` vor Z14-1.2. Z14 betrifft die Sicht/Persona-Ableitung, nicht das Berechtigungsmodell — Rollen/Permissions bleiben unveraendert. Begriffstrennung Rolle/Persona/aktive Ansicht ist Pflicht und wird in Z14-1.2 verbindlich definiert. R10 (Mobile-Layout Form-Editor) bleibt eigenstaendiger Backlog-Eintrag. |
| 2026-05-06 | Z13-2 IMPLEMENTIERUNG LINUX-HOST-METRIKEN (Z13 ABGESCHLOSSEN) | done | `api/API/Contracts/AdminRuntimeHealthDtos.cs`, `api/API/LifecycleRuntimeSettings.cs`, `api/API/Services/AdminRuntimeHealthService.cs`, `api/API.Tests/AdminRuntimeHealthEndpointsTests.cs`, `api/API.Tests/HealthEndpointsTests.cs`, `api/API.Tests/EntraDirectorySyncServiceTests.cs`, `api/API.Tests/RuntimeSecretConfigurationTests.cs`, `api/API.Tests/StartupValidationTests.cs`, `web/src/types/auth.ts`, `web/src/components/dashboard/DashboardAdminRuntimeHealthBlock.tsx`, `scripts/start-vm.sh`, `compose.prod.yml`, `.env.prod.example`, `KauthWorkflow/Betrieb/Setup.md`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z13-2 abgeschlossen. `HostHealthDto` (uptimeSeconds, loadAverage1m, memTotalBytes, memAvailableBytes, memUsedPercent, rootFsTotalBytes, rootFsFreeBytes, rootFsUsedPercent, severity) als optionales Feld in `AdminRuntimeHealthDto`. `AdminRuntimeHealthService.BuildHostHealthAsync` liest auf Linux `/proc/uptime`, `/proc/loadavg`, `/proc/meminfo` und misst Root-FS via `DriveInfo`; auf Windows oder wenn nicht aktiviert: `null`. Konfiguration: `HOST_RUNTIME_HEALTH_ENABLED` (Default false), `HOST_RUNTIME_PROCFS_PATH` (Default `/proc`), `HOST_RUNTIME_ROOT_PATH` (Default `/`). `scripts/start-vm.sh dev` aktiviert automatisch. `compose.prod.yml`: Env-Vars + readonly Mounts `/proc:/host-proc` + `/:/host-root`. `ComputeOverallSeverity` bekommt optionalen `host`-Parameter. FE: `DashboardAdminRuntimeHealthBlock` rendert „Host / VM"-Kachel nur wenn `health.host != null`. Schwellwerte: RAM warn ≥85%/critical >95%; Root-FS 80%/90%. LoadAverage als `loadAverage1m` (kein cpuPercent-Fake). 50 Tests gruen (8 neue); dotnet build gruen; FE lint exit 0; FE build 4.45s gruen. Zyklus 13 vollstaendig geschlossen. | Naechster Folgezyklus: Codex entscheidet. Host-Block bleibt leer wenn `HOST_RUNTIME_HEALTH_ENABLED` nicht gesetzt (Windows-Dev-Lokal unangetastet). Volume-Mounts in compose.prod.yml sind immer im Stack aktiv — nur wenn Env-Var gesetzt, wird der Block befuellt. Wer `/proc` oder `/` aus anderen Gruenden nicht mounten kann/will: `HOST_RUNTIME_HEALTH_ENABLED=false` reicht als Deaktivierung. |
| 2026-05-06 | Z13-1 ZYKLUSEROEFFNUNG LINUX-HOST-METRIKEN | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z13 eroeffnet als aktiver Zyklus. Thema: bestehender `GET /admin/runtime-health` bekommt optionalen `host`-Block mit Linux-Metriken (Uptime, Load Average 1m, RAM-Auslastung, Root-FS-Auslastung). Scope eng: kein neuer Endpoint, nur Linux, graceful null wenn nicht aktiviert. Aktivierung explizit ueber `HOST_RUNTIME_HEALTH_ENABLED`. Schwellwerte: RAM warn ≥85%/critical >95%; Root-FS identisch zu Storage (80%/90%). Load Average ehrlich als `loadAverage1m` (kein `cpuPercent`-Fake). Slice-Plan: Z13-1 Doku (done), Z13-2 Implementierung (offen). | Z13-2 muss Windows-Dev-Lokal unangetastet lassen; App-Block darf nicht umgebaut werden; bestehende Tests duerfen durch neue Host-Parameter in `ComputeOverallSeverity` nicht brechen. |
| 2026-05-06 | Z12-2.2 FRONTEND ADMIN-DASHBOARD RUNTIME-HEALTH-BLOCK (Z12 ABGESCHLOSSEN) | done | `web/src/types/auth.ts`, `web/src/services/queryKeys.ts`, `web/src/services/adminApi.ts`, `web/src/components/dashboard/DashboardAdminRuntimeHealthBlock.tsx` (neu), `web/src/components/dashboard/DashboardOverview.tsx`, `web/src/styles/admin.css`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z12-2.2 abgeschlossen. Neuer `DashboardAdminRuntimeHealthBlock` als Zone 4 in `DashboardOverview.tsx`, nur gerendert wenn `dashboardPersona === "admin"`. Zeigt: Severity-Badge mit `overallSeverity`; Kachel „API-Prozess" (Uptime formatiert, Managed Heap in KB/MB/GB, HighThreshold-Warnung wenn konfiguriert); neue Kachel „Abhaengigkeiten" (DB-Reachability + Latenz, Auth-Mode-Kurzlabel, Mail-Konfig-Status summarized); Storage-Kacheln (Schreibpfad: Label, usedPercent, belegter/Gesamt-Speicher) nur wenn `storage[]` nicht leer — kein Render fuer leere Liste. `useQuery` intern mit `staleTime: 60_000` / `refetchInterval: 120_000` (live-polling fuer Health-Daten). Wording streng: „Betriebsstatus" (SectionHeader), „API-Prozess", „Abhaengigkeiten", „Schreibpfad: {label}" — kein „Server", „RAM frei", „Disk frei". Panel-CSS-Modifier: `--critical` (neu, fuer critical-only), `--warning` (Warnungen oder Runtime-Concern), `--clear` (alles ok). `AdminOverviewWorkspaceSection` (Administration > Overview) bleibt vollstaendig unveraendert. Neue CSS-Klassen in `admin.css`: `.admin-health-panel--critical`, `.admin-health-head-row`, `.admin-health-severity-badge` (mit Varianten), `.admin-health-meta--runtime`, `.admin-health-load-notice`. Build gruen (4.50s), Lint exit 0; 16 pre-existierende Test-Fehler (aus Z11-F1 AdminListPage-Refactor) vor und nach dem Slice identisch — nicht von Z12-2.2 eingefuehrt. Zyklus 12 vollstaendig abgeschlossen. | Pre-existierende Test-Fehler (16) betreffen adminConfigApi.test.ts, WorkflowBuilderPage, WorkflowSearchPage, RotationPlanDetailPage, CreateWorkflowPage, AdminConfigPage — alle auf Z11-F1 AdminListPage-Stubs zurueckfuehrbar, nicht auf Z12. Storage-Kacheln erscheinen nur wenn `RUNTIME_HEALTH_STORAGE_PATHS` in Production gesetzt ist — ohne Env-Var bleibt `storage[]` leer und kein Storage-Tile wird gerendert. Optionaler Host-/VM-Metrik-Ausbau bleibt bewusst Folgeschritt nach Z12. |
| 2026-05-06 | Z12-2.1 BACKEND RUNTIME-HEALTH ENDPOINT | done | `api/API/Contracts/AdminRuntimeHealthDtos.cs` (neu), `api/API/Services/IAdminRuntimeHealthService.cs` (neu), `api/API/Services/AdminRuntimeHealthService.cs` (neu), `api/API/Endpoints/AdminRuntimeHealthEndpoints.cs` (neu), `api/API.Tests/AdminRuntimeHealthEndpointsTests.cs` (neu), `api/API/LifecycleRuntimeSettings.cs`, `api/API/Extensions/LifecycleServiceCollectionExtensions.cs`, `api/API/Endpoints/AdminEndpoints.cs`, `KauthWorkflow/Betrieb/Setup.md`, `CODE_REVIEW.md`, `TODO.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z12-2.1 abgeschlossen. `GET /admin/runtime-health` admin-only implementiert; `AdminRuntimeHealthService` aggregiert Prozess-Uptime, Managed Heap, Working Set, Thread-Pool, DB-Reachability mit Latenz, Entra-OIDC-Reachability mit Latenz, Mail-Konfig-Status, Directory-Sync-Status + pendingImportsCount, Storage-Auslastung explizit konfigurierter App-Schreibpfade (`RUNTIME_HEALTH_STORAGE_PATHS`). Schwellwerte 1:1 aus Z12-1.2: DB <1000/1000-3000/>3000 ms; Entra <2000/2000-5000/>5000 ms; Heap 75%/90% von `HighMemoryLoadThresholdBytes`; Storage 80%/90% `usedPercent`; Directory 2x/5x Intervall; Mail `disabled`→ok / `sandbox`→warning / `enabled+incomplete+no-sender`→critical. `auth.severity=unknown` fuer mode=none wird fuer `dependencies.severity` neutralisiert (bleibt nicht auf overallSeverity); dev-sim gibt `ok`. Bestehende `/health/*`-Endpunkte unveraendert. 42 neue Tests; 411/411 non-integration-Tests gruen. | Z12-2.2 muss Storage-Kacheln nur rendern wenn `storage[]` nicht leer. Wording in Z12-2.2 zwingend: kein „Server"/„RAM frei"/„Disk frei" im App-Block — nur „API-Prozess: Managed Heap" / „Schreibpfad der Anwendung". `RUNTIME_HEALTH_STORAGE_PATHS` muss in Production-Deployment-Doku gepflegt werden wenn Storage-Kacheln gewuenscht. |
| 2026-05-06 | Z12-1.2 VERTRAGS-SKIZZE FUER ADMIN RUNTIME HEALTH | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z12-1.2 abgeschlossen als reine Doku-Slice. Vertrags-Skizze in `CODE_REVIEW.md` § Z12-1.2 (§ 1 Endpoint+DTO, § 2 Severity-Modell, § 3 Schwellwerte, § 4 FE-Andockpunkt, § 5 Z12-2.x-Abgrenzung). **Endpoint:** ein einziger `GET /admin/runtime-health` admin-only (nicht offen wie `/health/*`); bestehende `/health/live|ready|*`-Endpunkte bleiben unveraendert fuer Caddy-/Container-Probes. **DTO-Familie:** `AdminRuntimeHealthDto` mit `generatedAt`, `overallSeverity`, `application` (`processStartedAt`/`uptimeSeconds` domain `app`; `managedHeapBytes`/`managedHeapHighThresholdBytes`/`workingSetBytes`/`threadPool?` domain `container`), `dependencies` (`database` domain `app`, `auth` domain `app+external`, `mail` domain `app` — jeweils mit eigener Severity), `directory` (`lastSyncAt`/`lastSyncStatus`/`lastError`/`nextScheduledSyncAt`/`pendingImportsCount` domain `app`) und `storage[]` (`label`/`path`/`totalBytes`/`freeBytes`/`usedPercent` domain `container`, nur explizit konfigurierte App-Schreibpfade — keine generische `DriveInfo.GetDrives()`-Enumeration). Domaenen-Tags `app|container|host|external` pro Feld; `host` in Z12 **nicht belegt**, nur als reservierter Tag fuer spaeteren Host-/VM-Metrik-Block dokumentiert. **Severity:** vier Stufen `ok/warning/critical/unknown`; `overallSeverity` = max ueber Sub-DTOs mit `unknown`-Neutralisierung fuer `auth.mode=dev-sim`/`none` und leere `storage[]`-Liste (sonst Dev-Sim-Lokal staendig „unbekannt"). **Schwellwerte deklarativ:** DB <1000 ms `ok` / 1000-3000 `warning` / >3000 `critical`; Entra OIDC <2000 `ok` / 2000-5000 `warning` / >5000 `critical`; Mail aus `mode`+`configurationStatus`; Directory-Sync nach Vielfachem von `SchedulingOptions.Interval` (2x warn / 5x crit); Managed Heap 75/90 % der `HighMemoryLoadThresholdBytes`; Storage `usedPercent` 80/90 %. Aenderungen der Schwellwerte sind ein Vertrags-Update in § Z12-1.2, nicht ein Service-Code-Schritt in Z12-2.1. **`application.workingSetBytes` und `threadPool.*`** in Z12 **nur informativ** (Severity `ok`/`unknown`), keine Schwelle — ohne Container-Sizing keine seriose Heuristik. **FE-Andock:** bestehender `admin-health-panel` in `AdminOverviewWorkspaceSection.tsx` bleibt einziger Anker; Z12-2.2 baut **innerhalb** des Panels (nicht daneben/darueber); SectionHeader darf zu „Betriebsstatus"/„Runtime Health der Anwendung" werden; Bestandskacheln Verzeichnis-Sync, Mail-Versand, Offene Warnungen bleiben semantisch erhalten; neue Kacheln „Anwendung" (Uptime + Managed Heap), „Abhaengigkeiten" (DB + Auth, Mail bleibt eigene Kachel) und „Schreibpfade der Anwendung" (`storage[]`, nur falls Liste nicht leer) ergaenzen. Severity-Badge im Header zeigt `overallSeverity`; bestehende Tone-Helper bleiben fuer Bestandskacheln. **Explizit nicht in Z12-2.1/Z12-2.2:** echte Host-/VM-Metrik (CPU/RAM/Disk Server, Load-Average, `docker stats`/`cgroup`); Prometheus/Grafana/`node_exporter`/`cAdvisor`/OTLP; tiefes Infra-Monitoring; aktiver Mail-Send-Probe als Pflichtbestandteil; Historisierung/Trends/Sparklines; Severity-Alerts/Notifications; Server-Logs; `/health/*`-Aenderungen. Reine Doku-Slice, kein Code-Change. Naechster Schritt: Z12-2.1 beauftragen mit `--model sonnet --effort high` (oder `medium`). | Z12-2.1 darf Schwellwerte aus § Z12-1.2 § 3 nicht eigenmaechtig veraendern — Aenderung ist Vertrags-Update in § Z12-1.2. Storage-Pfade muessen aus expliziter Konfiguration kommen, nicht aus generischer Drive-Enumeration — sonst rutscht „Disk-Free der VM" still in den App-Vertrag. `unknown`-Neutralisierung fuer dev-sim/none-Auth und leere Storage-Liste zwingend, sonst zeigt Dev-Sim staendig „unbekannt". Bestehende `/health/*`-Endpunkte sind tabu (Caddy-Probes). `application.workingSetBytes` und `threadPool.*` in Z12 nur informativ — keine Schwellwerte erfinden. Mail-Probe wird in Z12-2.1 nicht aktiv getriggert; nur bestehende Probe-Resultate werden wiedergegeben (oder `never_run`). |
| 2026-05-06 | Z12-1.1 INVENTUR RUNTIME-HEALTH FUER ADMIN-DASHBOARD | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z12-1.1 abgeschlossen als reine Doku-Slice. Inventur der heute existierenden Health-Signale: Backend-Endpunkte `/health/live` (`{status:ok}`), `/health/ready` (DB-Ping via `SELECT 1`), `/health` (DB + Entra-OIDC-Discovery-URL-Check + Auth-Mode) in `api/API/Extensions/LifecycleApplicationExtensions.cs:127-303`; Admin-UI-Block `admin-health-panel` in `web/src/components/admin-config/AdminOverviewWorkspaceSection.tsx` mit Verzeichnis-Sync-Kachel (Quelle `/admin/directory/status` + `/admin/directory/pending-imports`, DTO `DirectorySyncStatusDto` in `api/API/Services/IDirectorySyncService.cs:58`), Mail-Versand-Kachel (Quelle `/admin/config/notification-email`, Felder `mode`/`configurationStatus`), Warnungen-Kachel (FE-seitige Aggregation in `adminWorkspaceModel.ts`, Kategorien `department_lead`/`department_owner`/`responsibility_user`/`responsibility_department`/`mail_configuration`). Festgestellt: zwei Health-Welten (Backend-Endpunkte vs. UI-Panel) sind heute nicht verbunden — DB- und Auth-Reachability sind nur im Endpunkt sichtbar, nicht im UI. Echte App-/Runtime-Signale (Prozess-Uptime, Managed Heap via `GC.GetTotalMemory`, Working Set via `Process.GetCurrentProcess().WorkingSet64`, Storage-Auslastung App-Schreibpfade via `DriveInfo`) fehlen komplett — weder Backend noch UI. **Drei-Domaenen-Modell als Pflicht-Begriffsraster fuer Z12-1.2 fixiert:** (a) **App-/Runtime-Health** (Prozess-Uptime, Managed Heap, DB-/Entra-/Mail-Reachability, Directory-Sync-Status, Warnungen) = Z12-Scope; (b) **Container-/Volume-Sicht** (Working Set, App-Mount-Punkte) = Z12-Scope, aber als „API-Container"/„Schreibpfad der Anwendung" labeln, **nicht** als „Server"/„RAM frei"/„Disk frei"; (c) **Host-/VM-Metrik** (Host-CPU/RAM/Disk gesamt, Load-Average, `docker stats`/`cgroup`) = bewusst ausserhalb Z12. UI-Wording-Empfehlung: Block heisst „Betriebsstatus" oder „Runtime Health der Anwendung", **nicht** „Server-Status"/„Systemzustand". Speicher als „API-Prozess: Managed Heap"/„API-Prozess: Working Set", Storage als „Schreibpfad der Anwendung". Bestehende Kacheln (Verzeichnis-Sync, Mail, Warnungen) bleiben semantisch wo sie sind; Z12 ergaenzt Anwendung-/Abhaengigkeits-Kacheln, ersetzt nichts. Folgeentscheidung fuer Z12-1.2: jedes DTO-Feld traegt Domaenen-Zuordnung (App/Container/Host) explizit; Severity (`ok`/`warn`/`crit`) deklarativ in Z12-1.2, nicht in Z12-2.1 frei waehlen. Kein Code-Change. Naechster Schritt: Z12-1.2 beauftragen mit `--model opus --effort high`. | App-/Container-/Host-Semantik-Vermischung bleibt zentrale Watchout fuer alle weiteren Z12-Slices (besonders Z12-2.2 UI-Wording: kein „Server"/„RAM frei"/„Disk frei" im App-Block). DriveInfo-Pfadwahl muss in Z12-1.2 explizit auf App-Schreibpfade beschraenkt werden, sonst rutscht „Disk-Free der VM" still in den App-Vertrag. Schwellwerte und Severity-Stufen muessen in Z12-1.2 deklarativ stehen — Z12-2.1 darf sie nicht eigenmaechtig waehlen. |
| 2026-05-06 | Z12-1 ZYKLUSEROEFFNUNG ADMIN-RUNTIME-HEALTH | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Zyklus 12 eroeffnet als aktiver Zyklus. Thema: „Admin-Dashboard-Betriebsblock fuer Runtime-/System-Health-Signale". Produkt-/Scope-Entscheidung: Z12 adressiert **zuerst App-/Runtime-Health** (API/DB/Directory/Mail + einfache Runtime-Metriken Prozess-Speicher/Uptime/Storage), **nicht** vollwertige Host-/VM-Metrik. Bestehender Admin-Health-Begriff (`admin-health-panel` in `web/src/components/admin-config/AdminOverviewWorkspaceSection.tsx` + `/health/live`, `/health/ready`, `/health` aus `api/API/Extensions/LifecycleApplicationExtensions.cs`) bleibt Anker — keine konkurrierende zweite Betriebslogik. Slice-Reihenfolge streng sequenziell: **Z12-1.1** Begriffsklaerung / Vertragsinventur Runtime Health (HIGH/high/opus); **Z12-1.2** Vertrags-Skizze DTO + Schwellwerte + Begriffsabgrenzung App/Container/Host (HIGH/high/opus); **Z12-2.1** Backend Runtime-Health Endpoint + Service (HIGH/medium..high/sonnet); **Z12-2.2** Frontend Admin-Dashboard-Betriebsblock (HIGH/medium..high/sonnet). **Optionaler Host-/VM-Metrik-Ausbau** (CPU/RAM/Disk Server, Container-Health) ist klar als Folgeschritt **ausserhalb** Z12 dokumentiert — eigener Zyklus oder Slice nach Z12-2.2, nur bei konkretem Bedarf und nach Privilegien-/Plattform-Klaerung. Leitplanken: App-/Runtime-Health zuerst (kein Host-/VM-Metrik-Code in Z12); Begriffstrennung App vs. Container vs. Host Pflicht; Z12-1.x reine Doku-Slices; Schwellwerte/Severity in Z12-1.2 deklarativ skizzieren, nicht in Z12-2.1 frei erfinden; Schreibregel anwenden; nach jedem Slice Commit + Doku im selben Pass. Reine Doku-Eroeffnung, kein Code-Change. Naechster Schritt: Z12-1.1 beauftragen mit `--model opus --effort high`. | App-/Container-/Host-Semantik nicht vermischen — zentrale Watchout fuer alle Z12-Slices. Wer in einem Z12-Slice Host-CPU/Disk/RAM mitnimmt, weicht den Zuschnitt auf. Z12-2.1 darf den Vertrag aus Z12-1.2 nicht eigenmaechtig erweitern; Schwellwerte stehen in Z12-1.2. |
| 2026-05-06 | Z11-F3 P1-ROLLOUT BUILDER-LISTEN (Z11 ABGESCHLOSSEN) | done | `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.AdminConfigOperations.cs`, `api/API/Repositories/PostgresWorkflowRepository.WorkflowDefinitionOperations.cs`, `api/API/Services/IWorkflowAutomationService.cs`, `api/API/Services/WorkflowAutomationService.cs`, `api/API/Endpoints/AdminWorkflowDefinitionConfigEndpoints.cs`, `api/API/Endpoints/AdminProcessConfigEndpoints.cs`, `api/API/Endpoints/AdminAnswerConfigEndpoints.cs`, `api/API.Tests/WorkflowAutomationServiceTests.cs`, `api/API.Tests/AdminAutomationEndpointsTests.cs`, `api/API.Tests/WorkflowLifecycleServiceTests.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `api/API.Tests/PostgresWorkflowRepositoryAdminConfigIntegrationTests.cs`, `api/API.Tests/PostgresWorkflowRepositoryWorkflowDefinitionIntegrationTests.cs`, `web/src/services/adminConfigApi.ts`, `web/src/hooks/useAdminAnswerDefinitionManagement.ts`, `web/src/hooks/useAdminRoleAnswerDefaults.ts`, `web/src/hooks/useAdminTaskTemplateData.ts`, `web/src/hooks/useAdminWorkflowVersionReferenceData.ts`, `web/src/hooks/useAdminWorkflowBuilder.ts`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `FRONTEND_TODO.md` | Sieben scoped Builder-Lese-Endpunkte auf die in F1 etablierte P1-Hull umgestellt (`AdminListPageDto<T>` + `AdminListQuery`): `GET /admin/config/workflow-definitions`, `/admin/config/action-definitions`, `/admin/config/task-templates`, `/admin/config/task-templates/{id}/conditions`, `/admin/config/task-templates/{id}/dependencies`, `/admin/config/answer-definitions`, `/admin/config/role-answer-defaults`. Repository- und Service-Signaturen entlang derselben Kette umgestellt; SQL mit `COUNT(*) OVER()`, `LIMIT/OFFSET`, ILIKE-`search` und festen Sort-Whitelists pro Endpunkt; Pflicht-Scope (`workflowDefinitionId` bzw. `templateId`) bleibt zwingend. Endpoint-Handler nehmen `HttpRequest` und nutzen `AdminListQuery.From(request)`; bei Conditions/Dependencies wurde der Parameter `httpRequest` benannt, um Kollision mit dem bestehenden `request: AdminTaskTemplateConditionCreateRequest` zu vermeiden. Test-Stubs in fuenf Test-Dateien an neue Interface-Signaturen angepasst (Stub-Returns wrappen Listen in `AdminListPageDto<T>` mit `query.Limit/Offset`). Sieben Integration-Test-Aufrufstellen auf neue Signatur umgestellt (`new AdminListQuery { Limit = AdminListQuery.MaxLimit }` + `.Items`-Extraktion). FE-Service-Wrapper (`getAdminTaskTemplates`, `…Conditions`, `…Dependencies`, `getAdminAnswerDefinitions`, `getAdminRoleAnswerDefaults`, `getAdminWorkflowDefinitions`, `getAdminWorkflowActionDefinitions`) liefern jetzt `Promise<AdminListPage<T>>` und akzeptieren `AdminListQueryOptions`; alle Builder-Hooks lesen pragmatisch `page.items` mit `limit: 200`. Schreibpfade, Versionierungs-/Publish-Pfade und der Dependency-Graph-Endpunkt blieben bewusst unangetastet. Verifikation: `dotnet build api/API/API.csproj` gruen, `dotnet build api/API.Tests/API.Tests.csproj` gruen (3 vorbestehende CS8602-Warnungen), `npm run build` gruen (`built in 6.13s`); Unit-Tests 369/369, AdminConfig-Integration 22/22, WorkflowDefinition-Integration 9/9. **Z11 damit vollstaendig geschlossen** — kein neuer Zyklus aus diesem Slice eroeffnet. | F3 ist Vertrags-Rollout, keine sichtbare Paging-/Filter-UI im Builder-Inspector — die nutzt weiterhin `limit: 200` und `page.items`. URL-Filterzustand-Migration bleibt eigenstaendiger UI-Slice ausserhalb Z11. Dependency-Graph-Endpunkt ist strukturell ein Graph (kein Listen-Vertrag) und bleibt nackt; Schreibpfade und Publish-Pfade sind weiterhin nackte JSON-Antworten ohne Hull. Codex entscheidet, welcher Folgekandidat (A Identity-Listen, C `/admin/directory/identities`, C Gaps/Pending Split, E Notification-Templates, F Rotation, G Runtime-Sub-Resources, H Startable, oder Builder-UI-Slice) der naechste aktive Zyklus wird. |
| 2026-05-06 | Z11-F2 P2-HULL AUDIT-STREAMS | done | `api/API/Contracts/CursorPageDto.cs` (neu), `api/API/Repositories/IUserAuthorizationRepository.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.Permissions.cs`, `api/API/Services/IDirectorySyncService.cs`, `api/API/Services/EntraDirectorySyncService.cs`, `api/API/Endpoints/AdminOrgEndpoints.cs`, `api/API/Endpoints/AdminDirectorySyncEndpoints.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `web/src/services/api/cursorPage.ts` (neu), `web/src/services/adminApi.ts`, `web/src/services/adminConfigApi.ts`, `web/src/hooks/useAdminConfigData.ts`, `web/src/hooks/useAdminPermissionManagement.ts`, `web/src/components/admin-config/adminConfigWorkspaceContentTypes.ts`, `web/src/pages/AdminConfigPage.tsx`, `web/src/components/admin-config/AdminAccessWorkspaceContent.tsx`, `web/src/components/admin-config/AdminConfigWorkspaceSections.tsx`, `web/src/components/admin-config/AdminPermissionsSection.tsx`, `web/src/components/admin-config/AdminDirectorySyncSection.tsx`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `FRONTEND_TODO.md` | `CursorPageDto<T>` + `CursorPageQuery` als P2-Hull eingefuehrt. `GET /admin/auth/audit` und `GET /admin/directory/audit` liefern jetzt `items / nextCursor / hasMore` statt nackter Arrays. Cursor opaque Base64 ueber JSON `{occurredAt, id}`; Keyset-Pagination (`WHERE (created_at < @cursorTs OR (created_at = @cursorTs AND id < @cursorId)) ORDER BY created_at DESC, id DESC LIMIT @limit+1`); `limit+1`-Trick fuer HasMore ohne COUNT. Backend: Interface-Signaturen und Repo/Service-Implementierungen auf `CursorPageQuery` umgestellt; Endpoints nutzen `HttpRequest request` statt `[FromQuery] int? limit`; Test-Stubs angepasst. Frontend: neuer typed Wrapper `cursorPage.ts`; `adminApi.ts`/`adminConfigApi.ts` auf `CursorPage<T>` umgestellt; `useAdminConfigData` akkumuliert bei „Mehr laden" (kein Reset), resettet bei Neuladen; beide Audit-Tabs (`AdminPermissionsSection`, `AdminDirectorySyncSection`) haben „Mehr laden"-Knopf; Bundle-Typen und `AdminConfigPage` um drei neue Felder pro Audit-Tab erweitert; `useAdminPermissionManagement` auf neue Signatur adaptiert. Verifikation: `dotnet build api/API.Tests/API.Tests.csproj` gruen (3 vorbestehende CS8602-Warnungen, 0 Fehler), `npm run build` gruen (`built in 5.00s`, nur vorbestehende Chunk-Size-Warnung). | FE-Consumer nutzen Akkumulations-Muster (append on load-more, replace on reload) — keine Endlos-Loop-Gefahr, aber Cursor-State muss bei Navigation / Tab-Wechsel beachtet werden. P2-Cursor ist opaque; FE darf den String nie parsen. `search`-Parameter fuer Audit-Endpunkte ist bewusst draussen (P2-Vertrag sieht ihn vor, war nicht F2-Scope). |
| 2026-05-05 | Z11-F1 P1-HULL MASTER-DATA/LOOKUPS | done | `api/API/Contracts/AdminListPageDto.cs`, `api/API/Endpoints/AdminOrgEndpoints.cs`, `api/API/Endpoints/WorkflowMasterDataEndpoints.cs`, `api/API/Repositories/IUserAuthorizationRepository.cs`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.AdminReadOperations.cs`, `api/API/Repositories/PostgresUserAuthorizationRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/IWorkflowCatalogService.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `api/API.Tests/WorkflowLifecycleServiceTests.cs`, `api/API.Tests/PostgresWorkflowRepositoryAdminConfigIntegrationTests.cs`, `web/src/services/api/adminList.ts`, `web/src/services/lookupApi.ts`, `web/src/services/adminApi.ts`, `web/src/services/queries/roleQueries.ts`, `web/src/hooks/useRoles.ts`, `web/src/hooks/useWorkflowCreation.ts`, `web/src/hooks/useAdminConfigData.ts`, `web/src/hooks/useAdminWorkflowBuilder.ts`, `web/src/components/dashboard/dashboardInsightsLoaders.ts`, `web/src/components/admin-config/AdminResponsibilitiesAndRequirementsSection.tsx`, `web/src/pages/RotationPlanDetailPage.tsx`, `web/src/pages/WorkflowSearchPage.tsx`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `FRONTEND_TODO.md`, `CODEX_SYNC.md` | Claude war rate-limited, daher Codex-Fallback fuer den bereits begonnenen Slice. `AdminListPageDto<T>` + `AdminListQuery` eingefuehrt; fuenf Endpunkte auf P1 (`items/total/limit/offset`) umgestellt: `/departments`, `/roles`, `/admin/master-data/departments`, `/admin/master-data/positions`, `/admin/master-data/responsibilities`. SQL mit `COUNT(*) OVER()`, `LIMIT/OFFSET`, `search` und fester Sort-Whitelist auf Repo-Ebene. FE-Service-Layer auf typed Page-Huelle umgestellt; bestehende Screens konsumieren vorerst bewusst `page.items` mit `limit: 200`, damit der neue Vertrag live ist, ohne in F1 schon sichtbare Paging-UI nachzuschieben. Tests/Stubs an neue Signaturen angepasst. Verifikation: `dotnet build api/API.Tests/API.Tests.csproj` gruen (3 vorbestehende CS8602-Warnungen), `npm run build` unter `web/` gruen. | F1 liefert den API-/Service-Vertrag, aber noch keine sichtbare Paging-UI. Mehrere aktuelle FE-Consumer nutzen weiter `limit: 200` und lesen nur `items`; echte Pagination/URL-Filter folgen erst in spaeteren Slices. `GET /workflow-definitions/startable` bewusst weiter ausserhalb von Z11-F1. |
| 2026-05-05 | Z11 EROEFFNUNG | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Zyklus 11 eroeffnet als reiner Umsetzungszyklus auf Basis von Z10-1.3. Thema: „Admin-/Master-Data-Listen-Vertraege in Umsetzung". Slice-Reihenfolge verbindlich F1 → F2 → F3: **Z11-F1** P1-Hull `AdminListPageDto<T>` einfuehren + B Master-Data/Lookups (`/departments`, `/roles`, `/admin/master-data/{departments,positions,responsibilities}`) — HIGH/high/opus; **Z11-F2** P2-Hull `CursorPageDto<T>` einfuehren + Audit-Streams (`/admin/auth/audit`, `/admin/directory/audit`) — HIGH/medium..high/sonnet; **Z11-F3** P1 ausrollen + D Builder-Tabs (sieben scoped Endpunkte: `workflow-definitions`, `action-definitions`, `task-templates` + `…/conditions` + `…/dependencies`, `answer-definitions`, `role-answer-defaults`) — HIGH/medium..high/opus. Leitplanken: pro Slice **eine** Hull-Familie, kein Mischen P1/P2; nur Read-/Listen-Vertraege, keine Schreibpfade, keine Composite-DTO-Umbauten, keine Versionierungs-/Publish-Pfade; `limit` Default 50/Max 200 mit Server-Clamp; P2-Cursor opaque (Base64 ueber `(occurredAt, id)`); Schreibregel anwenden; FE-Folgen sind Konsequenz pro Slice — Eintrag in `FRONTEND_TODO.md` mit Trigger F1/F2/F3 erst beim Start des jeweiligen Slices, nicht praeventiv. **Bewusst in Z11 nicht angefasst** mit Begruendung in `CODE_REVIEW.md` § Z10-1.3: A Identity-Listen, C `/admin/directory/identities`, C Gaps/Pending Split, E Notification-Templates, F Rotation, G Runtime-Sub-Resources, H Startable. Naechster Schritt: Z11-F1 beauftragen mit `--model opus --effort high`. Reine Doku-Eroeffnung, kein Code-Change. | F3 setzt P1 aus F1 voraus — Reihenfolge nicht aufweichen. Composite-DTO-Umbau fuer C Gaps/Pending bleibt eigener vorbereiteter Slice nach Z11; nicht in F1/F2/F3 mitnehmen. |
| 2026-05-05 | Z10 ZYKLUSABSCHLUSS | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Formaler Abschluss Z10. „Aktiver Zyklus 10" in allen Steuerdateien auf „Abgeschlossener Zyklus 10" umgestellt; Geplante-Slices-Tabelle von „offen" auf „done (2026-05-05)"; Stand-Zeile in `CODE_REVIEW.md` auf „abgeschlossen, kein aktiver Zyklus"; `MEMORY.md` von Z10-Fokus bereinigt und auf Z11-Vorbereitung ausgerichtet; Vault-Status-Datei entsprechend nachgezogen. Kein Code-Change, keine Eroeffnung Z11 in diesem Slice. | Naechster Schritt ausserhalb Z10: Eroeffnung Z11 auf Basis F1/F2/F3 aus `CODE_REVIEW.md` § Z10-1.3. Codex entscheidet Reihenfolge und Modell-/Effort-Override. |
| 2026-05-05 | Z10-1.3 SLICE-PLAN FOLGEZYKLUS | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Reiner Slice-Plan auf Basis Z10-1.1/1.2, kein Code-Change. Drei Umsetzungsslices fuer Folgezyklus (Z11) priorisiert: **F1** P1-Hull (`AdminListPageDto<T>`) zentral einfuehren + B Master-Data/Lookups (`/departments`, `/roles`, `/admin/master-data/departments`/`positions`/`responsibilities`) — HIGH/high/opus, weil Hotspot #6 aus Z8-1.2, lautester User-Hebel im Erfassungseinstieg, etabliert die Plattform-Hull P1; **F2** P2-Hull (`CursorPageDto<T>`) zentral einfuehren + Audit-Streams (`/admin/auth/audit`, `/admin/directory/audit`) — HIGH/medium..high/sonnet, weil ersetzt heutigen stillen `limit=100`/`limit=50`-Cap und ist eigenstaendige Hull-Familie, kann F1 nicht stoeren; **F3** P1 ausrollen + D Builder-Tabs (sieben scoped Endpunkte: `/admin/config/workflow-definitions`, `/admin/config/action-definitions`, `task-templates` + Conditions/Dependencies, `answer-definitions`, `role-answer-defaults`) — HIGH/medium..high/opus, setzt P1 aus F1 voraus, breitester Wirkungsgrad fuer Builder-Adminerlebnis. Auswahllogik: Hull-Foundation vor Breitenrolle; Spuerbarkeit vor reinem Wachstumsschutz; eine Hull-Familie pro Slice (kein Mischen). FE-Folgen pro Slice als Konsequenz benannt (ein typed Wrapper pro Hull, plus Aufrufer-Refactor); Eintrag in `FRONTEND_TODO.md` erst beim Start des jeweiligen Slices mit Trigger-Kennzeichnung F1/F2/F3. **Bewusst in Z11 noch nicht angefasst** mit jeweiliger Begruendung in `CODE_REVIEW.md` § Z10-1.3 unter „Bewusst in Z11 noch nicht angefasst": A Identity-Listen (`/admin/auth/users|roles|groups|permissions`) — Wachstum vorhanden, aber akuter Audit-Hebel ist in F2; C `/admin/directory/identities` — billiger Mitnahmeschnitt nach F3, gehoert nicht in F1 (Stoffreinheit); C Gaps/Pending Split — Composite-DTO-Umbau ueber mehrere Endpunkte plus FE-Sichtumbau, eigener vorbereiteter Slice; E Notification-Templates — Bestand klein/stabil, nach F3 billiger Mitnahmeschnitt; F Rotation Action-Templates — Bestand klein, FE frisch, kein Trigger; G Runtime-Sub-Resources — pro Instanz, kein Spuerbarkeits-Trigger, F2 etabliert P2-Adapter fuer spaeter; H `/workflow-definitions/startable` — selber Lookup-Charakter wie B, bewusst nicht in F1 gebuendelt um F1 thematisch klar zu halten. Z10 damit inhaltlich abgeschlossen; formaler Zyklusabschluss bzw. Eroeffnung Z11 bewusst **nicht** in Z10-1.3, nur Plan geliefert. Status in TODO/MEMORY/Code-Review-Status nachgezogen. | Z10-1.3 trifft keine Eroeffnungs-Entscheidung fuer Z11 — Codex entscheidet Reihenfolge gegen andere Hebel und ggf. Modell-/Effort-Override pro Slice. Hull-Definitionen (`AdminListPageDto<T>`, `CursorPageDto<T>`) liegen erst nach F1/F2 als Code vor; bis dahin nur Vertragsskizze in `CODE_REVIEW.md` § Z10-1.2. |
| 2026-05-05 | Z10-1.2 VERTRAGS-SKIZZE ADMIN-LISTEN | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Reine Vertrags-Skizze auf Basis Z10-1.1, kein Code-Change. Drei gemeinsame Muster eingefuehrt (statt pro Endpunkt zu erfinden): **P1 Standard Admin Page** (`?limit&offset&search&sort` → `AdminListPageDto<T> { items, total, limit, offset }`), **P2 Cursor Stream** (`?limit&cursor&search` → `CursorPageDto<T> { items, nextCursor, hasMore }`, implizit `(occurredAt desc, id desc)`), **P3 Typeahead Lookup** (`?query&limit` → bare `T[]`, bereits etabliert). Allgemeine Regeln: `limit` Default 50/Max 200 mit Server-Clamp; `search` case-insensitive ueber im Endpunkt fest deklarierte Felder; `sort`-Felder als Whitelist pro Endpunkt mit Tie-Breaker `id`; bei P1 `total` als zusaetzliches `COUNT(*)`, bei P2 bewusst weggelassen; Cursor in P2 opaque (Base64 ueber `(timestamp, id)`). **Pro Block** Tabelle mit Muster + Server-`search`-Felder + Sort-Whitelist + Hull + endpunkt-eigene Filter, plus Praktisch-/Lohnenswert-/Nutzen-Erklaerung und FE-Folge: **A Identity & Permissions** Users/Roles/Groups/Permissions = P1, Audit = P2 (ersetzt heutigen `limit=100`-Cap); **B Master-Data + `/departments`/`/roles`** = gemeinsam P1 mit P3-kompatiblem Default (Erfassung schickt `?search&limit=20`, Pflege blaetterte) — explizit als gemeinsames Muster dokumentiert (Hotspot #6); **C Directory** Groups/Identities = P1 (Identities Upgrade von bestehendem `limit`/`offset`), Gaps/Pending **gesplittet** in Summary-Endpunkt + zwei P1-Listen (Composite-DTO Anti-Pattern), Audit = P2; **D Builder** alle sieben Endpunkte = scoped P1 mit Pflicht-Scope (`workflowDefinitionId` oder `task-template-id`), Conditions/Dependencies bewusst ohne Server-`search`; **E Notification Templates** = P1; **F Rotation Action Templates** = P1 mit explizitem `sortOrder, title`-Default; **G Runtime Sub-Resources** Events/Automation-Jobs = P2 (per Instanz unbeschraenkt, `total` waere irrefuehrend); **H Startable** = P1 mit P3-Default analog B. **Konsolidierte FE-Konsequenz**: nur zwei neue Hull-Adapter (`AdminListPageDto<T>`, `CursorPageDto<T>`) plus eine Refactor-Achse (clientseitige Filter werden Server-Filter, Filterzustand wandert in URL). **Kein** praeventives FE-TODO, **kein** neuer Slice fuer Z10 done markiert. Reihenfolgen-Hinweis fuer Z10-1.3 (informativ): 1) B Departments/Rollen, 2) A-Audit + C-Audit, 3) D Builder; C-Identitaeten als billiger Mitnahmeschnitt nach 1. | FE-Folgen bleiben Konsequenz, nicht TODO — Eintraege in `FRONTEND_TODO.md` erst nach Z10-1.3-Slice-Auswahl mit Trigger-Kennzeichnung. P1-`total`-Aufwand ist in der heutigen Bestandsgroesse vertretbar; Re-Bewertung nur, falls einzelne Endpunkte spaeter unerwartet teuer werden. Cursor-Format in P2 ist **opaque**; FE darf den String nie zerlegen. |
| 2026-05-05 | Z10-1.1 INVENTUR ADMIN-LISTEN | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Reine Inventur (kein Code-Change). Aufgenommen alle GET-Listen-Endpunkte unter Admin-Konfiguration, Directory-Sync, Workflow-Definitionsbestand und Master-Data, denen mindestens eine der Achsen Pagination, Server-`search`, dokumentierter `sort` fehlt. Gegliedert in `CODE_REVIEW.md` § Z10-1.1 nach **A Identity & Permissions** (`/admin/auth/users` `:12`, `/admin/auth/roles` `:31`, `/admin/auth/groups` `:50`, `/admin/auth/permissions` `:69`, `/admin/auth/audit` `:88`), **B Master-Data + Workflow-Lookups** (`/admin/master-data/departments` `:108`, `/admin/master-data/positions` `:127`, `/admin/master-data/responsibilities` `:306`; `/departments` `WorkflowMasterDataEndpoints.cs:12`, `/roles` `:29` — Hotspot #6 aus Z8-1.2), **C Directory-Sync** (`/admin/directory/groups` `:67`, `/admin/directory/identities` `:86` — hat `limit`/`offset` ohne Hull/`search`/`sort`, `/admin/directory/responsibility-gaps` `:107`, `/admin/directory/pending-imports` `:126`, `/admin/directory/audit` `:183`), **D Workflow-Konfiguration / Builder** (`/admin/config/workflow-definitions` `AdminWorkflowDefinitionConfigEndpoints.cs:13`, `/admin/config/action-definitions` `:32`; `/admin/config/task-templates` + Conditions/Dependencies in `AdminProcessConfigEndpoints.cs`; `/admin/config/answer-definitions` und `/admin/config/role-answer-defaults` in `AdminAnswerConfigEndpoints.cs`), **E Notification Templates** (`/admin/notification-templates` `:12`), **F Rotation Action Templates** (`/admin/rotation/action-templates` `:11`), **G Runtime-Subresources** (`/admin/runtime/workflow-instances/{uid}/events` `:65` und `…/automation-jobs` `:85`), **H Startable** (`/workflow-definitions/startable` `WorkflowMasterDataEndpoints.cs:46`). Pro Eintrag: Datei/Symbol, FE-Aufrufer (`adminApi.ts`/`adminConfigApi.ts`/`lookupApi.ts`/`rotationApi.ts`), aktueller Vertrag, fehlende Achsen, Praktisch-/Lohnenswert-/Nutzen-Erklaerung. Zusaetzlich: bereits saubere Listen (`/workflows`, `/workflows/{uid}/audit-log`, `/admin/system/logs`, typeahead-Endpunkte, Notification-Preview-Targets, Rotation-Plan-Audit/Notifications) als informativer Block; bewusst aussen vor: Single-Doc/Aggregat-Endpunkte (`/admin/directory/status`, `/admin/runtime/workflow-instances/{uid}`, `workflow-definition-versions/{id}`, `dependency-graph`, `system/logs/summary`). Kardinalitaets-Annahme statt Messung. FE-Folgen nur als potentielle Notiz benannt — kein praeventives FE-TODO. Naechster Schritt explizit Z10-1.2 (Vertrags-Skizze). | FE-Folgen erst nach Z10-1.2 sichtbar. Inventur basiert auf Annahme zur Wachstumsrichtung, nicht auf Messung — Vertrags-Schnitt soll das in Z10-1.2 nicht ueberbauen. |
| 2026-05-05 | Z10 EROEFFNUNG + REVIEW-SCHREIBREGEL | done | `CODE_REVIEW.md`, `CLAUDE_CONTROL.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | **Schreibregel verankert:** Review-Findings und Slices muessen ab sofort zusaetzlich zum technischen Befund kurz erklaeren, was er praktisch bedeutet (Alltagssicht), warum es sich lohnt (Anlass/Risiko) und was dadurch besser/sicherer/schneller/wartbarer wird. Quelle: `CODE_REVIEW.md` § „Schreibregel fuer Reviews und Findings"; verbindlich auch in `CLAUDE_CONTROL.md` § „Schreibregel fuer Reviews und Findings"; Kurzhinweis in `TODO.md`. **Z10 eroeffnet:** Thema „Master-Data-/Admin-Listen-Wachstum, Pagination-/Such-Vertraege, Query-Kontrakt-Risiken". Reiner Review-/Planungszyklus, keine Code-Umsetzung in Z10. Begruendung mit Bedeutung-/Nutzen-Erklaerung in `CODE_REVIEW.md` § „Aktiver Zyklus 10". Slice-Plan: **Z10-1.1** Inventur (HIGH/high/opus) — alle Admin-/Master-Data-/Directory-Read-Endpunkte ohne Pagination/Suche/Sort-Vertrag, mit Datei/Symbol, FE-Aufrufer, Kardinalitaet, Spuerbarkeit fuer Nutzer; **Z10-1.2** Vertrags-Skizze (HIGH/high/opus) — pro Endpunkt `limit/offset` vs. Cursor, Server-`search`, stabiler `sort`, Antwort-Hull, FE-Adaption-Folgen; **Z10-1.3** Slice-Plan fuer Folgezyklus (HIGH/medium..high/opus). Streng sequenziell. `MEMORY.md` auf Z10 als aktiven Zyklus aktualisiert; Active-Risks fuer Z10 ergaenzt. `KauthWorkflow/Stand/Code-Review-Status.md` mit Z10-Block + Schreibregel-Hinweis nachgezogen. Reine Doku-Aenderung, kein Code-Change. | Vor jeder Umsetzung muessen Z10-1.1 (Inventur), Z10-1.2 (Vertrag) und Z10-1.3 (Slice-Plan) sequenziell stehen. Kein praeventives FE-TODO; FE-Folgen erst beim sichtbaren Trigger. Z10 darf keinen Code-Slice als done markieren. |
| 2026-05-05 | Z9-3 COVERAGE + Z9 ABSCHLUSS | done | `api/API.Tests/EntraDirectorySyncServiceTests.cs` (neu), `api/API.Tests/EntraDirectorySyncOperationsIntegrationTests.cs` (neu), `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Coverage gegen die neuen Interfaces gesetzt und Z9 geschlossen. **Unit-Tests `EntraDirectorySyncService`** (3): `SyncAllAsync_MissingConnectionString_FailsBeforeGraphInit` (Status `failed`, EventKey `directory_sync_missing_connection_string`, kein Graph- und kein Operations-Aufruf), `SyncAllAsync_GraphMissingCredentials_FailsAndLogsEvent` (Stub-`EntraGraphInitStatus.MissingCredentials`, kein `LoadSecurityGroupsAsync`, `AppliedGroupPrefix` aus Override durchgereicht), `SyncAllAsync_GraphFailed_FailsAndLogsErrorWithDetails` (Stub-`Failed` mit ErrorMessage/ExceptionType, EventKey `directory_sync_graph_client_failed`). Stubs `RecordingGraphClient`/`RecordingOperations`/`RecordingSystemEventLog` private im Test-File. **Integration-Tests `EntraDirectorySyncOperations`** (3, `[Trait("Category","Integration")]`, gleiche `PostgresWorkflowRepositoryIntegrationCollection`-Fixture): `UpsertDirectoryIdentitiesBatchAsync_InsertsNewIdentitiesAndReturnsMapping` (RETURNING-Mapping + Spaltenpersistenz inkl. `Normalize`/`ParseDirectoryEmployeeNumber`), `UpsertDirectoryIdentitiesBatchAsync_UpdatesExistingAndDedupsLastWins` (ON CONFLICT DO UPDATE + dedup-last-wins), `InsertGroupMembershipsBatchAsync_InsertsAndIsIdempotent` (Insert + zweiter Aufruf ohne Duplikate + Empty-Array-No-Op). Verifikation: `dotnet build api/API.Tests/API.Tests.csproj` (0 Err, 3 vorbestehende CS8602-Warnings); `dotnet test --filter "FullyQualifiedName~EntraDirectorySyncServiceTests"` 3/3 gruen; `dotnet test --filter "FullyQualifiedName~EntraDirectorySyncOperationsIntegrationTests"` lokal nicht ausfuehrbar (Fixture braucht Docker / Postgres auf 26432; ArgumentException `Docker is either not running or misconfigured` — identisches Gating wie Z8-4.1). Kein Produktiv-Refactor in Z9-3. Z9 damit abgeschlossen — Status in `CODE_REVIEW.md`/`TODO.md`/`MEMORY.md`/`KauthWorkflow/Stand/Code-Review-Status.md` nachgezogen, LQ2-Z3 in den abgeschlossenen Bestand verschoben. | Tiefere Stub-Coverage von `RunGroupSyncAsync`/`RunDirectoryProjectionAsync` ist bewusst nicht in Z9 gelandet — der Service oeffnet weiterhin direkt `NpgsqlConnection`. Ein Connection-Factory-Schnitt ist explizit ausserhalb von Z9. Integration-Tests laufen lokal nur mit Docker oder konfigurierter `ONBOARDING_TEST_CONNECTION_STRING`-Live-DB. |
| 2026-05-05 | Z9-2.3 SYNC-OPERATIONS EXTRAHIEREN | done | `api/API/Services/Directory/IEntraDirectorySyncOperations.cs` (neu), `api/API/Services/Directory/EntraDirectorySyncOperations.cs` (neu), `api/API/Services/EntraDirectorySyncService.cs`, `api/API/Extensions/LifecycleServiceCollectionExtensions.cs`, `api/API.Tests/PostgresWorkflowRepositoryAdminConfigIntegrationTests.cs`, `PROJECT_STRUCTURE.md`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | DB-Batch-/Projection-Helfer aus `EntraDirectorySyncService` in das neue Interface `IEntraDirectorySyncOperations` + Impl `EntraDirectorySyncOperations` (beide unter `API.Services.Directory`) gezogen: `EnsureDirectoryProjectionUserColumnsAsync`, `UpsertDirectoryIdentitiesBatchAsync`, `InsertGroupMembershipsBatchAsync`, `AutoLinkIdentitiesToAppUsersAsync`, `EnsureDirectoryDepartmentsExistAsync`, `UpdateExistingAppUsersFromDirectoryAsync`, `EnsureDevelopmentDefaultGroupMappingsAsync`. Records `DirectoryDepartmentSyncResult`, `DirectoryUserProjectionSample`, `DirectoryUserProjectionResult` jetzt `public` im Sub-Namespace. Service injiziert `IEntraDirectorySyncOperations` per Konstruktor und delegiert in `RunGroupSyncAsync`/`RunDirectoryProjectionAsync`. Sync-Logging (`LogSyncRun`), Activation-State-Update, Admin-/Import-/Audit-Pfade und CPU-Helfer bleiben in der Hauptdatei (Scope-Treue). DI scoped registriert. Reflection-Test in `PostgresWorkflowRepositoryAdminConfigIntegrationTests.cs` umgestellt: kein `typeof(EntraDirectorySyncService).GetMethod(...)` mehr, sondern direkter Aufruf `new EntraDirectorySyncOperations().UpdateExistingAppUsersFromDirectoryAsync(...)`; `using System.Reflection;` raus, `using API.Services.Directory;` rein. Verhaltensgleichheit: Sync-Methoden- und Record-Signaturen 1:1 uebernommen, lediglich `Async`-Suffix ergaenzt; SQL bytes-equivalent. LOC Hauptdatei: ~2030 → 1563 (~470 Z. weniger). | Verhaltensgleichheit weiter nur ueber Build verifizierbar — kein Live-Tenant. Z9-3 ist offen: Integration-/Stub-Coverage gegen die neuen Interfaces. |
| 2026-05-05 | Z9-2.2 GRAPH-ADAPTER EXTRAHIEREN | done | `api/API/Services/Directory/IEntraGraphClient.cs` (neu), `api/API/Services/Directory/EntraGraphClient.cs` (neu), `api/API/Services/EntraDirectorySyncService.cs`, `api/API/Extensions/LifecycleServiceCollectionExtensions.cs`, `PROJECT_STRUCTURE.md`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Graph-Zugriff hinter Adapter-Interface gezogen. Neuer Sub-Namespace `API.Services.Directory`. `IEntraGraphClient` exponiert `InitializeAsync` (Status `Ready`/`MissingCredentials`/`Failed`, mit `ErrorMessage`+`ExceptionType`), `LoadSecurityGroupsAsync`, `LoadGroupMembersAsync`. Rueckgabe als plain DTOs (`EntraSecurityGroup`, `EntraDirectoryUser`), damit `Microsoft.Graph.*` aus der Hauptdatei verschwindet. `EntraGraphClient` kapselt Credential-Resolution ueber `IGraphApplicationConfigurationService`, `ClientSecretCredential`/`GraphServiceClient`-Bau, Pagination und das User-Filter (vorher im Orchestrator als `is not Microsoft.Graph.Models.User`). `EntraDirectorySyncService` konsumiert den Adapter per Konstruktor; `IGraphApplicationConfigurationService` und der direkte Graph-Client-Bau aus `SyncAllAsync` (vorher Z. 61-89) sind raus. `RunGroupSyncAsync` verliert den `GraphServiceClient`-Parameter. `validUsers`-Tupel und `UpsertDirectoryIdentitiesBatch` tragen jetzt `EntraDirectoryUser`. `LoadSecurityGroupsAsync`/`LoadGroupMembersAsync`/`ResolveGraphCredentialsAsync` aus der Hauptdatei entfernt; `using Azure.Identity;` / `using Microsoft.Graph;` / `using Microsoft.Graph.Models;` raus. DI scoped registriert. Eventlog-Eintraege `directory_sync_missing_graph_credentials` und `directory_sync_graph_client_failed` (inkl. `error`/`exceptionType`) verhaltensgleich. LOC Hauptdatei: 2104 → ~2030. | Verhaltensgleichheit nur ueber Build verifizierbar — kein Live-Tenant. Z9-2.3 muss die Reflection-Test-Umstellung sauber halten (Risiko Slice-Bloat). |
| 2026-05-05 | Z9-2.1 PRE-CLEANUP + PHASEN-SPLIT | done | `api/API/Services/EntraDirectorySyncService.cs`, `api/API.Tests/PostgresWorkflowRepositoryAdminConfigIntegrationTests.cs`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Dead-Code raus: `SyncDepartmentLeadAssignmentsFromDirectory` + 4 Sub-Helfer (`LoadDepartmentLeadSyncStatesAsync`, `EnsureDirectoryManagedPersonRecordAsync`, `UpsertDepartmentLeadAssignmentAsync`, `ClearDepartmentLeadAssignmentAsync`) + `LogDirectoryAuditEventAsync` (nur vom Resolver verwendet) + `CreateDepartmentLeadAuditSnapshot` + die zugehoerigen Records (`DepartmentLeadSyncCandidate`/-`State`/-`Summary`/`DepartmentLeadAuditSnapshot`); Single-Row-Helfer `UpsertDirectoryIdentity` (1469) + `InsertGroupMembership` (1511); im Test-Pfad: ganze Test-Methode `DirectorySync_MirrorsUniqueDepartmentLeadIntoDepartmentSettings` + die nur dort genutzten Stubs (`StubGraphApplicationConfigurationService`, `StubSystemEventLogService` in dieser Datei) und das `Microsoft.Extensions.Logging.Abstractions`-using. `SyncAllAsync` in drei Phasen-private-Methoden zerlegt: `RunGroupSyncAsync` (EnsureColumns + LoadGroups + foreach Group/Member-Batch), `RunDirectoryProjectionAsync` (AutoLink + Departments + AppUser-Projection + DevSim-Defaults), `RunActivationAsync` (Activation-States + Skipped-Lead-Log); Counters in mutable nested `SyncRunState`-Klasse. **Noch in derselben Datei**, kein File-Move. `explicitGroupIds`-Parameter dabei von `IReadOnlyList<Guid>` auf `IReadOnlySet<Guid>` angehoben (passt zur tatsaechlich uebergebenen `HashSet<Guid>`-Instanz, behebt CS1503 nach dem Extract). LOC: 2591 → 2104 (~487 Z. weniger). Test-Datei: 1985 → 1851 Z. | Verhaltensgleichheit zu pruefen erst bei Z9-2.2/2.3-Builds. Reflection-Test 177-179 fuer `UpdateExistingAppUsersFromDirectory` bleibt bewusst stehen — Z9-2.3 stellt um oder entfernt. |
| 2026-05-05 | Z9-1.2 EXTRACT-PLAN | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Verbindlicher Extract-Plan fuer den `EntraDirectorySyncService`-Split. Lebendigkeitspruefung im Repo bestaetigt: `SyncDepartmentLeadAssignmentsFromDirectory` (+ 5 Sub-Helfer), `UpsertDirectoryIdentity` (1469), `InsertGroupMembership` (1511) sind alle dead. Schnitt-Endzustand: 3 Files unter neuem `api/API/Services/Directory/`-Namespace — `IEntraGraphClient`/`EntraGraphClient` (2.2), `IEntraDirectorySyncOperations`/`EntraDirectorySyncOperations` (2.3, enthaelt die Z8-2.3-Batch-Helfer als `public`); `EntraDirectorySyncService` bleibt am alten Pfad als duenner Treiber + Admin-/Import-Pfade. Slice-Schnitt: **Z9-2.1** = Pre-Cleanup (Dead-Code-Loeschen + Reflection-Test 103-105 weg) + `SyncAllAsync` in 3 Phasen-private-Methoden zerlegt, ohne File-Move; **Z9-2.2** = Graph-Adapter extrahieren (Microsoft.Graph-`using` raus aus Hauptdatei); **Z9-2.3** = Sync-DB-Operations-Modul extrahieren, Reflection-Test 177-179 auf direkten Aufruf umstellen oder entfernen. Explizit draussen: Admin-Read/Write (Achse 7), Import-Pfad (Achse 8), Mapping-Audit-Helfer, CPU-Helfer, EventLog-Wrapper, Repository-/Connection-Factory-Abstraktion. Test-Strategie: keine neuen Tests in 2.1/2.2/2.3 (Coverage darf den Refactor nicht treiben), volle Coverage in **Z9-3** gegen die dann stabilen Interfaces. Reine Doku, kein Code-Change. | Z9-2.3 muss bei der Reflection-Test-Umstellung aufpassen, das Slice nicht durch Fixture-Aenderungen zu vergroessern — Fallback: Test entfernen, in Z9-3 neu schreiben. `PROJECT_STRUCTURE.md` muss in Z9-2.2 mitgezogen werden, wenn `Services/Directory/` als neuer Sub-Namespace zum ersten Mal Files enthaelt. |
| 2026-05-05 | Z9-1.1 BOUNDARY-INVENTUR | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Boundary-/Split-Inventur fuer `EntraDirectorySyncService` (2591 Z.) abgeschlossen. Dokumentiert: 11 oeffentliche `IDirectorySyncService`-Methoden mit LOC/Aufrufer; externe Aufrufer = `DirectorySyncHostedService` (nur `SyncAllAsync`) + `AdminDirectorySyncEndpoints` (alle Admin-Pfade) + Tests via Reflection; 9 interne Achsen (Sync-Orchestrierung 34-393, Graph-Adapter, DB-Batch-Helfer, Projection/Activation, Sync-Logging/Audit, CPU-Helfer, Admin-Read/Write, Import-Pfad, DepartmentLead-Resolver). Test-Isolations-Hindernisse: Reflection-Zugriffe auf `private SyncDepartmentLeadAssignmentsFromDirectory` (Instance) und `private static UpdateExistingAppUsersFromDirectory` in `PostgresWorkflowRepositoryAdminConfigIntegrationTests.cs:103,177`; `GraphServiceClient` inline in `SyncAllAsync` (kein Adapter-Interface); Z8-2.3-Batch-Helfer `private static` hinter `SyncAllAsync`; doppelter Audit-Kanal (`directory_*`-Tabellen + `_systemEventLogService`); direkte `NpgsqlConnection`-Konstruktion. Wichtige Nebenbefunde: `SyncDepartmentLeadAssignmentsFromDirectory` + Sub-Helfer sind **dead code im Prod-Pfad** — `SyncAllAsync` ruft sie nicht mehr (Z. 306-317 schreibt `directory_department_assignments_skipped`); Single-Row-`UpsertDirectoryIdentity`/`InsertGroupMembership` evtl. ungenutzt nach Z8-2.3 (Z9-1.2 muss Lebendigkeit pruefen). Schnittland-Empfehlung (informativ): Graph-Adapter + DB-Sync-Operations-Modul + duenner `EntraDirectorySyncOrchestrator`; Admin-Read/Write, Import und DepartmentLead-Resolver explizit **nicht** in den ersten Split. Reine Doku, kein Code-Change. | Z9-1.2 muss DepartmentLead-Resolver-Schicksal entscheiden (loeschen vs. isolieren) und Single-Row-Dead-Code-Kandidaten verifizieren, bevor der Schnitt steht. |
| 2026-05-05 | Z9 EROEFFNUNG | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Zyklus 9 eroeffnet. Thema: `EntraDirectorySyncService`-Split / Testbarkeit (LQ2-Z3 aktiviert) als Folge-Hebel aus Z8. Begruendung: Z8-2.3 hat die Group/Member-Last in `SyncAllAsync` per Bulk-Helfer gehaertet, aber `UpsertDirectoryIdentitiesBatch`/`InsertGroupMembershipsBatch` sind `private` hinter dem 2.4k-Z. Service mit Live-Graph + DB — saubere Test-Isolation verlangt File-Split + Graph-Stub. Geplante Slices: **Z9-1.1** Boundary-/Split-Inventur (HIGH/opus) → **Z9-1.2** Extract-Plan File-/Klassen-Schnitt (HIGH/opus) → **Z9-2.1** `SyncAllAsync`-Zuschnitt → **Z9-2.2** Graph-Adapter testbar → **Z9-2.3** DB-Batch-Helfer hinter Operations-Modul → **Z9-3** Coverage (aus Z8-4 verschoben). Reine Doku-Aenderung, kein Code-Change. LQ2-Z3 aus zyklusuebergreifend-offen herausgehoben und auf Z9 referenziert. | Keine FE-Folgen erwartet (Backend-/Timer-Pfad). Naechster Schritt: Z9-1.1 Inventur. |
| 2026-05-05 | Z8 ZYKLUSABSCHLUSS | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `CODEX_SYNC.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | Z8 (Skalierbarkeits- & Last-Haertung) abgeschlossen. Erledigt: #1 Bulk-Lookup (Z8-2.1), #2/#3 Sweep+Apply Batching (Z8-2.2), #4 Entra Group/Member Bulk (Z8-2.3), #5 als false positive verifiziert (Z8-3.1), #7 Recipient-Bulk-Lookup (Z8-3.1), Z8-4.1 Integration-Coverage fuer `LoadActiveUserNotificationRecipientsBulk`. Coverage fuer Z8-2.1/Z8-2.2 liegt bereits in den jeweiligen Umsetzungs-Slices. Bewusst deferred: #8 (`RotationTaskGenerationService.RegenerateDepartmentPlansAsync`, admin-getriggert, kein kleiner SQL-Hebel) bleibt zyklusuebergreifend offen; Coverage fuer EntraDirectorySync-Batch-Helfer (`UpsertDirectoryIdentitiesBatch`/`InsertGroupMembershipsBatch`) nach LQ2-Z3 verschoben — `private` hinter 2.4k-Z. `SyncAllAsync`, saubere Test-Isolation verlangt File-Split + Graph-Stub. #6 (Departments/Rollen Pagination) wegen FE-Folgen ohne Last-Trigger nicht in Z8 angefasst. Reine Doku-Aenderung. | Naechster Schritt: neuen Review-Zyklus eroeffnen, sobald klarer Hebel feststeht (Kandidaten: LQ2-Z3-Split inkl. Z8-2.3-Coverage, #6 Pagination bei Last-Trigger, oder neue Befunde). |
| 2026-05-05 | Z8-4.1 COVERAGE BULK-RECIPIENT | done | `api/API.Tests/PostgresRepositorySharedHelpersIntegrationTests.cs` (neu), `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Direkte Coverage fuer `PostgresRepositorySharedHelpers.LoadActiveUserNotificationRecipientsBulk` (Z8-3.1) ueber die bestehende `PostgresWorkflowRepositoryIntegrationCollection`-Fixture: (a) Rolle→PreferredPath-Mapping (`/workflows`/`/supervisor`/`/tasks/my`) inkl. `is_active = TRUE`-Filter und Skip fuer unbekannte Ids; (b) Empty-Input → leere Map; (c) `BTRIM`/`COALESCE` fuer `notification_email` und `external_key`. EntraDirectorySync-Batch-Helfer (`UpsertDirectoryIdentitiesBatch`/`InsertGroupMembershipsBatch`) bewusst nicht abgedeckt: `private` hinter 2.4k-Z. `SyncAllAsync`, Reflection-Probing waere fragil; saubere Test-Isolation verlangt LQ2-Z3-Split + Graph-Stub. Grenze offen benannt. | Lokal nicht ausfuehrbar (kein Docker, kein Postgres auf 127.0.0.1:26432) — selbe Fixture-Gating wie alle existierenden Integration-Tests. Build green; 364/364 Non-Integration-Tests gruen. |
| 2026-05-05 | Z8-3.2 #8 ROTATIONTASKGENERATION DEFER | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Hotspot #8 (`RotationTaskGenerationService.RegenerateDepartmentPlansAsync`, `api/API/Services/RotationTaskGenerationService.cs:53-64`) geprueft und bewusst **deferred** statt halbgaar optimiert. Begruendung: admin-getriggerter Pfad (Department-Regeneration), kein Hot-/Sweep-/Timer-Pfad, keine offene Last-Beschwerde. Pro Plan ruft die Schleife `SynchronizeRotationGeneratedTasks(planId)` — eine transaktionale Multi-Step-Sync (Diff vs. Bestand, Insert/Update/Delete generierter Tasks, Audit). Bundling mehrerer Plaene in eine SQL-Schicht waere genau der untersagte breite Umbau an `SynchronizeRotationGeneratedTasks` und den Repository-Pfaden; Parallelisierung der Schleife setzt mehrere transaktionale Sync-Pfade auf dieselbe Tx-Grenze und ist im aktuellen Pool-/Tx-Modell riskant. Kein Code-Change. Z8-3 damit geschlossen (Z8-3.1 done, Z8-3.2 deferred). #8 in zyklusuebergreifend offene Befunde verschoben. Naechster Schritt: Z8-4 (Test-Coverage). | Re-Bewertung nur bei konkretem Last-Trigger oder wenn `SynchronizeRotationGeneratedTasks` ohnehin aus anderem Grund angefasst wird. |
| 2026-05-05 | Z8-3.1 #5 VERIFIKATION + #7 RECIPIENT-BULK | done | `api/API/Repositories/PostgresRepositorySharedHelpers.cs`, `api/API/Repositories/PostgresWorkflowNotificationDispatchOperations.cs`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Hotspot #5 (`WorkflowVisibilityService.ApplyWorkflowTaskPermissions`) verifiziert: false positive als DB-Lasthebel — `CanUpdateTaskStatus`/`CanDecideTaskApproval`/`CanAddTaskComment` arbeiten in `AuthorizationPolicyService` rein auf `CurrentUser` (Rollen/Permissions/Responsibilities) und `TaskWithWorkflowDto` (Status, Assignments), keine Repo-Hits, kein N+1. Aus Z8 herausgenommen. Hotspot #7: neuer Bulk-Helper `PostgresRepositorySharedHelpers.LoadActiveUserNotificationRecipientsBulk(@userIds bigint[])` macht in **einer** Query (CTE-basiert mit `effective_roles`-Aggregation pro User) das Mapping `userId → (DisplayName, Email, IdentityKey, PreferredPath)`. `BuildReadyTaskNotificationPreviewTargetsAsync` ruft ihn einmal pro Preview statt pro Recipient. Selber Helper auch in `CreateReadyTaskNotificationsAsync` genutzt (identisches Recipient-Pattern, minimaler Mitnahme-Refactor wie Auftrag erlaubt). Inactive Users werden weiterhin uebersprungen (CTE `targets` filtert `is_active = TRUE`). Verifikation: `dotnet build API` (0 Warn/0 Err), `dotnet build API.Tests` (3 vorhandene CS8602-Warnungen, 0 Err), `dotnet test --filter StartupValidation` (7/7 gruen). | Direkter Unit-/Integration-Test fuer den Preview-Pfad fehlt im aktuellen Bestand — semantische Aequivalenz beruht auf der Spalten-/Filter-Treue zur alten Single-Row-`LoadActiveUserNotificationRecipient` (gleiche `effective_roles`-Logik, gleicher PreferredPath-Decision-Tree, gleiches `is_active = TRUE`-Filter). Coverage-Nachzug in Z8-4. |
| 2026-05-05 | Z8-2.3 ENTRADIRECTORYSYNC GROUP/MEMBER BATCH | done | `api/API/Services/EntraDirectorySyncService.cs`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Hotspot #4: per-Member Round-Trips in `SyncAllAsync` ersetzt. Pro Group werden valide `User`-Members eingesammelt; `UpsertDirectoryIdentitiesBatch` macht einen `INSERT … SELECT FROM unnest(uuid[],text[],text[],text[],bool[],text[],int[]) ON CONFLICT … RETURNING id, entra_object_id` (deduped per `entra_object_id`, last-wins) und liefert ein Mapping; `InsertGroupMembershipsBatch` macht einen `INSERT … SELECT @groupId, t.identity_id FROM unnest(@identityIds::bigint[]) ON CONFLICT DO NOTHING`. `ClearGroupMemberships` bleibt der Reset-Schritt vorab. Counts bleiben fachlich gleich (`identitiesSynced`/`membershipsSynced` += `validUsers.Count` pro Group), Fehlerpfad pro Group unveraendert (status=`partial`). LQ2-Z3 (File-Split) bleibt deferred; nur der Lastpfad wurde angepasst. | Keine Unit-Tests fuer diesen Pfad vorhanden — verifiziert ueber `dotnet build` (API + API.Tests, 0 Fehler) und `dotnet test --filter StartupValidation` (7/7). Echte Graph-/DB-Last-Verifikation nicht moeglich ohne Live-Tenant + DB; semantische Aequivalenz beruht auf der Spalten-Mapping-Treue zur alten Single-Row-`UpsertDirectoryIdentity`-/`InsertGroupMembership`-Logik. Grenze offen benannt. |
| 2026-05-05 | Z8-2.2 ROTATIONNOTIFICATION SWEEP+APPLY HAERTEN | done | `api/API/Repositories/IRotationRepository.cs`, `api/API/Repositories/PostgresRotationRepository.NotificationOperations.cs`, `api/API/Services/RotationNotificationService.cs`, `api/API.Tests/RotationNotificationServiceTests.cs`, `api/API.Tests/RotationPlanningServiceTests.cs`, `api/API.Tests/RotationTemplateAdminServiceTests.cs`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Hotspot #2+#3 als gemeinsamer Slice. `GetDispatchableRotationNotifications` erhaelt optionale `limit`/`excludeNotificationIds`-Parameter (Postgres-`LIMIT` + `id <> ALL(@excludeIds)`). `RotationNotificationService.ExecuteDailySweepAsync` schleift mit `DispatchBatchSize=200`, dispatcht batchweise und aggregiert created/dispatched/failed/disabled korrekt; SystemEventLog-Eintrag bleibt einer pro Sweep, Fruehausstieg ohne Targets weiter ohne Log. `ApplyRotationNotificationDispatchResults` lade Metadata fuer alle IDs in einer Query (`id = ANY(@ids)`) statt N×SELECT, und schreibt Status/Attempts/Sent-At/Error in einem `UPDATE … FROM unnest(@ids,@statuses,@attempted,@errors)` zurueck; Audit-Inserts weiter pro Result, aber ohne Per-Item-SELECT. Stubs in 3 Testdateien an die neue Signatur angepasst; neuer Test `ExecuteDailySweepAsync_ProcessesDispatchableNotificationsInBatches` verifiziert 250 Targets → 2 Sender-Calls (200+50) und 250 angewandte Results. Tests: `dotnet test --filter RotationNotificationServiceTests` 3/3 gruen, `RotationPlanningServiceTests`+`RotationTemplateAdminServiceTests` 17/17 gruen. | Keine API-Vertragsaenderung, keine FE-Folge. `failed`-Notifications koennen ueber den Sweep hinweg im Backlog bleiben (matchen weiter das WHERE) — werden im naechsten Sweep erneut versucht; das in-Memory-`processedIds`-Set verhindert Doppelversand innerhalb eines Sweeps. Bei extrem grossen Backlogs (≫10k) waechst `processedIds` linear, fuer aktuelle Lastvolumina unkritisch. |
| 2026-05-05 | Z8-2.1 WORKFLOWCATALOG N+1 AUFLOESEN | done | `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API.Tests/WorkflowEndpointsTests.cs`, `api/API.Tests/WorkflowLifecycleServiceTests.cs`, `api/API.Tests/AdminWorkflowDefinitionConfigEndpointsTests.cs`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Hotspot #1: N+1 ueber `IsManagerCreatableDefinition` aufgeloest. Neue Repository-Methode `GetManagerCreatableDefinitionKeys()` lifert die Set-Liste der `allows_manager_creation = TRUE`-Definition-Keys mit einem SQL-Statement; `WorkflowCatalogService.GetStartableWorkflowDefinitionsAsync` ruft sie lazy genau einmal pro Request statt N×Single-Lookup. Fall fuer Manager wird vor dem Lookup geprueft (kein Bulk-Call wenn nicht Manager und keine `WorkflowCreate`-Permission). `WorkflowRuntimeService.IsManagerCreatableDefinition`-Pfad bleibt unveraendert (Single-Key-Validation beim Create). Tests: neuer FactsTest `StartableWorkflowDefinitionsEndpoint_ManagerUsesBulkLookupWithoutPerDefinitionCalls` belegt 1× Bulk + 0× Single-Call. | Keine API-Vertragsaenderung, keine FE-Folge. Bulk-SQL ist ungefiltert und liefert alle Keys mit `allows_manager_creation = TRUE` — bei sehr grossem Definitionsbestand evtl. spaeter LIMIT/Filter pruefen, aktuell unkritisch. |
| 2026-05-05 | Z8-1.2 HOTSPOT-AUSWAHL + SLICE-PLAN | done | `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Top-3 fuer Z8-2.x bestaetigt: **#1** `WorkflowCatalogService` (Z8-2.1, opus), **#2+#3** `RotationNotificationService` Sweep+Apply als gemeinsamer Slice (Z8-2.2, sonnet, Begruendung: Bundling vermeidet halbe Migration zwischen LIMIT und Batch-Update), **#4** `EntraDirectorySyncService` Group/Member-Schleifen als Z8-2.3 (sonnet) — bewusst hinter den nutzersichtbaren Hotspots, aber explizit in Top-3, weil thematisch Pushdown und nicht Sweep-Resthebel. #5/#7/#8 in Z8-3 verschoben. #6 (Departments/Rollen) wegen FE-Folgen ohne Trigger nicht in Z8-2. Kein Code-Change, keine Tests noetig (nur Markdown). | FE bleibt unberuehrt; FRONTEND_TODO.md nicht angefasst. Wenn Z8-2.x doch API-Vertraege bricht (unerwartet), FE-Folgen erst dann eintragen. |
| 2026-05-05 | GIT-HYGIENE-CLEANUP | done | `.gitignore`, `CODEX_SYNC.md` | Git-Hygiene gegen aktuellen Repo-Stand geprueft. `quality-gates.yml` passt weiter zu `api/API.Tests` und `web/package.json`, daher keine Workflow-Aenderung noetig. `.gitignore` um `.tmp-test-bin/` sowie lokale Obsidian-Workspace-Dateien (`KauthWorkflow/.obsidian/app.json`, `workspace.json`) ergaenzt. Offensichtlicher lokaler Muell entfernt: `.tmp-*`-Build/Test-Caches, `artifacts/`, `handoff/`-ZIP, Root-Logs, Root-`node_modules/` sowie versehentliches Root-Node-Setup (`package.json`, `package-lock.json`) aus dem Repo entfernt. | `OnboardingERD.pgerd` bleibt bewusst unberuehrt: aktuell unreferenziert, aber nicht eindeutig als Muell klassifizierbar. Falls gewuenscht spaeter gesondert entscheiden (behalten, verschieben oder entfernen). |
| 2026-05-05 | VAULT-DOCS-QUALITY-OVERHAUL | done | `KauthWorkflow/00 Start.md`, `KauthWorkflow/Domäne/Begriffe.md`, `CODE_REVIEW.md`, `CODE_REVIEW_ARCHIVE.md`, `CODEX_SYNC.md`, `CODEX_SYNC_ARCHIVE.md`, `MEMORY.md`, `TODO.md`, `PROJECT_CONTEXT.md`, `PROJECT_STRUCTURE.md`, `CLAUDE_CONTROL.md`, `CLAUDE.md`, `DOCS_CONTROL.md`, `FRONTEND_TODO.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `KauthWorkflow/Arbeit/KI-Workflow.md`, `KauthWorkflow/Lernen/01-Die-grosse-Landkarte.md` | Doku-Qualitaet fuer Mensch und KI strukturell angehoben: fehlenden Vault-Einstieg `00 Start.md` angelegt, Glossar `Begriffe.md` ergaenzt, aktive vs. historische Review-/Sync-Inhalte in `*_ARCHIVE.md` getrennt, zentrale Root-Dateien mit einheitlichem Zweck-/Primaerquelle-/Nicht-verwenden-Header versehen und `MEMORY.md`/`TODO.md` deutlich auf aktive Arbeit reduziert. | Die neue Struktur ist absichtlich kompakter. Falls einzelne Teams spaeter mehr fachliche Detailtiefe in `PROJECT_STRUCTURE.md` oder `Code-Review-Status.md` brauchen, dort gezielt ergaenzen statt die aktiven Steuerungsdateien wieder aufzublaehen. |
| 2026-05-05 | MD-CLEANUP-Z8 | done | `MEMORY.md`, `TODO.md`, `CODE_REVIEW.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `KauthWorkflow/Architektur/Migrationspfad.md`, `KauthWorkflow/Lernen/06-Mini-Projekt-Todo-Board.md`, `KauthWorkflow/Lernen/07-Einarbeitungsplan-im-echten-Repo.md`, `CODEX_SYNC.md` | Markdown-Cleanup auf aktuellen Stand gebracht: `MEMORY.md` auf aktiven Zyklus 8 reduziert, `TODO.md` und Review-Dateien von Altstaenden bereinigt, `Migrationspfad.md` Schritt 7 auf erledigt gesetzt. | Historische Handoff-Eintraege liegen jetzt in `CODEX_SYNC_ARCHIVE.md`, falls weitere Verdichtung noetig wird. |
| 2026-05-05 | LEARNING-PATH-ONBOARDING | done | `KauthWorkflow/Lernen/07-Einarbeitungsplan-im-echten-Repo.md`, `KauthWorkflow/Lernen/06-Mini-Projekt-Todo-Board.md`, `CODEX_SYNC.md` | Neue Lerneinheit fuer die Einarbeitung ins echte Repo angelegt; Mini-Projekt-Einheit um den Verweis auf den Einarbeitungspfad ergaenzt. | Bei formalerem Team-Onboarding den Lernpfad mit echten Einstiegsaufgaben abgleichen. |
| 2026-05-05 | CLAUDE-CONTROL | done | `CLAUDE_CONTROL.md`, `DOCS_CONTROL.md`, `CLAUDE.md`, `PROJECT_STRUCTURE.md`, `KauthWorkflow/Arbeit/KI-Workflow.md`, `CODEX_SYNC.md` | Operative Claude-Steuerung unter Codex-Orchestrierung eingefuehrt: Rollen, Slice-Workflow, Doku-Pflicht, Commit-Regel. | `CLAUDE_CONTROL.md` als Primaerquelle halten; keine konkurrierenden Regeltexte aufbauen. |
| 2026-05-05 | Z8-1.1 HOTSPOT-INVENTUR | done | `CODE_REVIEW.md`, `TODO.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Reine Inventur fuer Z8 (Skalierbarkeit). 9 nummerierte Hotspots in `CODE_REVIEW.md` § Z8-1.1 dokumentiert (Datei/Symbol, Art a/b/c/d, Kardinalitaet, Prio). Top-3-Empfehlung fuer Z8-1.2: #1 `WorkflowCatalogService.GetStartableWorkflowDefinitionsAsync` (N+1 ueber `IsManagerCreatableDefinition`), #2+#3 `RotationNotificationService` Daily-Sweep + Apply-Dispatch-Results (kein LIMIT, Select+Update pro Item), #4 `EntraDirectorySyncService.SyncAllAsync` Group×Member-Schleifen. False positives gekennzeichnet (Lifecycle-Service, Z2-Workflow-Filter, Z7-Validatoren, Search-Endpoints). Kein Code-Change. | Z8-1.2 muss vor jedem Pushdown-Slice die Top-Auswahl bestaetigen. #6 (Departments/Rollen ohne Pagination) kann FE-Folgen erzeugen — erst bei Z8-1.2 entscheiden. |
| 2026-05-05 | CLAUDE-REVIEW-ZYKLUS-8 | done | `CODE_REVIEW.md`, `TODO.md`, `KauthWorkflow/Stand/Code-Review-Status.md`, `CODEX_SYNC.md` | Zyklus 8 eroeffnet. Thema: Skalierbarkeits- & Last-Haertung. Erste Schritte: Z8-1.1 Inventur, Z8-1.2 Hotspot-Auswahl, Z8-2.x SQL-Pushdown/Pagination, Z8-3 Sweep-/Dispatch-Performance, Z8-4 Tests. | Z8-1.1 muss vor jeder Umsetzung passieren, sonst wird Hotspot-Auswahl zum Bauchgefuehl. |
| 2026-05-08 | Z16-S2 BE-Permission-Vertrag + Admin-People-Endpunkt | done | `api/API/Authorization/IAuthorizationPolicyService.cs`, `AuthorizationPolicyService.cs`, `api/API/Endpoints/WorkflowMasterDataEndpoints.cs`, `api/API/Endpoints/AdminPeopleEndpoints.cs`, `api/API/Endpoints/AdminEndpoints.cs`, `api/API/Contracts/PeopleDirectoryDtos.cs`, `api/API/Repositories/IWorkflowRepository.cs`, `api/API/Repositories/PostgresWorkflowRepository.MasterDataOperations.cs`, `api/API/Services/IWorkflowCatalogService.cs`, `api/API/Services/WorkflowCatalogService.cs`, `api/API.Tests/AdminPeopleEndpointsTests.cs`, `api/API.Tests/AuthorizationPolicyServiceTests.cs`, `CODE_REVIEW.md`, `TODO.md`, `MEMORY.md`, `KauthWorkflow/Stand/Code-Review-Status.md` | CanAccessPeopleDirectory-Policy (HR + Admin) eingefuehrt. /people/search von CanCreateWorkflow auf CanAccessWorkflowOverview umgestellt (Breaking-Risk geprueft und in CODE_REVIEW.md dokumentiert: enger Edge-Case mit nur workflow.create-Permission ohne Rolle). GET /admin/people mit P1-Hull (AdminListPageDto, search/sort/pagination) als neuer Listenendpunkt. PersonDirectoryItemDto als kompaktes Listen-DTO. 493 Tests gruen. | Z16-S3 FE-Navigation (peopleDirectory-Feature, PersonSearchPage, Nav-Eintrag HR+Admin, Breadcrumb-Update) ist naechster Schritt. |
